# Long-Horizon Dynamics 长期记忆

最后更新：2026-05-16 CST。

本文档是 Codex 在本项目里的压缩长期记忆。完整历史流水账已经归档到：

- `docs/archive/Prompt_full_before_compaction_2026-05-10.md`

新聊天开始时优先阅读：

- `AGENTS.md`
- `MODEL_DEV_CURRENT.md`
- `MODEL_DEV_HANDOFF.md`
- 本文件 `Prompt.md`

同一窗口或 heartbeat 自动化已有当前状态时，优先只读 `MODEL_DEV_CURRENT.md`；只有状态冲突、上下文压缩后状态不明或需要历史复盘时，才完整读取本文件。

不要在这里记录密钥、token、私有登录信息。

## 当前状态（2026-05-16 12:42 CST）

- 当前无已确认 active training/evaluation。远程 `desktop-0r0t1bo-1 / 100.106.154.6` 在 `tailscale status` 中显示 active，但本机 Codex 对 `gpu4060`/`gpu4060-ts` SSH 与 `tailscale ping 100.106.154.6` 均 timeout。局域网 `192.168.1.108` 可 ping，22 端口返回 Ubuntu OpenSSH banner，公钥认证可通过，但远端 session 创建/命令执行阶段超时；单独执行 `hostname`/`pwd`/`whoami` 也会挂住。按远程 WSL/sshd/session 创建异常处理，不判定实验失败；已启动网络/SSH 诊断 subagent，主线暂时等待远程 shell 恢复。
- `modeldev_20260516_tcnlstm_geoactctx_H10_nulltrust_from_attitude_e3_p1`（`scale_init=0.01`）已在 e0 validation 前按新 Pro 建议停止并替换，原因是 additive side residual 首轮建议用 `0.003/0.005`，`0.01` 留给 FiLM/gate；该候选未读 horizon/test，artifacts 保留。
- `smoke_20260516_tcnlstm_geoactctx_H10_nulltrust_s005_from_attitude_e3_p1` 已启动，命令日志显示 one-batch train/valid finite，但远程在读取 `train_summary.json` 前不可达；下一轮先确认 summary/CSV gate stats 后再启动正式训练。
- 下一步正式候选：`modeldev_20260516_tcnlstm_geoactctx_H10_nulltrust_s005_from_attitude_e3_p1`。配置：TCNLSTM H10，`unroll_length=50`，只训 `tcnlstm_side_history`，`selector_prior=null_short`，`tcnlstm_side_history_scale_init=0.005`，`warmup_lr=1.5e-6`、`cosine_lr=4e-7`，batch `16`、accumulate `32`、epochs `4`，`limit_train_batches=0.25`、`limit_val_batches=0.5`，past-only `dmot/vbat` context，不用 future context，不用 `a/alpha`。
- 当前路线按 2026-05-16 用户/Pro 建议：TCNLSTM anchor-first，不急着 seq2seq。H10 有正信号才上 H20 conservative；H10 v/omega 有信号但 q/state_mse 被破坏则改 FiLM/gate；H10 无 v/omega 信号且 q/omega 同步变坏则转 actuator-only context；true seq2seq `Delta v[1:F]/Delta omega[1:F]` 只作最后 fallback。
- Gate：TCNLSTM H10 attitude reference `best_valid_loss=0.4615005`、`valid_q=0.0412516`、`valid_v=0.1503205`、`valid_omega=0.2379201`。Green：e0 `<=0.46155`，或 e1 `<=0.46145` 且 q/omega/v 至少两个指标改善；Yellow：`0.46155 < e0 <=0.46220` 且 q/omega 没有同步明显变坏，最多观察 e1；Red：e0 `>0.46220`，或 q/omega/state_mse 同步明显变坏，停止不读 horizon/test。必须记录 `side_residual_norm`、gate mean、null gate ratio、是否作用到 q residual、是否只影响 v/omega gate。

### code snapshot - 2026-05-16 12:55 CST

- 类型：architecture/protocol snapshot。
- 目的：把当前 dirty working tree 固化为可追踪版本，供 GPT Pro review。
- base SHA：`27be3448d7bbf3bda7f48522fa03c747477f8d1b`。
- 改动文件：
  - `AGENTS.md`
  - `MODEL_DEV_CURRENT.md`
  - `MODEL_DEV_HANDOFF.md`
  - `Prompt.md`
  - `scripts/config.py`
  - `scripts/eval.py`
  - `scripts/train.py`
  - `scripts/dynamics_learning/data.py`
  - `scripts/dynamics_learning/lighting.py`
  - `scripts/dynamics_learning/registry.py`
  - `scripts/dynamics_learning/models/grutcn.py`
  - `scripts/dynamics_learning/models/tcnlstm.py`
- 主要变化：
  - 补齐 `Git / Commit / Experiment Version Rules`，明确 Architecture Mode / Tuning Mode、dirty tree、summary version、GPT Pro review 和禁止 `git add .` 规则。
  - 固化当前 model sprint architecture/protocol snapshot：eval no-silent-skip guard、state_update_mode wiring、history-only `dmot/vbat` context、adaptive history stats、GRUTCN adaptive history side branch、TCNLSTM side-history/null-trust switches 和 train/eval summary 字段。
  - 后续正式训练 / locked audit / GPT Pro review 必须可追溯到 commit SHA。
- 检查：
  - py_compile: passed
  - git diff --check: passed
- commit:
  - `4f7309615dfe64ec59f2aa30ede17545202a3749` `arch: snapshot model sprint workflow and protocol changes`
- 当前 active:
  - none
- 下一步：
  - 等 GPT Pro review 当前 snapshot 后，再决定是否启动下一轮候选。

## 代码/协议变更（2026-05-15 23:40 CST）

- 改动文件：`scripts/dynamics_learning/models/tcnlstm.py`、`scripts/dynamics_learning/registry.py`。
- `TCNLSTM.forward()` 现在接受 `context_hist=None`，避免 `DynamicsLearning.forward(..., context_hist=...)` 在 TCNLSTM 上 TypeError。
- 新增默认关闭的 `adaptive_history_context` 支持：`tcnlstm_side_history_*` 分支把 H50 raw state/control、SO(3) geometric motion delta 和 past-only `dmot/vbat` 编码为 side context，通过 `null/short/mid/full` selector 与 reliability gate 产生 zero-init residual 注入 `head_input`。旧 checkpoint 初始为 no-op；旧路径不启用时行为不变。
- `registry.py` 将 `adaptive_history_context`、窗口参数和 `history_context_dim` 传给 TCNLSTM。检查：本地/远程 `py_compile` 和 `git diff --check` 通过；local/remote SHA256 一致；远程 dummy forward 与 one-batch smoke 通过。
- 2026-05-16 补充默认不改变旧行为的 null-trust 开关：`--tcnlstm_side_history_scale_init`（默认 `0.05`）和 `--tcnlstm_side_history_selector_prior uniform|null_short`（默认 `uniform`）。本地/远程 `py_compile`、`git diff --check` 和 H10 null-trust smoke 均通过；使用该 trick/协议变更的理由是上一轮 H50 side branch 自由 selector 倾向打开 full context 且 validation 退化，本轮需要先验证“弱注入 + null/short 先验”能否保住 anchor。

## 最近停止候选（2026-05-16 10:18 CST）

- `modeldev_20260515_tcnlstm_geoactctx_H50_from_attitude_e3_p1`：early_stopped=true，best=`0.4656468630`（epoch=2），e2 `valid_q_loss_epoch=0.0416224226`、`valid_v_loss_epoch=0.1515640318`、`valid_omega_loss_epoch=0.2400307506`，对比 TCNLSTM H10 attitude baseline 整体回退，按 gate 不跑 horizon/test；训练 tmux 已退出，GPU watch 已清理，artifacts 保留。

## 最近停止候选（2026-05-15 23:32 CST）

- `modeldev_20260515_grutcn_adaptivehist_H50_from_vomega_e3_p1` 已按 e1 validation gate 停止，不跑 horizon/test。e0->e1 `valid_loss_epoch=0.6879014373 -> 0.6870185733`，e1 `valid_v=0.1602650136`、`valid_q=0.0390152447`、`valid_omega=0.2308619916`、`valid_state_mse=0.2525024712`，仍明显差于 current GRUTCN best；adaptive gate e1 `null=0.0084`、`short=0.7560`、`full=0.1937`、`gate_saturation=0.3051`，长历史 side branch 没显示泛化收益。训练/watch tmux 已停止，artifacts 保留。

## 代码/协议变更（2026-05-15 16:45 CST）

- 改动文件：`scripts/dynamics_learning/models/grutcn.py`、`scripts/config.py`、`scripts/dynamics_learning/registry.py`、`scripts/dynamics_learning/lighting.py`、`scripts/train.py`。
- 新增 `--adaptive_history_context`、`--adaptive_history_short_window`、`--adaptive_history_mid_window`，默认关闭保持旧行为。GRUTCN 新增 checkpoint-safe `adaptive_history_*`：H20 anchor 主路径不变，长历史只走 raw-token Transformer side branch、`null/short/mid/full` selector、reliability gate、zero-init residual。
- `DynamicsLearning` 新增 `log_adaptive_history_stats()`，训练/验证 CSVLogger 记录 gate mean/std/saturation 与 reliability mean/std/saturation；`train_summary.json` 写入 adaptive history 配置。
- 检查/同步：本地 `py_compile` 通过；远程 `python3 -m py_compile` 通过；远程 conda env dummy forward finite；远程脚本级 smoke 通过。同步中曾误建远程 `scripts/scripts/` 副本，已只删除该误同步副本并重新同步正确路径。

## 代码/协议变更（2026-05-15 14:10 CST）

- 改动文件：`scripts/dynamics_learning/models/grutcn.py`、`scripts/config.py`、`scripts/dynamics_learning/data.py`、`scripts/dynamics_learning/registry.py`、`scripts/dynamics_learning/lighting.py`、`scripts/train.py`、`scripts/eval.py`。
- 新增 `--history_context_mode none|dmot_vbat`，默认 `none` 保持旧行为；`dmot_vbat` 模式下 `DynamicsDataset` 只选历史 `dmot/vbat` 作为 `context_hist`，`DynamicsLearning.full_state_rollout()` 将 `context_hist` 传给模型，GRUTCN 新增 gated/zero-init `history_context_*` 分支修正 `history_context`；train/eval summary 记录 `history_context_mode` 与 `history_context_dim`。
- 检查/同步：本地 `py_compile` 通过；远程 `py_compile` 通过；local/remote SHA256 一致；远程 smoke 通过，确认 `history_context_dim=5`、trainable count `19`、one-batch train/valid finite。

## 刚停止候选（2026-05-15 14:05 CST）

- `modeldev_20260515_grutcn_dmotvbatctx_H20_from_vomega_e3_p1` 已按 validation gate 停止，不跑 horizon/test。结构为 `history_context_mode=dmot_vbat`、past-only `dmot/vbat` context、`state_update_mode=residual_full_state`，只训练 `history_context`（trainable count `19`）。e0/e1 `valid_loss_epoch=0.6827901006 -> 0.6828653216`，e1 `valid_v=0.1596235037`、`valid_omega=0.2291074544`、`valid_q=0.0388034955`、`valid_state_mse=0.2502800524`，相对 current best 的 v/omega/q/state_mse 均无正信号。训练 tmux/GPU watch 与匹配实验路径的训练进程已停止，artifacts 保留。结论：当前 tiny context branch 失败；下一步先诊断 context 字段/归一化/分支表达力，再决定 adaptive history selector 或 TCNLSTM 迁移。
- `modeldev_20260515_grutcn_rawtokgeo_H20_from_vomega_e3_p1` 已按 validation gate/early stop 判失败，不跑 horizon/test。训练 early-stopped，best 停在 e0：e0/e1/e2 `valid_loss_epoch=0.6829264164 -> 0.6830326319 -> 0.6831049919`，e2 `valid_v=0.1597713530`、`valid_q=0.0388241075`、`valid_omega=0.2290891409`、`valid_state_mse=0.2502748668`；相对 current best 的 `valid_loss/v/q/state_mse` 均无正信号。训练 tmux 已退出，GPU watch 已停止，artifacts 保留；horizon/test 未读取。结论：SO(3) raw-token representation fix 单独未带来 validation 正信号，不做 rawtokgeo continuation，转 `dmot/vbat` context。
- `modeldev_20260515_grutcn_softvomega_H20_from_hard_e1_p1` 已按 e0 validation gate 停止，不跑 horizon/test。e0 `valid_v=0.1629530936`、`valid_omega=0.2299810797`、`valid_q=0.0454349183`、`valid_state_mse=0.2749448121`，相对 hard e1 和当前 GRUTCN best 均明显恶化。只停止对应训练/GPU watch tmux，artifacts 保留。
- `modeldev_20260514_grutcn_hardvomega_multistep_H20_from_vomega_e3_p1` 已按 gate 停止，不跑 horizon/test。e1 曾反转，但 e2 把 v/omega 信号打回：e2 `valid_v=0.1598088145`、`valid_omega=0.2292566448`，相对当前 best reference 明显回退。只停止对应训练/GPU watch tmux，artifacts 保留。
- `modeldev_20260514_grutcn_multistepdelta_u50_H20_from_vomegaweight_e3_p1` 已按 validation gate 停止，不跑 horizon/test。e0/e1 的 v/omega/q 相对当前 best 没有反转；e1 `valid_loss_epoch=0.4708360136`、`valid_v=0.1596593559`、`valid_q=0.0388144702`、`valid_omega=0.2290989012`。只停止对应训练/GPU watch tmux，artifacts 保留。

## 代码/协议变更（2026-05-14 17:55 CST）

- 改动文件：`scripts/eval.py`、`scripts/config.py`、`scripts/dynamics_learning/lighting.py`、`scripts/train.py`。
- `eval no-silent-skip guard`：`eval.py` 现在会在 eval 开始前检查 `unroll_length >= max(eval_horizons)`，避免再出现 `unroll_length=2` 却请求 h50 时 silent skip；`horizon_summary.json` 记录 `actual_unroll_length`、`requested_eval_horizons`、`computed_eval_horizons`、`skipped_eval_horizons` 和 `state_update_mode`。
- `state_update_mode`：新增 `residual_full_state`（默认旧行为）、`hard_vomega_kinematic`、`soft_vomega_kinematic`；hard 模式只用 `delta_v/delta_omega` 更新 v/omega，再用梯形积分恢复 p 和 q；soft 模式在 hard 结果上加小 `delta_p/dtheta` residual，scale 由 `--state_update_soft_residual_scale` 控制。`train_summary.json` 会写入这两个字段。
- 检查/同步：本地 `python -m py_compile scripts/config.py scripts/dynamics_learning/lighting.py scripts/eval.py scripts/train.py` 通过；已用 `rsync -avR` 同步到 `gpu4060`；本地/远程 SHA256 一致；远程 py_compile 通过；远程 smoke 确认 guard 会拒绝 `unroll_length=2 + eval_horizons=1,10,25,50`，50 步 summary 字段正常，hard/soft kinematic update 输出有限且 quaternion norm 为 1。
- 后续决策：hard/soft vomega 已完成并失败，保留为 ablation 结论；当前按 2026-05-15 Pro 更新，先完成 rawtokgeo，再依次考虑一次 conservative continuation、history-only `dmot/vbat` context、adaptive history selector、TCNLSTM 迁移，true seq2seq 放到最后 fallback。

## GPT Pro 策略更新（2026-05-15，覆盖优先级）

- 来源：用户提供的 `/Users/lixiang/Desktop/codex_priority_reorder_keep_seq2seq_last.md`。核心判断：`hard_vomega_kinematic` / `soft_vomega_kinematic` 失败只能说明当前“在已有 full-state residual head 上硬接/软接 v/omega kinematic update”的实现失败，不能否定 `Delta v / Delta omega` 父级思想；但 direct finite-horizon seq2seq 更像 sequence forecasting / future-control decoder，不应作为当前论文第一创新点。
- 新优先级：1) 完成当前 `raw_token_geometric_delta`；2) 若有接近正信号，最多做一次 conservative trainable scope 或短 continuation；3) history-only `dmot/vbat` context branch；4) adaptive history selector / multi-scale reliability gate；5) 将成熟 geometry/context trick 迁移到 `tcnlstm.py`；6) 只有前述方向均无 horizon/test 聚合收益时，才启动完整 true seq2seq `Delta v[1:F] / Delta omega[1:F]` predictor。
- 论文叙事：优先建立 SO(3)-aware geometric history representation、actuator-aware latent context、自适应历史选择这三个更符合 neural system identification / learned dynamics 的创新点；seq2seq 作为 late-stage future-control decoder extension 或 fallback。
- Gate 更新：rawtokgeo 是 representation fix，除非 e0/e1 多项关键指标灾难性恶化，否则至少看 e1 或 natural finish；`dmot/vbat` context 要记录字段、past-only、shape、gate/zero-init、leakage 风险和 validation/horizon 结果；adaptive selector 要记录 gate mean/std/saturation；true seq2seq 不能用 e0 直接杀死，至少给 1-2 个完整 validation 周期、独立 LR/warmup 和结构匹配 trainable scope。

## GPT Pro 建议并入路线（2026-05-14）

- 来源：用户提供的 `/Users/lixiang/Desktop/codex_delta_vomega_kinematic_rollout_plan.md`。该建议中的 eval guard、`state_update_mode` 和 raw-token 几何修正已执行/保留；其“优先 Delta v / Delta omega 主预测”的排序已被 2026-05-15 Pro 策略更新覆盖，seq2seq 改为最后 fallback。
- 最高优先级协议修复：`eval.py` 必须防止 silent skip。若请求 `eval_horizons=1,10,25,50`，则 `unroll_length` 必须至少覆盖 50；summary 要写 `actual_unroll_length`、`requested_eval_horizons`、`computed_eval_horizons`、`skipped_eval_horizons`。若 h50 被跳过，不能作为 locked audit 或 long-horizon 结论。
- 模型路线：新增默认保持旧行为的 `state_update_mode`，包括 `residual_full_state`、`hard_vomega_kinematic`、`soft_vomega_kinematic`。hard 模式忽略 `delta_p/dtheta`，只学习 `delta_v/delta_omega`，然后用梯形积分恢复 `p`、用 `omega` 梯形积分经 SO(3) exponential 恢复 `q`；soft 模式只在 hard 的 v/omega 有正信号但 p/q 回退时加入小 `delta_p_res/delta_theta_res`，是否保留由 `E_v/E_omega/E_q/MSE_1_to_F` 决定。
- raw-token 修正：当前 raw-token `dx` 对 quaternion 做欧氏差分，后续应加入 `raw_token_geometric_delta`，用 `Log(q_i^{-1} ⊗ q_{i+1})` 表示姿态增量，并同步替换 `dx_last`，保持输入维度兼容。
- context 分支排在后面：只有 hard/geometry 路线有信号后再试 `context_hist`，首轮只用历史 `dmot,vbat`，不使用未来 context，不先用 `a/alpha`。
- 训练策略：smoke 可只训 `multi_step`，正式实验不要长期只训 tiny branch；优先 head-level patterns（`multi_step,raw_token,decoder,correction_gate,context_delta` 等）和更像新结构学习的 LR。若 1-2 个候选不改善 `MSE_1_to_F` 或 h50 `E_v/E_omega`，立即收缩并换结构。

## Prompt.md 写入规则

- 默认使用中文写项目文档、交接记录、实验记录和说明文档；代码标识、命令、路径、实验 id、指标名和必要英文术语保持原样。
- `Prompt.md` 只保存能帮助后续决策的长期记忆，不再记录每 20 分钟一次的完整巡检流水账。
- 活跃实验只保留当前状态、关键门槛、当前最佳 checkpoint、是否允许 audit、下一步决策。
- 已完成实验只保留一段摘要：实验 id、模型/配置、最佳 validation、locked audit 结果、结论、是否保留/清理。
- 只有异常、停止、清理、代码变更、协议变更、trick 使用、locked audit、关键门槛触发才写入本文件。
- 普通巡检只在状态发生变化时写：例如新 best、early stop、失败、OOM、NaN、tmux/进程异常、horizon 文件出现。
- 自 2026-05-11 起，训练/开发阶段允许读取 horizon/test metric 内容，并允许用它来挑选候选、调整结构和改进模型；每次读取都必须记录实验 id、checkpoint、关键 horizon/test 指标、是否影响后续调参/结构决策，以及结论。
- 失败实验确认后可清理，但记录必须包含失败原因、实验 id、tmux、路径、清理范围；只能清理该失败实验的进程、tmux 和产物。
- 文件超过可读范围时，把旧流水账归档到 `docs/archive/`，主文件保留压缩摘要。

## 固定运行规则

- Mac 本地仓库是代码源头：`/Users/lixiang/Developer/long-horizon-dynamics`。
- 远程 GPU host alias：`gpu4060`，当前默认走 Tailscale IP `100.106.154.6`。
- SSH 备用别名：`gpu4060-ts` 同样走 Tailscale；`gpu4060-lan` 保留旧局域网 IP `192.168.1.108`，仅用于同局域网回退排查。
- 远程仓库：`/home/ubuntu/Developer/long-horizon-dynamics`。
- 远程 conda 环境：`dynamics_learning`。
- 远程训练必须在 tmux 里跑。
- 给用户 Linux/SSH 指令时，必须先用中文解释“我要执行什么”“为什么执行”“语法是什么”。
- 代码同步到远程时不要覆盖 `resources/experiments/`。
- 代码修改后要做 syntax/smoke 检查、同步远程，并验证 touched files 的 local/remote hash。

## 当前任务

目标是在 2026-05-10 的 12 小时 sprint 内，让至少一个新 latent-context dynamics 模型在 locked long-horizon rollout 指标上击败原始/旧基线，而不是只赢 validation loss。

2026-05-13 目标更新：`grutcn` / `tcnlstm` 两个新模型不再限制可见历史长度 `history_length`。后续候选可以使用更长历史、多尺度历史、自适应历史选择机制或更强历史记忆结构；比较目标改为超过原始 baseline 在各项 locked long-horizon rollout 指标上的最佳包络，而不是限定在同一个 `H10` / `H20` 设置内比较。若使用比原始 baseline 更长的历史，记录时必须明确标注为 history-expanded 候选，但这在当前目标下允许。历史长度不能简单理解为越长越好；候选优先考虑 learnable gate、attention over history、multi-scale pooling、reliability/slack 权重等机制，让模型自动保留有效历史并抑制过长历史引入的噪声。

主要模型文件：

- `scripts/dynamics_learning/models/grutcn.py`
- `scripts/dynamics_learning/models/tcnlstm.py`

优先只改这两个模型文件；只有为了让这两个模型正确运行、训练、测试或评估时，才允许改对应 train/test/eval wiring。

允许使用：

- `/Users/lixiang/Documents/Obsidian Vault/trick/trick.md`
- web research 的文献、模型结构、训练技巧、benchmark 思路或复杂模块
- 多 agent 分析

使用任何 trick 或外部研究导致模型、训练、测试或评估行为变化时，必须记录来源、理由、改动文件/协议和观察结果，并明确告诉用户。

2026-05-12 协议更新：允许根据 validation 结果、训练曲线、horizon/test 聚合指标和误差模式对 `grutcn` / `tcnlstm` 做较大结构改造，只要方向是更复杂、更有表达力、更有论文故事且可能提升指标；不要为了保守而局限在小 adapter。不得泄漏数据：不上传私有数据集、原始轨迹、标签、checkpoint、完整日志或可复现样本；不把 validation/test/horizon 的样本、标签或轨迹硬编码进模型、训练流程或提示词；只允许用聚合指标和误差趋势做结构决策。

## 模型故事约束

新模型叙事必须保留并强化：

- 从历史状态/控制 `[x,u]` 学习 latent context。
- 用 context 恢复当前状态 `x_t` 单独无法描述的未观测动态。
- 物理动机：actuator lag、aerodynamic state、hidden disturbances、filter state。
- 文献叙事：Mohajerin 2018 风格 RNN hidden-state initialization；Serrano 2024 风格 encoder context extraction。

不能把 `grutcn` / `tcnlstm` 简化成平凡 baseline。结构可以更复杂、更强表达力、更有论文故事，但不能削弱。

## 固定基线目标

来源：远程 `horizon_results.csv`，最后一次完整基线读取为 2026-05-07。

| config | best_valid_loss | h50_E_q | h50_E_v | h50_E_omega | mean_E_q | 结论 |
|---|---:|---:|---:|---:|---:|---|
| `gru_H10_F50_seed10` | `0.580389` | `0.0800042` | `0.356014` | `0.283807` | `0.0426877` | 当前最佳 h50 quaternion。 |
| `gru_H20_F50_seed10` | `0.573358` | `0.0802197` | `0.353015` | `0.260392` | `0.0420377` | 当前最佳 h50 velocity、omega、mean quaternion、sum quaternion。 |
| `tcn_H10_F50_seed10` | `0.533292` | `0.0891246` | `0.347036` | `0.327506` | `0.0452568` | validation 强，但 h50 quaternion 不如 GRU。 |
| `mlp_H1_F50_seed10` | `0.704774` | `0.174414` | `0.588885` | `0.579955` | `0.0847114` | h1 强，long horizon 弱。 |

成功标准：

- 允许使用 validation 与 horizon/test 指标共同挑选候选 checkpoint 和结构方向。
- 新模型的 `history_length` 不设硬上限，允许根据指标和结构需要扩大历史窗口；优先使用自适应历史选择或多尺度历史结构，而不是固定假设越长越好；最终按原始 baseline 最佳指标包络比较。
- 至少击败一个重要 long-horizon 目标，最好多个指标全面提升。
- 若需要声明“完全未见测试集”的最终结果，必须另设新的 holdout 或明确标注为开发期 horizon/test-guided 结果；当前 sprint 优先推进模型效果。

## 当前状态

- 2026-05-14 11:35 CST 当前无 active training/evaluation。`modeldev_20260514_grutcn_multistepdelta_H20_from_vomegaweight_e3_p1` e2 horizon/test MSE-schema eval p1 已完成：远程路径 `/home/ubuntu/Developer/long-horizon-dynamics/resources/experiments/modeldev_20260514_grutcn_multistepdelta_H20_from_vomegaweight_e3_p1`，checkpoint `checkpoints/model-epoch-02-best_valid_loss=0.17.pth`，eval log `logs/eval_horizontest_20260514_grutcn_multistepdelta_H20_e2_b32_mse_p1.log`，GPU watch log `logs/gpu_watch_eval.log`。训练已自然结束，`train_summary.json` 显示 `best_valid_loss=0.1660599709`、`valid_v_loss_epoch=0.0156964753`、`valid_omega_loss_epoch=0.0856593177`、`valid_q_loss_epoch=0.0021088682`、`valid_state_mse_epoch=0.0295935068`，trainable 参数仅 `model.multi_step_*`；smoke 通过 checkpoint-safe load 和 one-batch finite，缺失仅新增 `multi_step_*` key，无旧主路径 shape mismatch。horizon/test 只展开到 2 步：`average rollout loss=0.17696784436702728`，`h=1` 的 `E_p/E_v/E_q/E_omega/MSE_x` 已记录，`MSE_1_to_F=0.029530191901839857`，但 `h=10/25/50` 被脚本因 `unroll_length=2` 跳过，因此这轮不能作为 long-horizon locked audit 胜利或失败定论。下一步若要继续 multi-step 路线，先把 `unroll_length` 修正到能覆盖长 horizon，再做新的验证。
- 2026-05-14 已启动正式训练：`modeldev_20260514_grutcn_multistepdelta_H20_from_vomegaweight_e3_p1`，远程路径 `/home/ubuntu/Developer/long-horizon-dynamics/resources/experiments/modeldev_20260514_grutcn_multistepdelta_H20_from_vomegaweight_e3_p1`，训练 tmux `modeldev_grutcn_multistepdelta_H20_from_vomegaweight_e3_p1`，GPU watch `modeldev_gpu_watch_grutcn_multistepdelta_H20`，日志 `logs/train_phase1.log` / `logs/gpu_watch.log`。初始化 checkpoint 为 `modeldev_20260514_grutcn_vomegaweight_cont_H20_from_cont2_e3_p1/checkpoints/model-epoch=03-best_valid_loss=0.68.pth`；正式训练继续使用 `lambda_p=0.6`、`lambda_v=1.6`、`lambda_q=0.9`、`lambda_omega=1.6`，epochs `4`，batch `64`，accumulate `8`，`limit_train_batches=0.25`，`limit_val_batches=0.5`，`warmup_lr=8e-8`，`cosine_lr=3e-8`，`warmup_steps=50`，`cosine_steps=800`，early stopping patience `2`，min_delta `5e-6`，`--trainable_parameter_patterns multi_step`。smoke 已通过：checkpoint-safe load 仅 missing 新增 `multi_step_*` key，无旧主路径 shape mismatch；`train_loss_step=0.0523`、`valid_loss_epoch=0.0194`、`best_valid_loss=0.0194001738`；未读取 horizon/test，artifacts 保留。下一步观察 e0/e1 的 unweighted `valid_v/valid_omega/valid_q` 是否继续走强，再决定是否跑 horizon/test locked audit。
- 2026-05-14 已完成下一候选的代码准备与 smoke：`modeldev_20260514_grutcn_multistepdelta_H20_from_vomegaweight_e3_p1` 的 `smoke_20260514_grutcn_multistepdelta_H20_from_vomega_e3_p1` 从 `modeldev_20260514_grutcn_vomegaweight_cont_H20_from_cont2_e3_p1/checkpoints/model-epoch=03-best_valid_loss=0.68.pth` 成功加载，缺失项仅为新分支 `multi_step_*` key，无旧主路径 shape mismatch；trainable 参数仅 `model.multi_step_*`，一批 `train_loss_step=0.0523`、`valid_loss_epoch=0.0194`，`best_valid_loss=0.0194001738`。未读取 horizon/test metric，artifacts 保留。代码/协议改动集中在 `scripts/dynamics_learning/models/grutcn.py`、`scripts/config.py`、`scripts/dynamics_learning/registry.py`、`scripts/eval.py`、`scripts/train.py`；`scripts/dynamics_learning/lighting.py` 暂不需要改，因为新的分支仍保持 full_state rollout contract。下一步是启动正式训练，优先观察 unweighted `valid_v/valid_omega/valid_q` 是否继续走强，再决定是否做 horizon/test locked audit。
- 2026-05-14 09:18 CST `modeldev_20260514_grutcn_vomegaweight_cont_H20_from_vomega_e3_p1` e3 horizon/test MSE-schema eval p1 完成并读取 metric。checkpoint：`checkpoints/model-epoch=03-best_valid_loss=0.68.pth`，training 加权 `best_valid_loss=0.6827782393`，eval log `logs/eval_horizontest_20260514_grutcn_vomegaweight_cont_H20_e3_b32_mse_p1.log`，average rollout loss `0.8086838126`（加权 loss，不与未改权重实验直接比较）；h50 `E_q=0.0852212122`、`E_v=0.3800343019`、`E_omega=0.3008244657`、`MSE_x=0.7797312295`；mean `E_q=0.0436578958`、`E_v=0.2156254028`、`E_omega=0.2425080560`；`MSE_1_to_F=0.3993478401`。相对 vomegaweight e3，`MSE_1_to_F 0.3993592611 -> 0.3993478401`，h50 `E_v 0.3800689345 -> 0.3800343019`，h50 `E_omega 0.3008467932 -> 0.3008244657`，mean q/v/omega 与 h50 `MSE_x` 继续小幅改善；h50 `E_q` 第 8 位略回退，基本持平。结论：这是新的 GRUTCN 开发期 best，但提升已是 `1e-5` 量级且仍远落后 strongest GRU targets；该 horizon/test 读取影响后续结构决策：停止同类 reweight/latentse 微调，下一步转真正 `multi-step delta_v/delta_omega predictor`。eval/watch 已停止，artifacts 保留，无清理。
- 2026-05-14 08:58 CST `modeldev_20260514_grutcn_vomegaweight_cont_H20_from_vomega_e3_p1` 训练自然完成并启动 e3 horizon/test MSE-schema eval p1。训练 tmux/watch 已退出，GPU 回落到约 `506/8188 MiB`；无 OOM/NaN/Traceback。e0-e3 加权 validation 连续刷新：`0.6827921271 -> 0.6827870011 -> 0.6827825308 -> 0.6827781796`，`best_valid_loss=0.6827782393`，`early_stopped=false`，`max_epochs=4`；e3 unweighted `valid_p=0.0432733931`、`valid_v=0.1595940590`、`valid_q=0.0388018563`、`valid_omega=0.2290888280`、`valid_state_mse=0.2502373457`。相对 vomegaweight e3 参考 `valid_v=0.1596023738`、`valid_omega=0.2290926725`、`valid_q=0.0388069339`，v/omega/q 连续四轮小幅改善，q 未越线；训练阶段 horizon/test metric 未读取，artifacts 保留。eval tmux：`eval_grutcn_vomegaweight_cont_H20_e3_mse_p1`；eval GPU watch：`gpu_watch_eval_grutcn_vomegaweight_cont_H20`；eval log：`logs/eval_horizontest_20260514_grutcn_vomegaweight_cont_H20_e3_b32_mse_p1.log`；evaluated checkpoint：`checkpoints/model-epoch=03-best_valid_loss=0.68.pth`（来自 `train_summary.json`）。启动检查：eval/watch alive，GPU 约 `772/8188 MiB`、util `39%`，horizon/test metric 尚未读取。eval 完成后读取 average rollout loss、h50/mean `E_q/E_v/E_omega`、h50 `MSE_x`、`MSE_1_to_F`，比较 vomegaweight e3 `MSE_1_to_F=0.3993592611`、h50 `E_q=0.0852212047`、`E_v=0.3800689345`、`E_omega=0.3008467932`；若没有聚合收益，转 `multi-step delta_v/delta_omega predictor`。
- 2026-05-14 08:18 CST `modeldev_20260514_grutcn_vomegaweight_cont_H20_from_vomega_e3_p1` e2 validation 继续刷新并通过 gate。训练 tmux/watch alive，GPU 约 `6586/8188 MiB`、util `21%`，无 OOM/NaN/Traceback；加权 `valid_loss_epoch=0.6827921271 -> 0.6827870011 -> 0.6827825308`，`best_valid_loss=0.6827824116`；unweighted e2 `valid_p=0.0432737619`、`valid_v=0.1595954895`、`valid_q=0.0388026014`、`valid_omega=0.2290894836`、`valid_state_mse=0.2502363622`。相对 vomegaweight e3 参考 `valid_v=0.1596023738`、`valid_omega=0.2290926725`、`valid_q=0.0388069339`，v/omega/q 连续三轮小幅改善，q 未越线；state_mse 小幅上升但不是当前 gate 主指标。checkpoint `model-epoch=00/01/02-best_valid_loss=0.68.pth` 与 `last_model.pth` 已生成；horizon/test metric 未读取。决策：继续观察 natural finish；若后续 v/omega 信号消失或 `valid_q >0.03882`，停止不 eval 并转 `multi-step delta_v/delta_omega predictor`；若 natural finish 后仍保持正信号，跑/read MSE-schema horizon/test，并比较 vomegaweight e3 `MSE_1_to_F=0.3993592611`、h50 `E_q=0.0852212047`、`E_v=0.3800689345`、`E_omega=0.3008467932`。artifacts 保留，无清理。
- 2026-05-14 07:38 CST `modeldev_20260514_grutcn_vomegaweight_cont_H20_from_vomega_e3_p1` e1 validation 继续刷新并通过 gate。训练 tmux/watch alive，GPU 约 `6580/8188 MiB`、util `28%`，无 OOM/NaN/Traceback；加权 `valid_loss_epoch=0.6827921271 -> 0.6827870011`，`best_valid_loss=0.6827870011`；unweighted e1 `valid_p=0.0432742499`、`valid_v=0.1595965624`、`valid_q=0.0388036631`、`valid_omega=0.2290903032`、`valid_state_mse=0.2502349019`。相对 vomegaweight e3 参考 `valid_v=0.1596023738`、`valid_omega=0.2290926725`、`valid_q=0.0388069339`，v/omega/q 连续两轮小幅改善，q 未越线。checkpoint `model-epoch=00/01-best_valid_loss=0.68.pth` 与 `last_model.pth` 已生成；horizon/test metric 未读取。决策：继续观察 e2/natural finish；若后续 v/omega 信号消失或 `valid_q >0.03882`，停止不 eval 并转 `multi-step delta_v/delta_omega predictor`；若 natural finish 后仍保持正信号，跑/read MSE-schema horizon/test，并比较 vomegaweight e3 `MSE_1_to_F=0.3993592611`、h50 `E_q=0.0852212047`、`E_v=0.3800689345`、`E_omega=0.3008467932`。artifacts 保留，无清理。
- 2026-05-14 06:58 CST `modeldev_20260514_grutcn_vomegaweight_cont_H20_from_vomega_e3_p1` e0 validation 已出并通过 gate。训练 tmux/watch alive，GPU 约 `6580/8188 MiB`、util `39%`，无 OOM/NaN/Traceback；加权 `valid_loss_epoch=0.6827921271`、`best_valid_loss=0.6827921867`；unweighted e0 `valid_p=0.0432747900`、`valid_v=0.1595973969`、`valid_q=0.0388051271`、`valid_omega=0.2290917784`、`valid_state_mse=0.2502326965`。因为当前 loss 是加权 loss，不与未改权重 valid_loss 横向比较；相对 vomegaweight e3 参考 `valid_v=0.1596023738`、`valid_omega=0.2290926725`、`valid_q=0.0388069339`，v/omega/q 均继续小幅改善，q 未越线。checkpoint `model-epoch=00-best_valid_loss=0.68.pth` 与 `last_model.pth` 已生成；horizon/test metric 未读取。决策：继续观察 e1/e2/natural finish；若后续 v/omega 信号消失或 `valid_q >0.03882`，停止不 eval 并转 `multi-step delta_v/delta_omega predictor`；若 natural finish 后仍保持正信号，跑/read MSE-schema horizon/test，并比较 vomegaweight e3 `MSE_1_to_F=0.3993592611`、h50 `E_q=0.0852212047`、`E_v=0.3800689345`、`E_omega=0.3008467932`。artifacts 保留，无清理。
- 2026-05-14 05:58 CST `modeldev_20260514_grutcn_vomegaweight_H20_from_cont2_e3_p1` e3 horizon/test MSE-schema eval p1 完成并读取 metric，随后启动同权重低 LR continuation `modeldev_20260514_grutcn_vomegaweight_cont_H20_from_vomega_e3_p1`。eval checkpoint：`checkpoints/model-epoch=03-best_valid_loss=0.68.pth`，training 加权 `best_valid_loss=0.6828041077`，eval log `logs/eval_horizontest_20260514_grutcn_vomegaweight_H20_e3_b32_mse_p1.log`，average rollout loss `0.8087291121`（加权 loss，不与未改权重实验直接比较）；h50 `E_q=0.0852212047`、`E_v=0.3800689345`、`E_omega=0.3008467932`、`MSE_x=0.7797522271`；mean `E_q=0.0436584481`、`E_v=0.2156444532`、`E_omega=0.2425160258`；`MSE_1_to_F=0.3993592611`。相对 cont2 e3，`MSE_1_to_F 0.3993805727 -> 0.3993592611`，h50 q/v/omega 和 mean q/v/omega 均小幅改善，成为新的 GRUTCN 开发期 best；该 horizon/test 读取影响后续决策：允许一次更低 LR 同权重 continuation 验证收益是否延续，若不再改善聚合指标则转 `multi-step delta_v/delta_omega predictor`。新 active training 远程路径：`/home/ubuntu/Developer/long-horizon-dynamics/resources/experiments/modeldev_20260514_grutcn_vomegaweight_cont_H20_from_vomega_e3_p1`；训练 tmux `modeldev_grutcn_vomegaweight_cont_H20_from_vomega_e3_p1`；GPU watch `modeldev_gpu_watch_grutcn_vomegaweight_cont_H20`；init checkpoint：`resources/experiments/modeldev_20260514_grutcn_vomegaweight_H20_from_cont2_e3_p1/checkpoints/model-epoch=03-best_valid_loss=0.68.pth`；配置：epochs `4`，batch `64`，accumulate `8`，effective batch `512`，`limit_train_batches=0.25`，`limit_val_batches=0.5`，`warmup_lr=8e-8`，`cosine_lr=3e-8`，early stopping patience `2`，min_delta `5e-6`，同 loss weights，WANDB disabled。启动检查：train/watch alive，日志显示 checkpoint 加载与 trainable patterns 正确，GPU 约 `6576/8188 MiB`，无 OOM/NaN/Traceback；本 continuation 尚无 validation row，horizon/test metric 未读取。Gate：因当前 valid_loss 是加权 loss，不与未改权重 valid_loss 直接比较；参考 vomegaweight e3 unweighted `valid_v=0.1596023738`、`valid_omega=0.2290926725`、`valid_q=0.0388069339`，若 continuation e0 `valid_v >=0.1596024` 且 `valid_omega >=0.2290927`，或 `valid_q >0.03882`，停止不 eval 并转结构；若 v/omega 继续下降且 q 可控，natural finish 后跑/read MSE-schema horizon/test，并和 vomegaweight e3 聚合指标比较。
- 2026-05-14 05:18 CST `modeldev_20260514_grutcn_vomegaweight_H20_from_cont2_e3_p1` 训练自然完成并启动 e3 horizon/test MSE-schema eval p1。训练 e0-e3 加权 validation 连续刷新：`0.6828369498 -> 0.6828231215 -> 0.6828121543 -> 0.6828041673`，`train_summary.json` 指向 e3 checkpoint `checkpoints/model-epoch=03-best_valid_loss=0.68.pth`，`best_valid_loss=0.6828041077`，`early_stopped=false`，`max_epochs=4`；e3 unweighted 分项 `valid_p=0.0432761610`、`valid_v=0.1596023738`、`valid_q=0.0388069339`、`valid_omega=0.2290926725`、`valid_state_mse=0.2502301335`。因为本实验 valid_loss 是加权 loss，不与 cont2 `0.470826` 直接比较；e3 的 v/omega/q 继续优于 cont2 e3 参考线，满足预声明 eval 条件。训练 tmux/watch 已自然退出，artifacts 保留，训练阶段 horizon/test metric 未读取。已启动 eval tmux `eval_grutcn_vomegaweight_H20_e3_mse_p1`，eval GPU watch `gpu_watch_eval_grutcn_vomegaweight_H20`，eval log `logs/eval_horizontest_20260514_grutcn_vomegaweight_H20_e3_b32_mse_p1.log`，batch `32`；启动检查：eval/watch alive，eval log 到 `Seed set to 10`，GPU 约 `771/8188 MiB`、util `38%`，horizon/test metric 尚未读取。eval 完成后必须记录 average rollout loss、h50/mean `E_q/E_v/E_omega`、h50 `MSE_x`、`MSE_1_to_F`，并比较 cont2 e3 `MSE_1_to_F=0.3993805727`、h50 `E_q=0.0852223037`、`E_v=0.3801424071`、`E_omega=0.3008873714`；若无聚合收益，转 `multi-step delta_v/delta_omega predictor`。
- 2026-05-14 04:58 CST `modeldev_20260514_grutcn_vomegaweight_H20_from_cont2_e3_p1` e2 validation 继续刷新，实验保持健康。训练 tmux/watch alive，GPU 约 `6580/8188 MiB`、util `40%`，无 OOM/NaN/Traceback；checkpoint `model-epoch=00/01/02-best_valid_loss=0.68.pth` 和 `last_model.pth` 已生成。当前加权 `valid_loss_epoch=0.6828369498 -> 0.6828231215 -> 0.6828121543`，`best_valid_loss=0.6828124523`；unweighted e2 分项 `valid_p=0.0432770699`、`valid_v=0.1596052945`、`valid_q=0.0388082638`、`valid_omega=0.2290939987`、`valid_state_mse=0.2502283454`。相对 e1 与 cont2 e3 参考线，v/omega/q 继续小幅改善，state_mse 小幅上浮但不触发 gate。判定：v/omega 提权连续三轮有 validation 正信号，继续观察 natural finish；horizon/test metric 尚未读取。若 natural finish 后仍保持 v/omega 正信号，跑/read MSE-schema horizon/test；若后续正信号消失或 q 明显回退，停止不 eval 并转 `multi-step delta_v/delta_omega predictor`。
- 2026-05-14 04:18 CST `modeldev_20260514_grutcn_vomegaweight_H20_from_cont2_e3_p1` e1 validation 继续刷新，实验保持健康。训练 tmux/watch alive，GPU 约 `6580/8188 MiB`、util `30%`，无 OOM/NaN/Traceback；checkpoint `model-epoch=00/01-best_valid_loss=0.68.pth` 和 `last_model.pth` 已生成。当前加权 `valid_loss_epoch=0.6828369498 -> 0.6828231215`，`best_valid_loss=0.6828230023`；unweighted e1 分项 `valid_p=0.0432783440`、`valid_v=0.1596084684`、`valid_q=0.0388101935`、`valid_omega=0.2290958017`、`valid_state_mse=0.2502252459`。相对 e0 与 cont2 e3 参考线，v/omega/q 继续小幅改善，state_mse 微升但不触发 gate。判定：v/omega 提权连续两轮有 validation 正信号，继续观察 e2/natural finish；horizon/test metric 尚未读取。若 natural finish 后仍保持 v/omega 正信号，跑/read MSE-schema horizon/test；若后续正信号消失或 q 明显回退，停止不 eval 并转 `multi-step delta_v/delta_omega predictor`。
- 2026-05-14 03:38 CST `modeldev_20260514_grutcn_vomegaweight_H20_from_cont2_e3_p1` e0 validation 已出，实验继续健康。远程路径：`/home/ubuntu/Developer/long-horizon-dynamics/resources/experiments/modeldev_20260514_grutcn_vomegaweight_H20_from_cont2_e3_p1`；训练 tmux/watch 均 alive，GPU 约 `6580/8188 MiB`、util `30%`，无 OOM/NaN/Traceback；checkpoint `model-epoch=00-best_valid_loss=0.68.pth` 和 `last_model.pth` 已生成。当前 `valid_loss_epoch=0.6828369498` 是加权 loss，不能与 cont2 `0.470826` 直接比较；unweighted 分项为 `valid_p=0.0432799421`、`valid_v=0.1596120149`、`valid_q=0.0388131440`、`valid_omega=0.2290987074`、`valid_state_mse=0.2502215207`。相对 cont2 e3 参考 `valid_v=0.1596247554`、`valid_omega=0.2291012257`、`valid_q=0.0388166271`，v/omega/q 均小幅改善并通过预声明 gate；判定：v/omega 提权有 validation 正信号，继续观察 e1/e2/natural finish，horizon/test metric 尚未读取。若 natural finish 后仍保持正信号，跑/read MSE-schema horizon/test；若后续正信号消失或 q 明显回退，停止不 eval 并转 `multi-step delta_v/delta_omega predictor`。
- 2026-05-14 02:45 CST 启动新的前置验证 active training：`modeldev_20260514_grutcn_vomegaweight_H20_from_cont2_e3_p1`。远程路径：`/home/ubuntu/Developer/long-horizon-dynamics/resources/experiments/modeldev_20260514_grutcn_vomegaweight_H20_from_cont2_e3_p1`；训练 tmux：`modeldev_grutcn_vomegaweight_H20_from_cont2_e3_p1`；GPU watch：`modeldev_gpu_watch_grutcn_vomegaweight_H20`；log：`logs/train_phase1.log`；init checkpoint：`resources/experiments/modeldev_20260514_grutcn_rawtoktf_latentsefix_cont2_H20_from_cont_e5_p1/checkpoints/model-epoch=03-best_valid_loss=0.47.pth`。结构仍为 GRUTCN H20/F50 raw-token main + `latent_se`，不改源码，physics OFF；loss 权重改为 `lambda_p=0.6`、`lambda_v=1.6`、`lambda_q=0.9`、`lambda_omega=1.6`，用于验证 v/omega 提权是否能带来比低 LR 微调更大的 h50 `E_v/E_omega` 与 `MSE_1_to_F` 改善。配置：epochs `4`，batch `64`，accumulate `8`，effective batch `512`，`limit_train_batches=0.25`，`limit_val_batches=0.5`，`warmup_lr=1.5e-7`，`cosine_lr=5e-8`，early stopping patience `2`，min_delta `5e-6`，WANDB disabled。启动检查：tmux/watch alive，GPU 约 `6576/8188 MiB`，日志显示成功加载 cont2 e3 checkpoint、trainable patterns 正确且不含 `raw_token_adaptive_*`；无 OOM/NaN/Traceback，尚无 validation row，horizon/test metric 未读取。Gate：当前 valid_loss 为加权 loss，不可直接与 cont2 `0.470826` 比较；优先看 unweighted `valid_v_loss_epoch` / `valid_omega_loss_epoch` 是否低于 cont2 e3 的 `0.1596247554` / `0.2291012257`，且 `valid_q_loss_epoch` 不超过约 `0.03884`。若 e0 无 v/omega 正信号或 q 明显回退，停止不 eval 并转真正 `multi-step delta_v/delta_omega predictor`。
- 2026-05-14 02:38 CST `modeldev_20260514_grutcn_rawtoktf_latentsefix_cont2_H20_from_cont_e5_p1` e3 horizon/test MSE-schema eval p1 完成并读取 metric。checkpoint：`checkpoints/model-epoch=03-best_valid_loss=0.47.pth`，training `best_valid_loss=0.4708260596`，eval log `logs/eval_horizontest_20260514_grutcn_rawtoktf_latentsefix_cont2_H20_e3_b32_mse_p1.log`，average rollout loss `0.5625137091`；h50 `E_q=0.0852223037`、`E_v=0.3801424071`、`E_omega=0.3008873714`、`MSE_x=0.7797898716`；mean `E_q=0.0436600007`、`E_v=0.2156833140`、`E_omega=0.2425306675`；`MSE_1_to_F=0.3993805727`。相对 cont e5，`MSE_1_to_F 0.3993963243 -> 0.3993805727`，h50 q/v/omega 和 average rollout loss 小幅全面改善，是新的 GRUTCN 开发期 best；但改善仅 `1e-5` 到 `5e-5` 量级，仍明显落后 strongest GRU targets（h50_q `0.0800042`、h50_v `0.353015`、h50_omega `0.260392`、mean_q `0.0420377`）。该 horizon/test 读取影响后续结构决策：停止继续同类 latentse/raw-token 低 LR 微调，转 `multi-step delta_v/delta_omega predictor` 或其低风险前置验证。eval/watch 已自然退出，artifacts 保留，无清理。
- 2026-05-14 02:18 CST `modeldev_20260514_grutcn_rawtoktf_latentsefix_cont2_H20_from_cont_e5_p1` 训练自然完成并启动 e3 horizon/test MSE-schema eval p1。训练 e0-e3 validation 连续刷新：`0.4708608985 -> 0.4708451629 -> 0.4708339274 -> 0.4708261788`，`train_summary.json` 指向 e3 checkpoint `checkpoints/model-epoch=03-best_valid_loss=0.47.pth`，`best_valid_loss=0.4708260596`；e3 分项 `valid_p_loss_epoch=0.0432835445`、`valid_v_loss_epoch=0.1596247554`、`valid_q_loss_epoch=0.0388166271`、`valid_omega_loss_epoch=0.2291012257`、`valid_state_mse_epoch=0.2502173185`。训练 tmux 和训练 GPU watch 已自然退出，artifacts 保留；训练阶段本实验 horizon/test metric 未读取。已启动 eval tmux `eval_grutcn_rawtoktf_latentsefix_cont2_H20_e3_mse_p1`，eval GPU watch `gpu_watch_eval_grutcn_rawtoktf_latentsefix_cont2_H20`，eval log `logs/eval_horizontest_20260514_grutcn_rawtoktf_latentsefix_cont2_H20_e3_b32_mse_p1.log`，eval batch `32`；启动检查：tmux/watch alive，log 到 `Seed set to 10`，GPU 约 `766/8188 MiB`、util `40%`。下一步等 eval 完成后读取 average rollout loss、h50/mean `E_q/E_v/E_omega`、h50 `MSE_x`、`MSE_1_to_F`，并和 cont e5 比较；若无聚合改善，停止同类 latentse/raw-token 微调并转 `multi-step delta_v/delta_omega predictor`。
- 2026-05-14 01:38 CST `modeldev_20260514_grutcn_rawtoktf_latentsefix_cont2_H20_from_cont_e5_p1` e2 继续刷新 validation：e0 `valid_loss_epoch=0.4708608985` -> e1 `0.4708451629` -> e2 `0.4708339274`，当前 `best_valid_loss=0.4708339274`；e2 分项 `valid_p_loss_epoch=0.0432847776`、`valid_v_loss_epoch=0.1596295685`、`valid_q_loss_epoch=0.0388175435`、`valid_omega_loss_epoch=0.2291020453`、`valid_state_mse_epoch=0.2502169013`。训练 tmux/GPU watch alive，GPU 约 `6574/8188 MiB`，无 OOM/NaN/Traceback；checkpoint `model-epoch=00/01/02-best_valid_loss=0.47.pth` 与 `last_model.pth` 已生成，`train_summary.json` 尚未生成，本实验 horizon/test metric 未读取。判定：e0-e2 连续健康刷新，继续观察 natural finish；最终仍以 horizon/test `MSE_1_to_F` 和 h50 `E_q/E_v/E_omega` 是否超过 cont e5 为准。
- 2026-05-14 00:58 CST `modeldev_20260514_grutcn_rawtoktf_latentsefix_cont2_H20_from_cont_e5_p1` e1 继续刷新 validation：e0 `valid_loss_epoch=0.4708608985` -> e1 `0.4708451629`，当前 `best_valid_loss=0.4708452523`；e1 分项 `valid_p_loss_epoch=0.0432867706`、`valid_v_loss_epoch=0.1596363783`、`valid_q_loss_epoch=0.0388187915`、`valid_omega_loss_epoch=0.2291033268`、`valid_state_mse_epoch=0.2502155006`。训练 tmux/GPU watch alive，GPU 约 `6570/8188 MiB`，无 OOM/NaN/Traceback；checkpoint `model-epoch=00/01-best_valid_loss=0.47.pth` 与 `last_model.pth` 已生成，`train_summary.json` 尚未生成，本实验 horizon/test metric 未读取。判定：e0/e1 连续健康刷新，继续观察 e2/natural finish；最终仍以 horizon/test `MSE_1_to_F` 和 h50 `E_q/E_v/E_omega` 是否超过 cont e5 为准。
- 2026-05-14 00:18 CST `modeldev_20260514_grutcn_rawtoktf_latentsefix_cont2_H20_from_cont_e5_p1` e0 validation 已出并刷新：`valid_loss_epoch=0.4708608985`、`best_valid_loss=0.4708608091`，分项 `valid_p_loss_epoch=0.0432894938`、`valid_v_loss_epoch=0.1596454829`、`valid_q_loss_epoch=0.0388207026`、`valid_omega_loss_epoch=0.2291049510`、`valid_state_mse_epoch=0.2502139807`。训练 tmux/GPU watch alive，GPU 约 `6570/8188 MiB`，无 OOM/NaN/Traceback；checkpoint `model-epoch=00-best_valid_loss=0.47.pth` 与 `last_model.pth` 已生成，`train_summary.json` 尚未生成，本实验 horizon/test metric 未读取。判定：e0 低于健康线 `0.47088`，继续观察 e1/e2/natural finish；最终仍以 horizon/test `MSE_1_to_F` 和 h50 `E_q/E_v/E_omega` 是否超过 cont e5 为准。
- 2026-05-13 23:26 CST `modeldev_20260513_grutcn_rawtoktf_latentsefix_cont_H20_from_latentse_e4_p1` e5 horizon/test MSE-schema eval p1 完成并读取 metric。checkpoint：`checkpoints/model-epoch=05-best_valid_loss=0.47.pth`，training `best_valid_loss=0.4708843529`，eval log `logs/eval_horizontest_20260513_grutcn_rawtoktf_latentsefix_cont_H20_e5_b32_mse_p1.log`，average rollout loss `0.5625592470`；h50 `E_q=0.0852242063`、`E_v=0.3801910379`、`E_omega=0.3009120027`、`MSE_x=0.7798174486`；mean `E_q=0.0436615263`、`E_v=0.2157105523`、`E_omega=0.2425391548`；`MSE_1_to_F=0.3993963243`。结论：相对上一轮 latentsefix e4，`MSE_1_to_F`、h50 q/v/omega、mean q 和 average rollout loss 小幅全面改善，是新的 GRUTCN 开发期 best；仍落后 strongest GRU targets（h50_q `0.0800042`、h50_v `0.353015`、h50_omega `0.260392`、mean_q `0.0420377`）。该 horizon/test 读取影响后续决策：允许最后一次严格低 LR continuation；若下一轮不再改善 `MSE_1_to_F` 或 h50 `E_q/E_v/E_omega`，停止同类 latentse/raw-token 微调，转 `multi-step delta_v/delta_omega predictor`。
- 2026-05-13 23:26 CST 启动最后一次同类 continuation `modeldev_20260514_grutcn_rawtoktf_latentsefix_cont2_H20_from_cont_e5_p1`。远程路径：`/home/ubuntu/Developer/long-horizon-dynamics/resources/experiments/modeldev_20260514_grutcn_rawtoktf_latentsefix_cont2_H20_from_cont_e5_p1`；训练 tmux：`modeldev_grutcn_rawtoktf_latentsefix_cont2_H20_from_cont_e5_p1`；GPU watch：`modeldev_gpu_watch_grutcn_rawtoktf_latentsefix_cont2_H20`；log：`logs/train_phase1.log`；init checkpoint：`resources/experiments/modeldev_20260513_grutcn_rawtoktf_latentsefix_cont_H20_from_latentse_e4_p1/checkpoints/model-epoch=05-best_valid_loss=0.47.pth`。结构仍为 GRUTCN H20/F50 raw-token main + `latent_se`，physics OFF，trainable patterns 同前且不含 `raw_token_adaptive_*`。配置：epochs `4`，batch `64`，accumulate `8`，effective batch `512`，`limit_train_batches=0.25`，`limit_val_batches=0.5`，`warmup_lr=1e-7`，`cosine_lr=3e-8`，`warmup_steps=50`，`cosine_steps=800`，early stopping patience `2`，min_delta `5e-6`，WANDB disabled。第一次启动因非交互 shell `conda` 不在 PATH 立即退出，已只清理该 cont2 空运行目录/对应 tmux，并用显式 `/home/ubuntu/miniconda3/bin/conda` 重启成功。启动检查：tmux/watch alive，日志已加载 e5 checkpoint，trainable patterns 正确，GPU 约 `6568/8188 MiB`，无 OOM/NaN/Traceback；horizon/test 未读取。Gate：e0 `>0.47091` 或 p/v/q/omega/state_mse 同步回退则停止不 eval 并转结构；e0 `<=0.47088` 或后续刷新则健康。
- 2026-05-13 23:02 CST `modeldev_20260513_grutcn_rawtoktf_latentsefix_cont_H20_from_latentse_e4_p1` 训练自然跑满 `max_epochs=6`，`early_stopped=false`，`train_summary.json` 指向 e5 best checkpoint `checkpoints/model-epoch=05-best_valid_loss=0.47.pth`，`best_valid_loss=0.4708843529`。e0-e5 validation 连续刷新：`0.4710687995 -> 0.4710116088 -> 0.4709645212 -> 0.4709274471 -> 0.4709027410 -> 0.4708842933`；e5 分项为 `valid_p_loss_epoch=0.0432935506`、`valid_v_loss_epoch=0.1596616060`、`valid_q_loss_epoch=0.0388227813`、`valid_omega_loss_epoch=0.2291064113`、`valid_state_mse_epoch=0.2502119839`。训练 tmux 已退出，训练 GPU watch 已停止，artifacts 保留；训练阶段未读取本轮 horizon/test metric。已启动 e5 horizon/test MSE-schema eval：tmux `eval_grutcn_rawtoktf_latentsefix_cont_H20_e5_mse_p1`，GPU watch `gpu_watch_eval_grutcn_rawtoktf_latentsefix_cont_H20`，eval log `logs/eval_horizontest_20260513_grutcn_rawtoktf_latentsefix_cont_H20_e5_b32_mse_p1.log`，eval batch `32`，log 到 `Seed set to 10`，GPU watch 约 `756/8188 MiB`、util `36%`；horizon/test metric 尚未读取。完成后必须记录 average rollout loss、h50/mean `E_q/E_v/E_omega`、h50 `MSE_x`、`MSE_1_to_F`，并判断是否继续 latentse/raw-token 或 pivot 到 `multi-step delta_v/delta_omega predictor`。
- 2026-05-13 22:20 CST `modeldev_20260513_grutcn_rawtoktf_latentsefix_cont_H20_from_latentse_e4_p1` e4 继续刷新 validation best：e0 `valid_loss_epoch=0.4710687995` -> e1 `0.4710116088` -> e2 `0.4709645212` -> e3 `0.4709274471` -> e4 `0.4709027410`，`best_valid_loss=0.4709026217`；e4 分项为 `valid_p_loss_epoch=0.0432964787`、`valid_v_loss_epoch=0.1596727371`、`valid_q_loss_epoch=0.0388250090`、`valid_omega_loss_epoch=0.2291083932`、`valid_state_mse_epoch=0.2502122521`。训练 tmux 与 GPU watch 均 alive，GPU 约 `6569/8188 MiB`，无 OOM/NaN/Traceback；checkpoint `model-epoch=02/03/04-best_valid_loss=0.47.pth` 和 `last_model.pth` 已生成，`train_summary` 尚未生成，horizon/test metric 尚未读取。判定：validation 仍在缓慢下降，继续等待 natural finish；最终以 horizon/test MSE-schema 决策是否保留 latentse continuation 方向。
- 2026-05-13 21:40 CST `modeldev_20260513_grutcn_rawtoktf_latentsefix_cont_H20_from_latentse_e4_p1` e3 继续刷新 validation best：e0 `valid_loss_epoch=0.4710687995` -> e1 `0.4710116088` -> e2 `0.4709645212` -> e3 `0.4709274471`，`best_valid_loss=0.4709275663`；e3 分项为 `valid_p_loss_epoch=0.0433006957`、`valid_v_loss_epoch=0.1596887559`、`valid_q_loss_epoch=0.0388276987`、`valid_omega_loss_epoch=0.2291103750`、`valid_state_mse_epoch=0.2502110302`。训练 tmux 与 GPU watch 均 alive，GPU 约 `6569/8188 MiB`，无 OOM/NaN/Traceback；checkpoint `model-epoch=01/02/03-best_valid_loss=0.47.pth` 和 `last_model.pth` 已生成，`train_summary` 尚未生成，horizon/test metric 尚未读取。判定：e0-e3 validation 连续健康刷新，继续等待 natural finish；后续必须用 horizon/test MSE-schema 判断是否转化为长时域收益。
- 2026-05-13 21:00 CST `modeldev_20260513_grutcn_rawtoktf_latentsefix_cont_H20_from_latentse_e4_p1` e2 继续刷新 validation best 并首次低于 `0.471`：e0 `valid_loss_epoch=0.4710687995` -> e1 `0.4710116088` -> e2 `0.4709645212`，`best_valid_loss=0.4709644318`；e2 分项为 `valid_p_loss_epoch=0.0433063544`、`valid_v_loss_epoch=0.1597131640`、`valid_q_loss_epoch=0.0388310663`、`valid_omega_loss_epoch=0.2291137427`、`valid_state_mse_epoch=0.2502114773`。训练 tmux 与 GPU watch 均 alive，GPU 约 `6570/8188 MiB`，无 OOM/NaN/Traceback；checkpoint `model-epoch=00/01/02-best_valid_loss=0.47.pth` 和 `last_model.pth` 已生成，`train_summary` 尚未生成，horizon/test metric 尚未读取。判定：validation 正信号明确，但最终仍以 horizon/test MSE-schema 是否改善 `MSE_1_to_F` 与 h50 `E_q/E_v/E_omega` 为准；继续等待 natural finish 后评估。
- 2026-05-13 20:20 CST `modeldev_20260513_grutcn_rawtoktf_latentsefix_cont_H20_from_latentse_e4_p1` e1 继续刷新 validation best：e0 `valid_loss_epoch=0.4710687995` -> e1 `0.4710116088`，`best_valid_loss=0.4710114896`；e1 分项为 `valid_p_loss_epoch=0.0433137342`、`valid_v_loss_epoch=0.1597451419`、`valid_q_loss_epoch=0.0388350338`、`valid_omega_loss_epoch=0.2291177213`、`valid_state_mse_epoch=0.2502109706`。训练 tmux 与 GPU watch 均 alive，GPU 约 `6576/8188 MiB`，无 OOM/NaN/Traceback；checkpoint `model-epoch=00/01-best_valid_loss=0.47.pth` 和 `last_model.pth` 已生成，`train_summary` 尚未生成，horizon/test metric 尚未读取。判定：e0/e1 连续健康刷新，继续等待 e2/natural finish；训练结束后必须读取 horizon/test MSE-schema 验证是否转化为 `MSE_1_to_F` 或 h50 `E_q/E_v/E_omega` 改善。
- 2026-05-13 19:40 CST `modeldev_20260513_grutcn_rawtoktf_latentsefix_cont_H20_from_latentse_e4_p1` e0 validation 已出并刷新当前 GRUTCN validation best：`valid_loss_epoch=0.4710687995`、`best_valid_loss=0.4710687697`、`valid_p_loss_epoch=0.0433222577`、`valid_v_loss_epoch=0.1597835869`、`valid_q_loss_epoch=0.0388404727`、`valid_omega_loss_epoch=0.2291224897`、`valid_state_mse_epoch=0.2502124310`。训练 tmux 与 GPU watch 均 alive，GPU 约 `6568/8188 MiB`，无 OOM/NaN/Traceback；checkpoint `model-epoch=00-best_valid_loss=0.47.pth` 与 `last_model.pth` 已生成，`train_summary` 尚未生成，horizon/test metric 尚未读取。判定：e0 低于健康线 `0.47112`，分项较上一轮 latentsefix e4 同步小幅改善；继续观察 e1/e2/natural finish，训练结束后按协议读取 horizon/test MSE-schema，再决定是否继续同类 latentse/raw-token 或转 `multi-step delta_v/delta_omega predictor`。
- 2026-05-13 18:42 CST `modeldev_20260513_grutcn_rawtoktf_latentsefix_H20_from_physkin_e5_shufflefix_p1` e4 horizon/test MSE-schema eval p1 完成并读取 metric。checkpoint：`checkpoints/model-epoch=04-best_valid_loss=0.47.pth`，training `best_valid_loss=0.4711425900`，eval log `logs/eval_horizontest_20260513_grutcn_rawtoktf_latentsefix_H20_e4_b32_mse_p1.log`，average rollout loss `0.5627378821`；h50 `E_q=0.0852424028`、`E_v=0.3803976436`、`E_omega=0.3009922140`、`MSE_x=0.7799340487`；mean `E_q=0.0436740862`、`E_v=0.2158192034`、`E_omega=0.2425650592`；`MSE_1_to_F=0.3994537865`。结论：相对 physkin e5 与 rawtoktf cont e7，小幅但全面改善 h50 q/v/omega、mean q 和 `MSE_1_to_F`，是当前 GRUTCN 开发期 best；仍明显落后 strongest GRU targets（h50_q `0.0800042`、h50_v `0.353015`、h50_omega `0.260392`、mean_q `0.0420377`）。该 horizon/test 读取影响后续决策：允许只做一次低 LR continuation；若 continuation 不继续改善聚合指标，则停止同类 latentse/raw-token 微调，转 `v/omega` 主导路线：先做 loss reweight 低风险验证，再做 `multi-step delta_v/delta_omega predictor` + kinematic `p/q` recovery ablation。
- 2026-05-13 下一结构执行顺序已定：若当前 latentse continuation 不继续改善 horizon/test 聚合指标，优先和主控/分支 agents 讨论并推进两步方案。第一步 `v/omega` 优先训练协议：不改当前输出语义，先提高 `lambda_v/lambda_omega`、降低但不归零 `lambda_p/lambda_q`，验证只重点汇报 `E_v` / `E_omega` 是否能赢。第二步 kinematic update ablation：主模型输入历史 `[p,v,q,omega]`、历史控制和 future controls，核心输出整段 `delta_v_{t:t+F-1}` 与 `delta_omega_{t:t+F-1}`，`p` 用 `v` 梯形积分恢复、`q` 用 `omega` 梯形积分恢复；小 `delta_p_res` / `delta_theta_res` 只作为 ablation，若加入后 `E_v`、`E_omega`、`E_q` 或 `MSE_1_to_F` 更好则保留，否则删除，避免退回自由预测完整 state。全程仍做 full-state open-loop rollout，并监控 `p/q` 漂移是否污染后续状态。
- 2026-05-13 18:40 CST 启动 continuation active training `modeldev_20260513_grutcn_rawtoktf_latentsefix_cont_H20_from_latentse_e4_p1`。远程路径：`/home/ubuntu/Developer/long-horizon-dynamics/resources/experiments/modeldev_20260513_grutcn_rawtoktf_latentsefix_cont_H20_from_latentse_e4_p1`；训练 tmux：`modeldev_grutcn_rawtoktf_latentsefix_cont_H20_from_latentse_e4_p1`；GPU watch：`modeldev_gpu_watch_grutcn_rawtoktf_latentsefix_cont_H20`；log：`logs/train_phase1.log`；init checkpoint：`resources/experiments/modeldev_20260513_grutcn_rawtoktf_latentsefix_H20_from_physkin_e5_shufflefix_p1/checkpoints/model-epoch=04-best_valid_loss=0.47.pth`。结构仍为 GRUTCN H20/F50 raw-token main + `latent_se`，physics OFF，trainable patterns 同上一轮且不含 `raw_token_adaptive_*`。配置：epochs `6`，batch `64`，accumulate `8`，effective batch `512`，`limit_train_batches=0.25`，`limit_val_batches=0.5`，`warmup_lr=2e-7`，`cosine_lr=7e-8`，`warmup_steps=50`，`cosine_steps=1200`，early stopping patience `2`，min_delta `1e-5`，WANDB disabled。启动检查：tmux/watch alive，日志已加载 e4 checkpoint，trainable params 约 `3.4M`，GPU 约 `6543/8188 MiB`，无 OOM/NaN/Traceback；horizon/test 未读取。Gate：e0 `>0.47118` 或 q/v/omega/state_mse 同步回退则停止不 eval；e0 `0.47112-0.47118` 观察 e1/e2；e0 `<=0.47112` 健康。artifacts 全部保留，旧 eval GPU watch 已停止。
- 2026-05-13 18:15 CST `modeldev_20260513_grutcn_rawtoktf_latentsefix_H20_from_physkin_e5_shufflefix_p1` 训练自然完成并启动 horizon/test eval。训练最终 e4：`valid_loss_epoch=0.4711423814`、`best_valid_loss=0.4711425900`、`valid_p_loss_epoch=0.0433329195`、`valid_v_loss_epoch=0.1598363668`、`valid_q_loss_epoch=0.0388464183`、`valid_omega_loss_epoch=0.2291269004`、`valid_state_mse_epoch=0.2502166033`；`train_summary.json` 显示 `best_model_path=checkpoints/model-epoch=04-best_valid_loss=0.47.pth`、`stopped_epoch=5`、`early_stopped=False`。启动 eval：tmux `eval_grutcn_rawtoktf_latentsefix_H20_e4_mse_p1`，GPU watch `gpu_watch_eval_grutcn_rawtoktf_latentsefix_H20`，log `logs/eval_horizontest_20260513_grutcn_rawtoktf_latentsefix_H20_e4_b32_mse_p1.log`，batch `32`，horizons `1,10,25,50`。首次启动因非交互 tmux 中 `conda` 不在 PATH 立即退出，未读取 horizon/test metric；已用 `source /home/ubuntu/miniconda3/etc/profile.d/conda.sh && conda activate dynamics_learning` 重启，log 到 `Seed set to 10`，GPU 约 `766/8188 MiB`、util `37%`。当前等待 eval 完成后读取并记录 `average rollout loss`、h50/mean 指标和 `MSE_1_to_F`。
- 2026-05-13 17:42 CST `modeldev_20260513_grutcn_rawtoktf_latentsefix_H20_from_physkin_e5_shufflefix_p1` 巡检：训练 tmux 与 GPU watch 均 alive，无 Traceback/OOM/NaN；`/usr/lib/wsl/lib/nvidia-smi` 显存约 `6581/8188 MiB`、util `25%`。CSVLogger e3 继续刷新：`valid_loss_epoch=0.4711873233`、`best_valid_loss=0.4711873233`、`valid_p_loss_epoch=0.0433392152`、`valid_v_loss_epoch=0.1598675549`、`valid_q_loss_epoch=0.0388507284`、`valid_omega_loss_epoch=0.2291298509`、`valid_state_mse_epoch=0.2502192855`。checkpoint：`last_model.pth`、`model-epoch=01/02/03-best_valid_loss=0.47.pth`；`train_summary.json` 尚未生成，horizon/test metric 未读取。判定：validation 仍健康且分项同步小幅改善，继续等待 natural finish / early stop，再按协议跑/read horizon/test MSE-schema。
- 2026-05-13 用户新增下一结构讨论方向：若当前 `modeldev_20260513_grutcn_rawtoktf_latentsefix_H20_from_physkin_e5_shufflefix_p1` natural finish 后 horizon/test 的 `E_v` / `E_omega` 仍没有明显赢，下一轮优先和主控/分支 agents 讨论 `multi-step delta_v/delta_omega predictor`，不要继续开同类小 adapter。核心假设：`v` 与 `omega` 是更直接的动力学量，更好预测；主模型输入历史 `[p,v,q,omega]`、历史控制和 future controls，核心输出整段 `delta_v_{t:t+F-1}` 与 `delta_omega_{t:t+F-1}`，再通过运动学关系恢复完整 rollout。默认候选不自由预测完整 `p/q`：`v_{k+1}=v_k+delta_v_k`，`omega_{k+1}=omega_k+delta_omega_k`，`p_{k+1}=p_k+0.5*dt*(v_k+v_{k+1})`，`q_{k+1}=q_k ⊗ Exp(0.5*dt*(omega_k+omega_{k+1}))`。小 `delta_p_res` / `delta_theta_res` 只作为 ablation：若加入后 `E_v`、`E_omega`、`E_q` 或 `MSE_1_to_F` 更好则保留，否则删掉，避免重新变成自由预测完整 state。训练协议建议先做完整 state rollout 但提高 `lambda_v/lambda_omega`、降低但不归零 `lambda_p/lambda_q`；用户可优先汇报 `E_v` 和 `E_omega`，`E_q` 也可作为重点，`E_p` 作为辅助，但开发时仍需监控 `p/q` 漂移是否污染 open-loop 后续预测。
- 2026-05-13 16:38 CST `modeldev_20260513_grutcn_rawtoktf_latentsefix_H20_from_physkin_e5_shufflefix_p1` 巡检：训练 tmux `modeldev_grutcn_rawtoktf_latentsefix_H20_from_physkin_e5_shufflefix_p1` 与 GPU watch `modeldev_gpu_watch_grutcn_rawtoktf_latentsefix_shufflefix_H20` 均 alive，无 Traceback/OOM/NaN；`/usr/lib/wsl/lib/nvidia-smi` 显存约 `6565/8188 MiB`、util `41%`。CSVLogger e2 新 best：`valid_loss_epoch=0.4712539911`、`best_valid_loss=0.4712539911`、`valid_p_loss_epoch=0.0433480702`、`valid_v_loss_epoch=0.1599146277`、`valid_q_loss_epoch=0.0388568044`、`valid_omega_loss_epoch=0.2291345745`、`valid_state_mse_epoch=0.2502252162`。checkpoint：`last_model.pth`、`model-epoch=00/01/02-best_valid_loss=0.47.pth`（共 4 个）；`train_summary.json` 尚未生成，horizon/test metric 未读取。判定：继续等待 natural finish / early stop，再按协议跑/read horizon/test MSE-schema。
- 2026-05-13 16:45 CST 修复两处“可能导致误判/误选 checkpoint”的 wiring（未读取 horizon/test metric 内容）：
  1) `scripts/eval.py`：当缺少 `train_summary.json` 时，不再用 checkpoint 文件名里的 `{best_valid_loss:.2f}`（会截断导致 tie）选 best；改为优先用 `train_summary.json.best_model_path`，否则从 `csv_logs/version_*/metrics.csv` 的 `valid_loss_epoch` 选出 best epoch，再按 epoch 精确定位对应 checkpoint；同时 `Average rollout loss` 改为按 batch size 加权。
  2) `scripts/train.py`：收紧 checkpoint shape mismatch 过滤逻辑，除 `raw_token_pos` / `raw_token_adaptive_pos` 外，任何核心参数 shape mismatch 直接报错停止初始化，避免“半随机初始化核心路径”被误当作 checkpoint-safe 候选。
  检查：local `python3 -m py_compile scripts/eval.py scripts/train.py` 通过；并已 `scp` 同步到远程 `/home/ubuntu/Developer/long-horizon-dynamics/scripts/`，local/remote sha256 一致：`eval.py=968648c550edf54f...`、`train.py=b676af40b4d02cc1...`；`MODEL_DEV_CURRENT.md` 也已同步一致（sha256 `cee2f89b46973397...`）。
- 2026-05-13 15:08 CST `modeldev_20260513_grutcn_rawtoktf_latentsefix_H20_from_physkin_e5_shufflefix_p1` 巡检：SSH 已恢复，训练 tmux `modeldev_grutcn_rawtoktf_latentsefix_H20_from_physkin_e5_shufflefix_p1` 与 GPU watch `modeldev_gpu_watch_grutcn_rawtoktf_latentsefix_shufflefix_H20` 均 alive，无 Traceback/OOM/NaN；GPU watch 约 `1181-1409/8187 MiB`。CSVLogger e0 已出：`valid_loss_epoch=0.4714630544`、`best_valid_loss=0.4714630544`、`valid_p=0.0433743075`、`valid_v=0.1600606740`、`valid_q=0.0388789475`、`valid_omega=0.2291491032`、`valid_state_mse=0.2502500415`。判定：e0 低于健康线 `0.47159`，优于 physkin e5 `0.4715962`，说明 validation shuffle 修复后该候选是健康新 best；继续等待 e1/e2 或 natural finish，再按协议跑/read horizon/test MSE-schema。当前 checkpoint 文件数 `2`，`train_summary.json` 尚未生成，horizon/test metric 未读取。
- 2026-05-13 15:08 CST `modeldev_20260513_grutcn_rawtoktf_latentsefix_H20_from_physkin_e5_shufflefix_p1` 巡检：SSH 已恢复，训练 tmux `modeldev_grutcn_rawtoktf_latentsefix_H20_from_physkin_e5_shufflefix_p1` 与 GPU watch `modeldev_gpu_watch_grutcn_rawtoktf_latentsefix_shufflefix_H20` 均 alive，无 Traceback/OOM/NaN；GPU watch 约 `1181-1409/8187 MiB`。CSVLogger e0 已出：`valid_loss_epoch=0.4714630544`、`best_valid_loss=0.4714630544`、`valid_p=0.0433743075`、`valid_v=0.1600606740`、`valid_q=0.0388789475`、`valid_omega=0.2291491032`、`valid_state_mse=0.2502500415`。判定：e0 低于健康线 `0.47159`，优于 physkin e5 `0.4715962`，说明 validation shuffle 修复后该候选是健康新 best；继续等待 e1/e2 或 natural finish，再按协议跑/read horizon/test MSE-schema。当前 checkpoint 文件数 `2`，`train_summary.json` 尚未生成，horizon/test metric 未读取。
- 2026-05-13 14:48 CST heartbeat 巡检远程连接异常：`gpu4060` 和 `gpu4060-ts` 到 Tailscale IP `100.106.154.6` 均 SSH timeout，`gpu4060-lan` 连接到 `192.168.1.108` 后关闭。本轮无法确认 `modeldev_20260513_grutcn_rawtoktf_latentsefix_H20_from_physkin_e5_shufflefix_p1` 的 tmux/CSVLogger/checkpoint 状态；未停止任何实验、未清理 artifacts、未读取 horizon/test metric。结论：先按网络/SSH 短时不可达处理，不把它判为训练失败；下一轮 automation 继续重试远程巡检。
- 2026-05-13 14:25 CST 发现并修复 validation shuffle 协议 bug，重开可比版 active training `modeldev_20260513_grutcn_rawtoktf_latentsefix_H20_from_physkin_e5_shufflefix_p1`。诊断过程：旧 `modeldev_20260513_grutcn_rawtoktf_latentsefix_H20_from_physkin_e5_p1` e0 `valid_loss_epoch=0.5998380780` 最初按 gate 停止，但 no-op 诊断显示根因不是模型结构崩坏，而是 `scripts/dynamics_learning/data.py` 的 `load_dataset()` 对 validation 也使用 `args.shuffle`；在 `limit_val_batches=0.5` 下会随机抽半个验证集，导致与历史 best 不可比。当前代码加载 physkin e5 checkpoint 不训练时，沿用失败实验 `args.shuffle=True` 得到 `valid_loss_epoch=0.6004404`；把 validation loader 固定顺序后，同一 checkpoint 精确回到 `valid_loss_epoch=0.4715962`。代码修复：`load_dataset()` 改为 `shuffle=args.shuffle and mode == "training"`，并已同步远程；修复后训练命令仍可 `--shuffle true`，但 validation/test 使用顺序 sampler。旧 `latentsefix_H20` p1 artifacts 保留，horizon/test metric 未读取，其“结构失败”结论作废为“validation shuffle 协议 bug”。新 active：远程路径 `/home/ubuntu/Developer/long-horizon-dynamics/resources/experiments/modeldev_20260513_grutcn_rawtoktf_latentsefix_H20_from_physkin_e5_shufflefix_p1`；训练 tmux `modeldev_grutcn_rawtoktf_latentsefix_H20_from_physkin_e5_shufflefix_p1`；GPU watch tmux `modeldev_gpu_watch_grutcn_rawtoktf_latentsefix_shufflefix_H20`；log `logs/train_phase1.log`；GPU watch `logs/gpu_watch.log`。结构仍为 GRUTCN H20/F50 raw-token main + `latent_se` channel recalibration，从 physkin e5 初始化，physics loss OFF，trainable patterns 显式避开 `raw_token_adaptive_*`。启动检查：tmux/watch 已启动，checkpoint missing keys 仅 `raw_token_adaptive_*`，GPU watch 首条约 `1090/8187 MiB`，无 Traceback/OOM/NaN。Gate：参考 physkin e5 `best_valid_loss=0.4715962`；e0 `>0.47170` 或 q/v/omega/state_mse 同步明显回退则停止不 eval；e0 `0.47159-0.47170` 观察 e1/e2；e0 `<=0.47159` 健康。natural finish/validation best 后按协议读取 horizon/test MSE-schema，并比较 physkin e5、rawtoktf cont e7、strongest GRU targets。
- 2026-05-13 13:50 CST `modeldev_20260513_grutcn_rawtoktf_latentsefix_H20_from_physkin_e5_p1` 按 gate 判定失败并停止。该实验为 GRUTCN H20 raw-token main branch + `latent_se` channel recalibration，从 physkin e5 checkpoint 初始化，physics loss OFF，并显式避免训练 `raw_token_adaptive_*`。CSVLogger：e0 `valid_loss_epoch=0.5998380780`、`best_valid_loss=0.5998381972`，远高于 stop line `0.47170`；checkpoint `checkpoints/model-epoch=00-best_valid_loss=0.60.pth`。无 OOM/NaN/Traceback；停止前 GPU 约 `6553/8188 MiB`，停止后约 `474/8188 MiB`。已只停止该实验训练 tmux `modeldev_grutcn_rawtoktf_latentsefix_H20_from_physkin_e5_p1`、匹配该实验路径的训练进程和 GPU watch `modeldev_gpu_watch_grutcn_rawtoktf_latentsefix_H20`；artifacts 保留，未读取 horizon/test metric。结论：H20 从 physkin e5 继续也崩到约 `0.600`，说明问题不只是 H30 长历史噪声；优先怀疑当前 `grutcn.py` 相对 physkin checkpoint 的 no-op 兼容被破坏。下一步不要继续同类 raw-token/latent_se 微调，先做 checkpoint/code no-op 诊断：加载 physkin e5 checkpoint 不训练时 validation 是否仍接近 `0.4715962`，若不接近先修复兼容。
- 2026-05-13 13:03 CST 启动新的 active training：`modeldev_20260513_grutcn_rawtoktf_latentsefix_H20_from_physkin_e5_p1`。远程路径：`/home/ubuntu/Developer/long-horizon-dynamics/resources/experiments/modeldev_20260513_grutcn_rawtoktf_latentsefix_H20_from_physkin_e5_p1`；训练 tmux：`modeldev_grutcn_rawtoktf_latentsefix_H20_from_physkin_e5_p1`；GPU watch tmux：`modeldev_gpu_watch_grutcn_rawtoktf_latentsefix_H20`；训练日志：`logs/train_phase1.log`；GPU watch 日志：`logs/gpu_watch.log`；初始化 checkpoint：`resources/experiments/modeldev_20260513_grutcn_rawtoktf_physkin_H20_from_cont_e7_p1/checkpoints/model-epoch=05-best_valid_loss=0.47.pth`。结构/协议：GRUTCN H20/F50，从 physkin e5 继续，physics loss OFF；目标不是继续只微调 physics，而是在 H20 校准内同时训练 raw-token 主分支与 `latent_se` channel recalibration。首次 p1 启动时使用 `raw_token,latent_se` 误把 `raw_token_adaptive_*` 也纳入训练，已立即停止 p1（artifacts 保留，horizon/test 未读）并重启 fix 版；fix 版 trainable patterns 显式为 `latent_se,raw_token_pos,raw_token_input_norm,raw_token_proj,raw_token_encoder,raw_token_query,raw_token_score,raw_token_context_norm,raw_token_head,raw_token_velocity,raw_token_attitude`，确认 trainable list 不含 `raw_token_adaptive_*`。配置：epochs `5`，batch `64`，accumulate `8`，effective batch `512`，`limit_train_batches=0.25`，`limit_val_batches=0.5`，`warmup_lr=3e-7`，`cosine_lr=1e-7`，`warmup_steps=50`，`cosine_steps=1000`，early stopping patience `2`，min_delta `1e-5`，WANDB disabled。启动检查：tmux/process/GPU watch healthy，GPU 约 `6517/8188 MiB`、util `40%`，checkpoint missing keys 仅未训练的 `raw_token_adaptive_*`，未读取 horizon/test metric。Gate：参考 physkin e5 `best_valid_loss=0.4715962` 与 horizon/test `h50 E_q=0.0853132015, E_v=0.3808130120, E_omega=0.3010985338, MSE_1_to_F=0.3995425400`；e0 若 `valid_loss_epoch >0.47170` 或 q/v/omega/state_mse 同步明显回退则停止不 eval；e0 `0.47159-0.47170` 观察 e1/e2；e0 `<=0.47159` 健康。natural finish/validation best 后按协议跑/read horizon/test MSE-schema，并比较 physkin e5、rawtoktf cont e7 和 strongest GRU targets。
- 2026-05-13 12:55 CST `modeldev_20260513_grutcn_rawtoktf_adapthistfix_H30_from_cont_e7_p1` 按 gate 判定失败并停止。该实验为 H30 history-expanded adaptive raw-token fix，从 rawtoktf cont e7 H20 checkpoint 初始化，p2 已修复旧 `raw_history_proj` 只看 recent H20，并且只训练 `raw_token_adaptive_*`；但 e0 validation 仍明显崩坏：日志显示 `train_loss_epoch=0.359`、`valid_loss_epoch=0.604`、`best_valid_loss=0.604`，checkpoint 为 `checkpoints/model-epoch=00-best_valid_loss=0.60.pth`，远高于 stop line `0.47190`。checkpoint 加载日志显示 missing keys 仅 `raw_token_adaptive_*`，无 OOM/NaN/Traceback；GPU watch 约 `5717/8188 MiB`，停止后 GPU 回到约 `474/8188 MiB`。已只停止该实验训练 tmux `modeldev_grutcn_rawtoktf_adapthistfix_H30_from_cont_e7_p1`、匹配该实验路径的训练进程和 GPU watch `modeldev_gpu_watch_grutcn_rawtoktf_adapthistfix_H30`；artifacts 保留，未读取 horizon/test metric。结论：H30 adaptive-only 分支即使修复旧 raw-history 校准后仍会在第一个 epoch 迅速破坏 validation，暂不继续同形态 H30；下一步应避免继续“长历史 adaptive 分支单独训练”，优先改成更保守的短窗校准 + 长窗只读/强 gate/极小 LR 或回到 H20 强结构升级。
- 2026-05-13 11:50 CST `modeldev_20260513_grutcn_rawtoktf_adapthist_H30_from_cont_e7_p1` 按 gate 判定失败并停止。该实验为 H30 history-expanded adaptive raw-token p1，从 rawtoktf cont e7 H20 checkpoint 初始化；e0 validation `valid_loss_epoch=0.6041508` / `best_valid_loss=0.6041512`，远高于 stop line `0.47190`，未生成 `train_summary.json`，未读取 horizon/test metric。GPU watch 显示约 `7536/8188 MiB`，无 OOM/NaN/Traceback。已只停止该实验训练 tmux `modeldev_grutcn_rawtoktf_adapthist_H30_from_cont_e7_p1`、匹配该实验 id 的训练进程和 GPU watch `modeldev_gpu_watch_grutcn_rawtoktf_adapthist_H30`；GPU 回到约 `453/8188 MiB`，artifacts 保留。
- 2026-05-13 11:53 CST 修复 H30 checkpoint 迁移风险并启动 p2。问题复盘：p1 虽然 `encoder`/raw-token anchor 已用 `anchor_history_len=20`，但旧的 `raw_history_proj(x).mean(dim=1)` 仍直接吃 full H30；该分支来自 H20 checkpoint 且不是新 zero-init 分支，可能破坏校准。代码改动：`scripts/dynamics_learning/models/grutcn.py` 在 forward 中改为 `raw_history_proj(x_anchor).mean(dim=1)`，让旧 raw-history residual 也只看 recent H20，长历史只通过 `raw_token_adaptive_*` zero-init 分支进入。local py_compile、`git diff --check`、remote py_compile 通过；local/remote `grutcn.py` hash 均为 `631e12d6b96120477b9108cf59b9fc5ee506e2b41ba29d28f6794474f3f112ac`。remote smoke `smoke_20260513_grutcn_adapthist_H30_anchorrawfix_p1` 从 H20 e7 checkpoint 加载，missing keys 仅新增 `raw_token_adaptive_*`，无 unexpected/shape-mismatch；一批 train/valid finite，未读取 horizon/test metric。
- 当前 active training：`modeldev_20260513_grutcn_rawtoktf_adapthistfix_H30_from_cont_e7_p1`。远程路径：`/home/ubuntu/Developer/long-horizon-dynamics/resources/experiments/modeldev_20260513_grutcn_rawtoktf_adapthistfix_H30_from_cont_e7_p1`；训练 tmux：`modeldev_grutcn_rawtoktf_adapthistfix_H30_from_cont_e7_p1`；GPU watch tmux：`modeldev_gpu_watch_grutcn_rawtoktf_adapthistfix_H30`；训练日志：`logs/train_phase1.log`；GPU watch 日志：`logs/gpu_watch.log`；init checkpoint：`resources/experiments/modeldev_20260512_grutcn_rawtoktf_cont_H20_from_rawtok_e4_p1/checkpoints/model-epoch=07-best_valid_loss=0.47.pth`。结构：H30 history-expanded raw-token adaptive fix，anchor/base/raw-history/raw-token anchor 均保持 recent H20 校准，full H30 只进 `raw_token_adaptive_*`；本轮 `--trainable_parameter_patterns raw_token_adaptive`，冻结旧 H20 raw-token 主分支，只训练新 adaptive 长历史分支，physics loss OFF。配置：epochs `6`，batch `64`，accumulate `8`，effective batch `512`，`limit_train_batches=0.25`，`limit_val_batches=0.5`，`warmup_lr=8e-7`，`cosine_lr=2e-7`，`warmup_steps=50`，`cosine_steps=1500`，early stopping patience `2`，min_delta `1e-5`，WANDB disabled。Gate：e0 `>0.47190` 或 q/v/omega/state_mse 同步明显回退则停止不 eval；e0 `0.47165-0.47190` 观察 e1/e2；e0 `<=0.47165` 健康。natural finish/validation best 后按协议读取 horizon/test MSE-schema，并比较 rawtoktf cont e7、physkin e5 和 strongest GRU targets。

状态：

- 2026-05-12 12:01 CST 启动 active training `modeldev_20260512_grutcn_jointobserver_H20_from_anchor_e4_p1`。结构/协议：无代码改动，沿用当前 `scripts/dynamics_learning/models/grutcn.py` 中已存在的 zero-init `attitude_observer_*` 和 `velocity_observer_*`，从 GRUTCN anchor e4 初始化，并用 `--trainable_parameter_patterns attitude_observer,velocity_observer` 冻结 anchor，只训练两个 output-space observer；forward 只通过这两个分支写 `y[:, 3:6]` 与 `y[:, 6:12]`，不写 `y[:, 0:3]`，不改 `encoder`、`base_decoder`、`state_initializer` 或 GRU memory。动机：刚完成的 attobserver horizon/test 显示 frozen output observer 对 h50 q/v/omega 均有微小正增益，velocity_observer 单独对 h50_v 有极微小信号，联合训练测试二者是否可叠加。
- remote smoke `smoke_20260512_grutcn_jointobserver_H20_from_anchor_e4_p1` 从 anchor e4 加载成功，missing keys 为新增/兼容 observer 分支，无 unexpected keys；trainable parameters 仅 `velocity_observer*` 与 `attitude_observer*`；一批 train/valid finite：`train_loss_step=0.138`、`valid_loss_epoch=0.0808`；只确认 `plots/testset` 存在，未读取 horizon/test metric。正式训练配置：GRUTCN H20/F50，epochs `5`，batch `64`、accumulate `8`、effective batch `512`，`limit_train_batches=0.25`、`limit_val_batches=0.5`，`warmup_lr=8e-7`、`cosine_lr=2e-7`，`warmup_steps=50`、`cosine_steps=1000`，early stopping patience `2`、min_delta `2e-5`，WANDB disabled。tmux `modeldev_grutcn_jointobserver_H20_from_anchor_e4_p1`；GPU watch `modeldev_gpu_watch_grutcn_jointobserver_H20`；log `resources/experiments/modeldev_20260512_grutcn_jointobserver_H20_from_anchor_e4_p1/logs/train_phase1.log`。启动巡检：tmux/process/GPU watch healthy，GPU 约 `2527/8188 MiB`、util `33%`，epoch 0 train steps finite；`plots/testset` 目录存在但未读取 horizon/test metric。gate：参考 attobserver e4 `best_valid_loss=0.4720434` 与 horizon/test 小幅正增益；e0 `>0.4728` 停止不 eval；e0 `0.47210-0.47280` 观察 e1/e2；e0 `<=0.47205` 健康。训练自然结束后按新协议读取 horizon/test metric，重点比较 attobserver e4 的 h50 `E_q=0.0853396507`、`E_v=0.3809912724`、`E_omega=0.3012224156`、mean `E_q=0.0437367480` 是否继续改善。
- 2026-05-12 12:49 CST `modeldev_20260512_grutcn_jointobserver_H20_from_anchor_e4_p1` 巡检：训练仍在 tmux 中运行，GPU watch 正常，GPU 约 `2545/8188 MiB`、util `33%`；CSVLogger 已有 e0/e1 validation，e0 `valid_loss_epoch=0.4720957`，e1 `valid_loss_epoch=0.4720626`、`best_valid_loss=0.4720626`，checkpoint e0/e1 与 `last_model.pth` 已产生，`train_summary.json` 尚未生成，未读取 horizon/test metric。判定：e1 比 e0 小幅刷新 best，低于观察区上沿但仍未达到健康线 `<=0.47205`；继续等待 e2 或 natural finish，再决定是否按新协议做 horizon/test evaluation。
- 2026-05-12 13:05 CST `modeldev_20260512_grutcn_jointobserver_H20_from_anchor_e4_p1` e2 validation 已写入 CSVLogger：e0/e1/e2 `valid_loss_epoch=0.4720957 -> 0.4720626 -> 0.4720305`，当前 `best_valid_loss=0.4720304`，训练 tmux 与 GPU watch 仍在运行，`train_summary.json` 尚未生成，`horizon_summary.json` 尚未生成，未读取 horizon/test metric。判定：e2 已优于 attobserver e4 validation `0.4720434`，是当前 GRUTCN frozen-observer 分支的新 validation best，但仍需等待 natural finish/summary 后按新协议读取 horizon/test，不能仅凭 validation 宣称超过最强 long-horizon 主线。
- 2026-05-12 13:38 CST `modeldev_20260512_grutcn_jointobserver_H20_from_anchor_e4_p1` e3 validation 继续刷新：e0/e1/e2/e3 `valid_loss_epoch=0.4720957 -> 0.4720626 -> 0.4720305 -> 0.4720052`，当前 `best_valid_loss=0.4720051`。训练 tmux 与 GPU watch 仍在运行，GPU 约 `2545/8188 MiB`、util `34%`，`train_summary.json` 尚未生成，`horizon_summary.json` 尚未生成，未读取 horizon/test metric。判定：e3 已进入健康区并非常接近旧 `<=0.47200` evaluation 线，是 GRUTCN observer 分支目前最强 validation 候选；等待 natural finish/summary 后应按新协议做 horizon/test evaluation，再判断是否真正超过 attobserver/anchor/strongest GRU long-horizon 指标。
- 2026-05-12 14:30 CST `modeldev_20260512_grutcn_jointobserver_H20_from_anchor_e4_p1` 训练自然结束并启动 horizon/test-guided evaluation。训练结果：e0/e1/e2/e3/e4 `valid_loss_epoch=0.4720957 -> 0.4720626 -> 0.4720305 -> 0.4720052 -> 0.4719877`，`best_valid_loss=0.4719877`，`train_summary.json` 指向 `checkpoints/model-epoch=04-best_valid_loss=0.47.pth`；训练阶段未读取 horizon/test metric，artifacts 保留。判定：e4 低于旧 evaluation 线 `<=0.47200`，是当前 GRUTCN observer 分支最强 validation 候选；已停止训练 GPU watch `modeldev_gpu_watch_grutcn_jointobserver_H20`，启动 eval tmux `eval_grutcn_jointobserver_H20_e4_p1`，eval GPU watch `gpu_watch_eval_grutcn_jointobserver_H20`，eval log `logs/eval_horizontest_20260512_grutcn_jointobserver_H20_e4_b32.log`，命令使用 `scripts/eval.py --eval_batch_size 32 --eval_horizons 1,10,25,50 --experiment_path resources/experiments/modeldev_20260512_grutcn_jointobserver_H20_from_anchor_e4_p1`。启动检查：eval tmux/process/GPU watch healthy，GPU 约 `539/8188 MiB`、util `3%`，日志已写入 `Seed set to 10`，`horizon_summary.json` 尚未生成，尚未读取 horizon/test metric。完成后必须记录 checkpoint、average rollout loss、h50/mean 关键指标、是否影响后续结构决策和结论。
- 2026-05-12 14:52 CST 按用户要求加入截图里的 multi-step full-state MSE 指标。文件改动：`scripts/eval.py` 在 horizon/test 输出中新增每个 horizon 的 `MSE_x = mean_i ||x_{i,h}-xhat_{i,h}||_2^2`，并在 `horizon_summary.json` 中新增 `MSE_1_to_F = mean_h MSE_x(h)`；`scripts/dynamics_learning/lighting.py` 新增 `state_mse` 指标并记录 `train_state_mse`、`valid_state_mse`、`test_state_mse`，但不并入训练目标、不改变 `valid_loss` / `best_valid_loss`。检查：local `python -m py_compile scripts/eval.py scripts/dynamics_learning/lighting.py`、`git diff --check` 通过；remote py_compile 通过；local/remote hash 一致：`eval.py=473cc1104ec72a237e5f8ddccdf35a9364d2337cdccb5b88541a2d5a733e636e`、`lighting.py=411a2105b488df26baa2aebc4fa3cf92337fc39b13353f67717df4a4517bb21b`。一次 `rsync --relative` 误把副本放到远程 `scripts/scripts/...`，已只删除本次误放的 `scripts/scripts/eval.py` 与 `scripts/scripts/dynamics_learning/lighting.py`，确认无残留。
- `modeldev_20260512_grutcn_jointobserver_H20_from_anchor_e4_p1` 首次 eval 在 MSE 代码同步前已完成，读取到 old-schema log 中 `Average rollout loss=0.5631942749`，但 `horizon_metrics.csv` header 仍为 `horizon,E_p,E_v,E_q,E_omega`，没有 `MSE_x`；该读取只用于确认需要重跑新 schema，不用于结构调参结论。已保留 artifacts 并启动同 checkpoint 的 MSE-schema rerun：tmux `eval_grutcn_jointobserver_H20_e4_mse_p1`，GPU watch `gpu_watch_eval_grutcn_jointobserver_H20`，log `logs/eval_horizontest_20260512_grutcn_jointobserver_H20_e4_b32_mse.log`。启动检查：tmux healthy，日志写入 `Seed set to 10`；rerun 完成后必须读取并记录 average rollout loss、h50/mean 指标和新增 `MSE_1_to_F`。
- 2026-05-12 用户提出下一阶段结构 idea，已确认作为待尝试候选：若 jointobserver horizon/test 仍只是小幅提升，下一候选优先做 GRUTCN/TCNLSTM 的 checkpoint-safe latent SENet/SE gate，接在 TCN encoder 输出 `enc_seq` 后而不是原始 `x[:, :, feature]` 前，形式为 `enc_seq = enc_seq + scale * enc_seq * zero_init_se_delta`，避免直接缩放 quaternion、omega、control 原始量纲并保持旧 checkpoint 初始 no-op。若 SE 有信号，再升级为 raw-history token Transformer side branch：把原始历史 `x` 投到 latent tokens，用 1-2 层 lightweight self-attention 提取长时域 context，zero-init 注入 observer/head，不替换 `encoder`、`base_decoder`、`state_initializer` 或 GRU memory。优先级：先 latent SE，小步安全；再 raw-input Transformer side context，作为更强结构升级；不要继续无结构含义地堆 output observer。
- 2026-05-12 15:12 CST 修复新增 `MSE_x` 指标的 eval 汇总 wiring：`compute_horizon_metrics` 已产生 `MSE_x`，但 `run_prediction` 的 `metric_sums` 仍用旧四指标列表，导致 `_mse` rerun 在 summary 阶段 `KeyError: 'MSE_x'`。修复：`scripts/eval.py` 增加统一 `HORIZON_METRICS = ["E_p", "E_v", "E_q", "E_omega", "MSE_x"]`，`summarize_horizon_metrics`、CSV header、`run_prediction` 聚合共同使用该列表。local `python -m py_compile scripts/eval.py scripts/dynamics_learning/lighting.py` 与 `git diff --check` 通过；remote py_compile 通过；local/remote hash 一致：`eval.py=69631944db1592c51934ff0a0d58079f319dcfdbd7c8b128335aaaa89872e657`、`lighting.py=411a2105b488df26baa2aebc4fa3cf92337fc39b13353f67717df4a4517bb21b`。已启动同 checkpoint 的 eval rerun `eval_grutcn_jointobserver_H20_e4_mse_p2`，log `logs/eval_horizontest_20260512_grutcn_jointobserver_H20_e4_b32_mse_p2.log`；完成后读取并记录 h50/mean 与 `MSE_1_to_F`。
- 2026-05-12 16:59 CST `modeldev_20260512_grutcn_jointobserver_H20_from_anchor_e4_p1` e4 MSE-schema rerun p2 完成并读取 horizon/test metric 内容。checkpoint：`checkpoints/model-epoch=04-best_valid_loss=0.47.pth`，training `best_valid_loss=0.4719877`，eval log `logs/eval_horizontest_20260512_grutcn_jointobserver_H20_e4_b32_mse_p2.log`，average rollout loss `0.5631942749`；h50 `E_q=0.0853401803`、`E_v=0.3809713071`、`E_omega=0.3012216526`、`MSE_x=0.7803988811`；mean `E_q=0.0437370777`、`E_v=0.2160828500`、`E_omega=0.2426515404`；新增 `MSE_1_to_F=0.3996392623`。与 attobserver e4 相比，`h50_v` 和 `h50_omega` 仅极小改善，`h50_q` 与 mean `E_q` 极小回退；仍明显落后 strongest GRU targets（h50_q `0.0800042`、h50_v `0.353015`、h50_omega `0.260392`、mean_q `0.0420377`）。结论：output observer 叠加没有形成有效结构增益，horizon/test 读取已用于结构决策；下一候选转向 checkpoint-safe latent SE residual，接在 GRUTCN TCN encoder latent `enc_seq` 后，先不继续堆 output observer。artifacts 保留，eval tmux/GPU watch 已自然结束，无清理。
- 2026-05-12 17:08 CST 启动 active training `modeldev_20260512_grutcn_latentse_H20_from_anchor_e4_p1`。代码/结构：`scripts/dynamics_learning/models/grutcn.py` 新增 checkpoint-safe latent SE residual，位于 `_encode()` 中 `encoder_norm` 后、进入现有 temporal refiner/context GRU 前；形式 `enc_seq = enc_seq + latent_se_scale * enc_seq * tanh(latent_se_gate(LayerNorm(mean_t enc_seq)))`，`latent_se_gate` final projection zero-init，`latent_se_scale=0.02`，初始对 anchor checkpoint 严格 no-op；不直接缩放原始 `x/q/u`，不改 `base_decoder`、`state_initializer` 或 GRU memory，旧 output-observer 分支仅保留 key 兼容且本实验不训练。检查：local `python -m py_compile scripts/dynamics_learning/models/grutcn.py` 和 `git diff --check -- scripts/dynamics_learning/models/grutcn.py` 通过；local/remote `grutcn.py` hash 均为 `efe53c64bc86811ff3bd0d4bec38210bd08aa0b211138c3a0136ec54c6b21c86`；remote py_compile 通过。remote smoke `smoke_20260512_grutcn_latentse_H20_from_anchor_e4_p1` 从 GRUTCN anchor e4 checkpoint 加载成功，missing keys 为新增/兼容分支，无 unexpected keys；trainable parameters 仅 `latent_se*`，一批 train/valid finite：`train_loss_step=0.118`、`valid_loss_epoch=0.0771`，checkpoint、CSVLogger、`train_summary.json` 正常；smoke 未读取 horizon/test metric。正式训练：从 `resources/experiments/modeldev_20260510_grutcn_anchor_e7_ultralow_p2/checkpoints/model-epoch=04-best_valid_loss=0.47.pth` 初始化，H20/F50，epochs `5`，batch `64`、accumulate `8`、effective batch `512`，`limit_train_batches=0.25`、`limit_val_batches=0.5`，`warmup_lr=8e-7`、`cosine_lr=2e-7`，early stopping patience `2`、min_delta `2e-5`，WANDB disabled，`--trainable_parameter_patterns latent_se`。tmux `modeldev_grutcn_latentse_H20_from_anchor_e4_p1`；GPU watch `modeldev_gpu_watch_grutcn_latentse_H20`；log `resources/experiments/modeldev_20260512_grutcn_latentse_H20_from_anchor_e4_p1/logs/train_phase1.log`。启动巡检：tmux/process/GPU watch healthy，GPU 约 `2671/8188 MiB`、util `35%`，epoch 0 train steps finite；horizon/test metric 尚未读取。Gate：e0 `>0.4728` 停止不 eval；e0 `0.47210-0.47280` 观察 e1/e2；e0 `<=0.47205` 健康。训练 natural finish 或 validation best 后按新协议读取 horizon/test，重点看是否优于 jointobserver/attobserver 的 h50 `E_q/E_v/E_omega`、mean `E_q` 与新增 `MSE_1_to_F`，并继续比较 strongest GRU targets。
- 2026-05-12 18:04 CST `modeldev_20260512_grutcn_latentse_H20_from_anchor_e4_p1` 巡检：训练 tmux/GPU watch 仍健康，GPU 约 `2669/8188 MiB`、util `34%`，训练进入 e2；CSVLogger 已有 e0/e1 validation，e0 `valid_loss_epoch=0.4721178`、`best_valid_loss=0.4721180`、`valid_q=0.0389169`、`valid_p=0.0434284`、`valid_v=0.1605331`、`valid_omega=0.2292395`、`valid_state_mse=0.2503605`；e1 小幅刷新为 `valid_loss_epoch=0.4721160`、`best_valid_loss=0.4721158`、`valid_q=0.0389169`、`valid_p=0.0434278`、`valid_v=0.1605319`、`valid_omega=0.2292391`、`valid_state_mse=0.2503596`；checkpoint e0/e1 与 `last_model.pth` 已产生，`train_summary.json` 尚未生成，horizon/test metric 未读取。判定：e0/e1 均在灰区 `0.47210-0.47280`，未触发停止，也未达到健康线 `<=0.47205`；继续观察 e2/natural finish，再决定是否 eval。
- 2026-05-12 18:58 CST `modeldev_20260512_grutcn_latentse_H20_from_anchor_e4_p1` 训练 early stop 完成并启动 horizon/test MSE-schema evaluation。训练结果：e0/e1/e2 `valid_loss_epoch=0.4721178 -> 0.4721160 -> 0.4721141`，`best_valid_loss=0.4721142`，`train_summary.json` 指向 `checkpoints/model-epoch=02-best_valid_loss=0.47.pth`，`early_stopped=true`、`stopped_epoch=3`；训练阶段未读取 horizon/test metric，artifacts 保留。判定：latent SE 仅 validation 微小改善，仍在灰区且弱于 jointobserver validation，但按新协议需要读取 horizon/test 判断是否改善长时域聚合指标。已停止训练 GPU watch `modeldev_gpu_watch_grutcn_latentse_H20`；启动 eval tmux `eval_grutcn_latentse_H20_e2_mse_p1`，eval GPU watch `gpu_watch_eval_grutcn_latentse_H20`，eval log `logs/eval_horizontest_20260512_grutcn_latentse_H20_e2_b32_mse_p1.log`，GPU 启动约 `732/8188 MiB`、util `34%`，日志已写入 `Seed set to 10`；尚未读取 horizon/test metric。完成后必须记录 checkpoint、h50/mean `E_q/E_v/E_omega`、`MSE_1_to_F`、是否影响结构决策和结论。
- 2026-05-12 19:12 CST `modeldev_20260512_grutcn_latentse_H20_from_anchor_e4_p1` e2 horizon/test MSE-schema eval 完成并读取 metric 内容。checkpoint：`checkpoints/model-epoch=02-best_valid_loss=0.47.pth`，training `best_valid_loss=0.4721142`，eval log `logs/eval_horizontest_20260512_grutcn_latentse_H20_e2_b32_mse_p1.log`，average rollout loss `0.5632528663`；h50 `E_q=0.0853664371`、`E_v=0.3810531020`、`E_omega=0.3012494289`、`MSE_x=0.7804412069`；mean `E_q=0.0437534364`、`E_v=0.2161137365`、`E_omega=0.2426598685`；`MSE_1_to_F=0.3996510302`。与 jointobserver p2 相比全面或近全面更差（jointobserver h50 `E_q=0.0853401803`、`E_v=0.3809713071`、`E_omega=0.3012216526`、`MSE_1_to_F=0.3996392623`），也没有接近 strongest GRU targets。结论：latent SE 小 adapter 方向没有形成有效长时域增益，不值得继续调 scale/lr；本次 horizon/test 读取已用于结构决策。artifacts 保留，eval GPU watch 已停止，当前无 active training/eval。下一步应转向更大表达力但 checkpoint-safe 的 raw-history token Transformer side branch，或基于 web research 引入更强的历史记忆/频域/状态空间模块，仍需 zero-init 注入且不泄漏数据。
- 2026-05-12 19:25 CST 启动 active training `modeldev_20260512_grutcn_rawtoktf_H20_from_joint_e4_p1`。结构/来源：基于用户提出的 Transformer 输入分支 idea，并参考 PatchTST / iTransformer 的 time-series token 建模思路（只用公开文献/结构概念，不上传或泄漏任何私有数据），在 `scripts/dynamics_learning/models/grutcn.py` 新增 checkpoint-safe `raw_token_*` 分支。该分支把原始历史 `[x,u]` 与差分 `dx` 拼成 tokens，经 `LayerNorm -> Linear -> 2-layer TransformerEncoder -> query pooling` 形成 `raw_token_context`；再通过 zero-init `raw_token_head_delta` 注入 `head_input`，并通过 zero-init `raw_token_velocity_decoder` / `raw_token_attitude_decoder` 只写 `y[:, 3:6]` 和 `y[:, 6:12]`。不替换 `encoder`、`base_decoder`、`state_initializer` 或 GRU memory；旧 observer 与 latent SE 保留 key 兼容。新增分支 final projections zero-init，因此从旧 checkpoint 加载时初始 no-op。文件检查：local `python3 -m py_compile scripts/dynamics_learning/models/grutcn.py`、`git diff --check -- scripts/dynamics_learning/models/grutcn.py` 通过；local/remote hash 均为 `4e5fe4251d34dca3390e8c005cf08022cf09168dfd94d9236089403dda6e5248`；remote py_compile 通过。remote smoke `smoke_20260512_grutcn_rawtoktf_H20_from_joint_e4_p1` 从 jointobserver e4 checkpoint 加载成功，missing keys 为新增 `raw_token_*` 与 zero-init `latent_se_*` 兼容分支，无 unexpected keys；trainable parameters 仅 `raw_token*`；一批 train/valid finite：`train_loss_step=0.146`、`valid_loss_epoch=0.0819`，checkpoint、CSVLogger、`train_summary.json` 正常；未读取 horizon/test metric，仅确认 `plots/testset` 存在。正式训练从 `resources/experiments/modeldev_20260512_grutcn_jointobserver_H20_from_anchor_e4_p1/checkpoints/model-epoch=04-best_valid_loss=0.47.pth` 初始化，H20/F50，epochs `5`，batch `64`、accumulate `8`、effective batch `512`，`limit_train_batches=0.25`、`limit_val_batches=0.5`，`warmup_lr=8e-7`、`cosine_lr=2e-7`，early stopping patience `2`、min_delta `2e-5`，WANDB disabled，`--trainable_parameter_patterns raw_token`。tmux `modeldev_grutcn_rawtoktf_H20_from_joint_e4_p1`；GPU watch `modeldev_gpu_watch_grutcn_rawtoktf_H20`；log `resources/experiments/modeldev_20260512_grutcn_rawtoktf_H20_from_joint_e4_p1/logs/train_phase1.log`。启动巡检：tmux/process/GPU watch healthy，GPU 约 `4718/8188 MiB`、util `30%`，train steps finite；horizon/test metric 尚未读取。Gate：e0 `>0.4730` 停止不 eval；e0 `0.47220-0.47300` 最多观察 e1/e2；e0 `<=0.47205` 健康；优先 eval/freeze 线 `best_valid_loss <=0.47195`，次级 eval 线 `<=0.47205` 且 valid submetrics/state_mse 不同步恶化；若 natural finish 后 best 在 `0.47205-0.47215` 但曲线持续下降，可做一次开发期 horizon/test-guided screening，并记录指标。
- 2026-05-12 20:37 CST `modeldev_20260512_grutcn_rawtoktf_H20_from_joint_e4_p1` 巡检：训练 tmux 与 GPU watch tmux 仍健康，训练进程存在，GPU watch 显示约 `4720/8188 MiB`、util `30-41%`；CSVLogger 已有 e0/e1 validation。e0 `valid_loss_epoch=0.4719697`、`best_valid_loss=0.4719697`、`valid_q=0.0389067`、`valid_p=0.0434165`、`valid_v=0.1604236`、`valid_omega=0.2292230`、`valid_state_mse=0.2503312`；e1 刷新到 `valid_loss_epoch=0.4719442`、`best_valid_loss=0.4719442`、`valid_q=0.0389052`、`valid_p=0.0434147`、`valid_v=0.1604052`、`valid_omega=0.2292190`、`valid_state_mse=0.2503258`。checkpoint e0/e1 与 `last_model.pth` 已产生，`train_summary.json` 尚未生成，`horizon_summary.json` / `horizon_metrics.csv` 尚未生成，未读取 horizon/test metric 内容。判定：e1 已低于优先 eval/freeze 线 `<=0.47195`，且 validation submetrics/state_mse 同步小幅改善；继续训练到 natural finish/summary，再用 `train_summary.json` 指向的 best checkpoint 启动 horizon/test MSE-schema evaluation。
- 2026-05-12 22:22 CST `modeldev_20260512_grutcn_rawtoktf_H20_from_joint_e4_p1` 训练自然跑满 `max_epochs=5` 并启动 horizon/test MSE-schema evaluation。训练结果：e0/e1/e2/e3/e4 `valid_loss_epoch=0.4719697 -> 0.4719442 -> 0.4719169 -> 0.4718953 -> 0.4718790`，`best_valid_loss=0.4718790`，e4 submetrics `valid_q=0.0389019`、`valid_p=0.0434097`、`valid_v=0.1603578`、`valid_omega=0.2292095`、`valid_state_mse=0.2503134`，全部随训练同步小幅改善；`train_summary.json` 指向 `checkpoints/model-epoch=04-best_valid_loss=0.47.pth`，`early_stopped=false`、`stopped_epoch=5`。训练阶段未读取 horizon/test metric，artifacts 保留；已停止训练期 GPU watch `modeldev_gpu_watch_grutcn_rawtoktf_H20`，启动 eval tmux `eval_grutcn_rawtoktf_H20_e4_mse_p1` 与 eval GPU watch `gpu_watch_eval_grutcn_rawtoktf_H20`，eval log `logs/eval_horizontest_20260512_grutcn_rawtoktf_H20_e4_b32_mse_p1.log`，命令使用 `scripts/eval.py --eval_batch_size 32 --eval_horizons 1,10,25,50 --experiment_path resources/experiments/modeldev_20260512_grutcn_rawtoktf_H20_from_joint_e4_p1`。启动检查：eval tmux/process/GPU watch healthy，GPU 约 `795/8188 MiB`、util `35%`，日志已写入 `Seed set to 10`；`horizon_summary.json` / `horizon_metrics.csv` 尚未生成，尚未读取 horizon/test metric。完成后必须读取并记录 h50/mean `E_q/E_v/E_omega`、`MSE_1_to_F`，并与 jointobserver p2 与 strongest GRU targets 比较。
- 2026-05-12 22:51 CST `modeldev_20260512_grutcn_rawtoktf_H20_from_joint_e4_p1` e4 horizon/test MSE-schema eval 完成并读取 metric 内容。checkpoint：`checkpoints/model-epoch=04-best_valid_loss=0.47.pth`，training `best_valid_loss=0.4718789756`，eval log `logs/eval_horizontest_20260512_grutcn_rawtoktf_H20_e4_b32_mse_p1.log`，average rollout loss `0.5631443858`；h50 `E_q=0.0853260663`、`E_v=0.3809074430`、`E_omega=0.3011899105`、`MSE_x=0.7803351939`；mean `E_q=0.0437284932`、`E_v=0.2160562063`、`E_omega=0.2426384474`；`MSE_1_to_F=0.3996152170`。对比 jointobserver p2（h50 `E_q=0.0853401803`、`E_v=0.3809713071`、`E_omega=0.3012216526`、mean `E_q=0.0437370777`、`MSE_1_to_F=0.3996392623`）为小幅全面改善，说明 raw-history token Transformer side branch 有真实但很小的 long-horizon 正信号；但仍明显落后 strongest GRU targets（h50_q `0.0800042`、h50_v `0.353015`、h50_omega `0.260392`、mean_q `0.0420377`）。该 horizon/test 读取已用于结构决策：保留 rawtoktf 方向，下一步优先从 e4 checkpoint 做 continuation/更长训练，必要时叠加小权重 physics-informed regularization + slack/reliability gate；不回到 latent SE 或单纯 output observer。artifacts 保留，eval tmux/GPU watch 已自然结束，无清理，当前无 active training/eval。
- 2026-05-12 22:56 CST 启动 active training `modeldev_20260512_grutcn_rawtoktf_cont_H20_from_rawtok_e4_p1`，用于验证 rawtoktf e4 是否欠训练。初始化 checkpoint：`resources/experiments/modeldev_20260512_grutcn_rawtoktf_H20_from_joint_e4_p1/checkpoints/model-epoch=04-best_valid_loss=0.47.pth`；结构不改，继续只训练 `raw_token` 分支，`encoder`、`base_decoder`、`state_initializer`、GRU memory 仍冻结/不替换。配置：GRUTCN H20/F50，epochs `8`，batch `64`、accumulate `8`、effective batch `512`，`limit_train_batches=0.25`、`limit_val_batches=0.5`，`warmup_lr=8e-7`、`cosine_lr=2e-7`，`warmup_steps=50`、`cosine_steps=1500`，early stopping patience `3`、min_delta `1e-5`，WANDB disabled，`--trainable_parameter_patterns raw_token`。tmux `modeldev_grutcn_rawtoktf_cont_H20_from_rawtok_e4_p1`；GPU watch `modeldev_gpu_watch_grutcn_rawtoktf_cont_H20`；log `resources/experiments/modeldev_20260512_grutcn_rawtoktf_cont_H20_from_rawtok_e4_p1/logs/train_phase1.log`。启动检查：tmux/process/GPU watch healthy，GPU 约 `4719/8188 MiB`、util `33%`，checkpoint 已加载，trainable parameters 仅 `raw_token*`，epoch 0 train steps finite；当前训练阶段未读取新的 horizon/test metric。总控 automation 已更新为每 `40` 分钟巡检该实验。Gate：continuation e0 若 `valid_loss >0.47195` 或 valid q/v/omega/state_mse 同步回退则停止不 eval；e0 `<=0.47188` 或后续继续刷新则健康；natural finish/validation best 后按协议读取 horizon/test，并与 rawtoktf e4 和 strongest GRU targets 比较。
- 2026-05-12 23:40 CST `modeldev_20260512_grutcn_rawtoktf_cont_H20_from_rawtok_e4_p1` e0 validation 已刷新 rawtoktf best：`valid_loss_epoch=0.4718495`、`best_valid_loss=0.4718494`，checkpoint `checkpoints/model-epoch=00-best_valid_loss=0.47.pth` 与 `last_model.pth` 已生成；训练继续进入 e1，GPU watch healthy，GPU 约 `4698/8188 MiB`、util `30%`，无 OOM/NaN/Traceback。判定：e0 低于健康线 `<=0.47188`，支持 rawtoktf e4 确有欠训练，继续等待后续 epoch/natural finish；训练阶段未读取新的 horizon/test metric。
- 2026-05-13 00:20 CST `modeldev_20260512_grutcn_rawtoktf_cont_H20_from_rawtok_e4_p1` e1 validation 继续刷新：`valid_loss_epoch=0.4718124`、`best_valid_loss=0.4718124`，checkpoint `checkpoints/model-epoch=01-best_valid_loss=0.47.pth` 与 `last_model.pth` 已更新；训练进入 e2，GPU watch healthy，GPU 约 `4702/8188 MiB`、util `34%`，无 OOM/NaN/Traceback。判定：continuation 明确有效，继续等待 natural finish/summary 后按协议做 horizon/test MSE-schema eval；训练阶段未读取新的 horizon/test metric。
- 2026-05-13 04:21 CST `modeldev_20260512_grutcn_rawtoktf_cont_H20_from_rawtok_e4_p1` continuation 自然跑满 `max_epochs=8`，`early_stopped=false`，`train_summary.json` 指向 `checkpoints/model-epoch=07-best_valid_loss=0.47.pth`。validation 曲线 e0-e7：`0.4718495 -> 0.4718124 -> 0.4717747 -> 0.4717430 -> 0.4717138 -> 0.4716942 -> 0.4716801 -> 0.4716686`，`best_valid_loss=0.4716687`，继续显著优于 rawtoktf e4 `0.4718790`；训练阶段未读取新的 horizon/test metric，artifacts 保留，无 OOM/NaN/Traceback。已停止训练 GPU watch `modeldev_gpu_watch_grutcn_rawtoktf_cont_H20`，启动 horizon/test MSE-schema eval：tmux `eval_grutcn_rawtoktf_cont_H20_e7_mse_p1`，GPU watch `gpu_watch_eval_grutcn_rawtoktf_cont_H20`，eval log `resources/experiments/modeldev_20260512_grutcn_rawtoktf_cont_H20_from_rawtok_e4_p1/logs/eval_horizontest_20260513_grutcn_rawtoktf_cont_H20_e7_b32_mse_p1.log`，eval GPU watch log `logs/gpu_watch_eval.log`；启动检查显示 eval process healthy，GPU 约 `529/8188 MiB`、util `2%`，`horizon_summary.json` / `horizon_metrics.csv` 尚未生成，尚未读取本次 horizon/test metric。下一步：eval 完成后读取 h50/mean `E_q/E_v/E_omega` 与 `MSE_1_to_F`，记录是否影响后续结构决策，并与 rawtoktf e4、jointobserver p2、strongest GRU targets 比较。
- 2026-05-13 04:59 CST `modeldev_20260512_grutcn_rawtoktf_cont_H20_from_rawtok_e4_p1` e7 horizon/test MSE-schema eval 完成并读取 metric。checkpoint：`checkpoints/model-epoch=07-best_valid_loss=0.47.pth`，training `best_valid_loss=0.4716687`，eval log `logs/eval_horizontest_20260513_grutcn_rawtoktf_cont_H20_e7_b32_mse_p1.log`，average rollout loss `0.5630676746`；h50 `E_q=0.0853119630`、`E_v=0.3808232145`、`E_omega=0.3011252939`、`MSE_x=0.7802118972`；mean `E_q=0.0437197493`、`E_v=0.2160187981`、`E_omega=0.2426086313`；`MSE_1_to_F=0.3995643737`。相对 rawtoktf e4（h50 `E_q=0.0853260663`、`E_v=0.3809074430`、`E_omega=0.3011899105`、mean `E_q=0.0437284932`、`MSE_1_to_F=0.3996152170`）是小幅全面改善，但仍明显落后 strongest GRU targets（h50_q `0.0800042`、h50_v `0.353015`、h50_omega `0.260392`、mean_q `0.0420377`）。该 horizon/test 读取影响后续结构决策：rawtoktf 长训练方向有效但斜率很小，不能只靠继续训练追目标；下一步优先在 rawtoktf 基础上加入小权重 physics-informed regularization + slack/reliability gate，或升级更强 raw-token/长历史结构。Eval GPU watch `gpu_watch_eval_grutcn_rawtoktf_cont_H20` 已停止，artifacts 保留，无清理。
- 2026-05-13 05:05 CST 新增 physics-informed regularization + slack/reliability gate 训练 wiring，默认关闭，不改变已有训练/验证/测试行为。文件改动：`scripts/config.py` 新增 `physics_loss_weight`、`physics_kinematic_weight`、`physics_quat_norm_weight`、`physics_v_smooth_weight`、`physics_omega_smooth_weight`、`physics_reliability_scale`、`physics_slack_margin`；`scripts/dynamics_learning/lighting.py` 新增 train-only `physics_regularization`，主要约束预测 `p_{t+1}` 与 `p_t + 0.5*dt*(v_t+v_{t+1})` 的局部运动学一致性，并用 target 轨迹自身运动学残差作为 slack/reliability，数据不符合简单物理时自动降低惩罚；`scripts/train.py` 把 physics 配置写入 `train_summary.json`。local `py_compile`、`git diff --check` 通过；remote 同步并 py_compile 通过；local/remote hash：`config.py=63262b8cf5b79640b8ad8b7543acebf06e6d3215ea0e62f50eb031cb30834541`、`lighting.py=af55b5105dc5100e2e8e3fb5feea2526c0b868c38e37ddd2c022d82334fb9445`、`train.py=98760f8ac90446e8c1dd4f0476a9504963f10df0caebf9e554e29bb5636db655`。remote smoke `smoke_20260513_grutcn_rawtoktf_phys_H20_from_cont_e7_p1` 从 e7 checkpoint 加载成功，只训练 `raw_token*`，一批 train/valid finite：`train_loss_epoch=0.1463`、`train_objective_epoch=0.1463`、`train_physics_loss_epoch=1.0259e-06`、`train_physics_reliability_epoch=0.9910`、`valid_loss_epoch=0.0818`；未读取 horizon/test metric。结论：wiring 可运行，物理项量级很小，正式候选需用较大 `physics_loss_weight` 才能形成轻约束。
- 2026-05-13 05:06 CST 启动 active training `modeldev_20260513_grutcn_rawtoktf_physkin_H20_from_cont_e7_p1`。结构/协议：不改 GRUTCN forward，沿用 raw-history token Transformer side branch，只训练 `raw_token*`；从 `modeldev_20260512_grutcn_rawtoktf_cont_H20_from_rawtok_e4_p1/checkpoints/model-epoch=07-best_valid_loss=0.47.pth` 初始化；开启 train-only physics regularization：`physics_loss_weight=1000.0`、`physics_kinematic_weight=1.0`、`physics_quat_norm_weight=0.01`、`physics_v_smooth_weight=0.0`、`physics_omega_smooth_weight=0.0`、`physics_reliability_scale=10.0`、`physics_slack_margin=0.0`。训练配置：GRUTCN H20/F50，epochs `6`，batch `64`、accumulate `8`、effective batch `512`，`limit_train_batches=0.25`、`limit_val_batches=0.5`，`warmup_lr=5e-7`、`cosine_lr=1.5e-7`，`warmup_steps=50`、`cosine_steps=1200`，early stopping patience `2`、min_delta `1e-5`，WANDB disabled。tmux `modeldev_grutcn_rawtoktf_physkin_H20_from_cont_e7_p1`；GPU watch `modeldev_gpu_watch_grutcn_rawtoktf_physkin_H20`；log `resources/experiments/modeldev_20260513_grutcn_rawtoktf_physkin_H20_from_cont_e7_p1/logs/train_phase1.log`。启动检查：tmux/process/GPU watch healthy，GPU 约 `4702/8188 MiB`、util `30%`，train steps finite；horizon/test metric 未读取。Gate：参考 e7 `best_valid_loss=0.4716687` 与 e7 horizon/test 小幅全面 best。e0 `>0.47180` 或 physics objective 明显破坏 validation 即停止不 eval；e0 `0.47167-0.47180` 观察 e1/e2；e0 `<=0.47166` 健康。natural finish 或 validation best 后按协议读取 horizon/test，重点看 h50 `E_q/E_v/E_omega`、mean `E_q`、`MSE_1_to_F` 是否超过 e7。
- 2026-05-13 09:00 CST `modeldev_20260513_grutcn_rawtoktf_physkin_H20_from_cont_e7_p1` 自然跑满 `max_epochs=6`，`early_stopped=false`，`train_summary.json` 指向 e5 checkpoint `checkpoints/model-epoch=05-best_valid_loss=0.47.pth`。e0-e5 validation 连续刷新 rawtoktf best：`0.4716556 -> 0.4716390 -> 0.4716238 -> 0.4716125 -> 0.4716022 -> 0.4715962`，`best_valid_loss=0.4715962`；e5 train objective `0.2396219`，train physics loss `7.024e-06`，physics reliability `0.9758`。训练阶段 horizon/test metric 未读取，artifacts 保留，无 OOM/NaN/Traceback；已停止训练 GPU watch `modeldev_gpu_watch_grutcn_rawtoktf_physkin_H20`，启动 horizon/test MSE-schema eval tmux `eval_grutcn_rawtoktf_physkin_H20_e5_mse_p1` 与 eval GPU watch `gpu_watch_eval_grutcn_rawtoktf_physkin_H20`，eval log `logs/eval_horizontest_20260513_grutcn_rawtoktf_physkin_H20_e5_b32_mse_p1.log`，eval 启动检查 healthy，日志写入 `Seed set to 10`，`horizon_summary.json` / `horizon_metrics.csv` 尚未生成。判定：physics regularization 继续改善 validation，下一步等 eval 完成后读取 h50/mean `E_q/E_v/E_omega` 与 `MSE_1_to_F`，判断是否真正优于 rawtoktf cont e7。
- 2026-05-13 09:38 CST `modeldev_20260513_grutcn_rawtoktf_physkin_H20_from_cont_e7_p1` e5 horizon/test MSE-schema eval 完成并读取 metric。checkpoint：`checkpoints/model-epoch=05-best_valid_loss=0.47.pth`，training `best_valid_loss=0.4715962`，eval log `logs/eval_horizontest_20260513_grutcn_rawtoktf_physkin_H20_e5_b32_mse_p1.log`，average rollout loss `0.5630497932`；h50 `E_q=0.0853132015`、`E_v=0.3808130120`、`E_omega=0.3010985338`、`MSE_x=0.7801643574`；mean `E_q=0.0437205195`、`E_v=0.2160128946`、`E_omega=0.2425938835`；`MSE_1_to_F=0.3995425400`。相对 rawtoktf cont e7（h50 `E_q=0.0853119630`、`E_v=0.3808232145`、`E_omega=0.3011252939`、mean `E_q=0.0437197493`、`MSE_1_to_F=0.3995643737`）仅 `h50_v`、`h50_omega`、`MSE_1_to_F` 极小改善，`h50_q` 与 mean `E_q` 极小回退；仍明显落后 strongest GRU targets（h50_q `0.0800042`、h50_v `0.353015`、h50_omega `0.260392`、mean_q `0.0420377`）。结论：physics kinematic regularization 可降 validation 与 full-state MSE，但未突破最强 long-horizon baseline；该 horizon/test 读取已影响后续结构决策，下一步不能只继续当前小权重物理 loss，应转向更强 raw-token/长历史结构或重新审视 TCNLSTM true-anchor velocity partial win。eval GPU watch 已停止，artifacts 保留，无清理。
- 2026-05-13 11:05 CST 启动 history-expanded active training `modeldev_20260513_grutcn_rawtoktf_adapthist_H30_from_cont_e7_p1`。架构思路：`scripts/dynamics_learning/models/grutcn.py` 在 raw-token 分支上加入自适应长历史选择；主 `encoder`/anchor path 通过 `anchor_history_len=min(history_len,20)` 只看最近 H20，full H30 history 只进入新增 `raw_token_adaptive_*` multi-scale short/mid/full context 分支，避免跨 history 直接扰乱已校准 anchor。`scripts/train.py` 新增 checkpoint shape filter，加载旧 checkpoint 时跳过同名但 shape 不匹配参数，防止未来 H30/H50 间迁移崩溃。检查：local `python3 -m py_compile scripts/dynamics_learning/models/grutcn.py scripts/train.py scripts/config.py scripts/dynamics_learning/lighting.py scripts/eval.py` 与 `git diff --check` 通过；同步远程后 remote py_compile 通过；local/remote hash 一致：`grutcn.py=4fb3632d0c1eb9a41541576b062eb4818ea86b06e3ddbddcd01dd28648bc41bd`、`train.py=364c26c723e1904d450895ea3e3cac3172a65fc19b73dafb246fdac3c8d95cdf`。一次 `rsync` 目标少 `--relative` 导致 `grutcn.py/train.py` 误放到远程 repo 根目录，已只删除本次误放文件并重新按正确路径同步。Smoke：H30 p2 从 rawtoktf cont e7 H20 checkpoint 加载成功，missing 仅新增 `raw_token_adaptive_*`，无 unexpected/shape crash，`--trainable_parameter_patterns raw_token` 覆盖 `raw_token_adaptive_*`；H20 control smoke 同配置也通过，因此一批 loss 波动不作为 H30 失败依据；未读取 horizon/test metric。正式训练：从 `resources/experiments/modeldev_20260512_grutcn_rawtoktf_cont_H20_from_rawtok_e4_p1/checkpoints/model-epoch=07-best_valid_loss=0.47.pth` 初始化，H30/F50，epochs `6`，batch `64`、accumulate `8`、effective batch `512`，`limit_train_batches=0.25`、`limit_val_batches=0.5`，`warmup_lr=8e-7`、`cosine_lr=2e-7`，`warmup_steps=50`、`cosine_steps=1500`，early stopping patience `2`、min_delta `1e-5`，WANDB disabled，physics loss OFF，`--trainable_parameter_patterns raw_token`。tmux `modeldev_grutcn_rawtoktf_adapthist_H30_from_cont_e7_p1`；GPU watch `modeldev_gpu_watch_grutcn_rawtoktf_adapthist_H30`；log `resources/experiments/modeldev_20260513_grutcn_rawtoktf_adapthist_H30_from_cont_e7_p1/logs/train_phase1.log`。启动巡检：tmux/process/GPU watch healthy，GPU 约 `7527/8188 MiB`、util `40%`，train steps finite，trainable params `4.1M`，missing keys 仅新增 adaptive 分支；horizon/test metric 未读取。Gate：参考 rawtoktf cont e7 `best_valid_loss=0.4716687` 与 physkin e5 `0.4715962`；e0 `>0.47190` 或 q/v/omega/state_mse 同步明显回退则停止不 eval；e0 `0.47165-0.47190` 观察 e1/e2；e0 `<=0.47165` 健康；natural finish/validation best 后按协议读取 horizon/test MSE-schema，比较 rawtoktf cont e7、physkin e5 与 strongest GRU targets。
- 2026-05-12 用户新增后续候选思路：可尝试 physics-informed / physics-inspired loss，作为 rawtoktf 或后续 GRUTCN/TCNLSTM 的训练协议升级，不影响当前已启动的 rawtoktf e4 eval。公开参考：Saviolo/Li/Loianno 2022 `Physics-Inspired Temporal Learning of Quadrotor Dynamics for Accurate Model Predictive Trajectory Tracking` 的 PI-TCN 思路是在 temporal dynamics learning 训练中嵌入 physics constraints 以提升泛化；Serrano et al. 2024 `Physics-Informed Neural Network for Multirotor Slung Load Systems Modeling` 使用由一阶原理离散模型构造的 physics-based loss，并用 slack variables 允许神经预测与物理模型有小偏差。可落地方案：先做默认关闭的 train-only physics regularization，不改 evaluation metric；用可学习/可调 reliability gate 或 slack head 判断物理约束是否可靠，避免错误物理先验压坏数据拟合；候选物理项可从最稳的局部约束开始，例如 `p_{t+1}≈p_t+dt*v_t`、速度/角速度平滑或由控制输入诱导的 acceleration consistency、quaternion norm/小角速度一致性。若 rawtoktf horizon/test 有正信号，优先做 e4 continuation + 小权重 physics loss；若 horizon/test 回退，则把该思路作为下一结构候选，而不是直接覆盖当前模型。不得上传/泄漏数据；只用聚合指标和误差趋势决定是否启用。
- 2026-05-12 11:14 CST `modeldev_20260511_grutcn_attobserver_H20_from_anchor_e4_p1` e4 horizon/test-guided evaluation 完成并读取 metric 内容。checkpoint：`checkpoints/model-epoch=04-best_valid_loss=0.47.pth`，eval log `logs/eval_horizontest_20260512_grutcn_attobserver_H20_e4_b32.log`，average rollout loss `0.5632079840`，h50 `E_q=0.0853396507`、`E_v=0.3809912724`、`E_omega=0.3012224156`，mean `E_q=0.0437367480`、`E_v=0.2160949155`、`E_omega=0.2426517642`。结论：相对 GRUTCN anchor e4（h50 `E_q=0.0853667619`、`E_v=0.3810590338`、`E_omega=0.3012506581`、mean `E_q=0.0437535938`）和 velocity_observer e4（h50 `E_q=0.0853668251`、`E_v=0.3810359016`、`E_omega=0.3012500490`、mean `E_q=0.0437536468`）都是小幅正增益，但仍远未接近 strongest GRU targets（h50_q `0.0800042`、h50_v `0.353015`、h50_omega `0.260392`、mean_q `0.0420377`）。该结果用于后续结构决策：frozen output observer 方向有效但单一姿态分支表达力不足；下一步优先无代码改动地从 GRUTCN anchor e4 初始化、联合训练 `attitude_observer,velocity_observer`，测试两个 zero-init output observer 的叠加效果。已停止 eval GPU watch；artifacts 保留。
- 2026-05-12 11:06 CST `modeldev_20260511_grutcn_attobserver_H20_from_anchor_e4_p1` 训练自然结束并启动 horizon/test-guided evaluation。训练结果：`max_epochs=5`，`early_stopped=false`，validation 连续小幅下降，e4 best `best_valid_loss=0.4720434`，checkpoint `checkpoints/model-epoch=04-best_valid_loss=0.47.pth`，`train_summary.json` 指向该 e4 checkpoint；CSVLogger 本实验未写出 `valid_q/valid_p/valid_v/valid_omega` 分列，只能确认 total validation 与 train loss。训练阶段未读取 horizon/test metric；artifacts 保留。判定：best 优于 GRUTCN anchor e4 `0.4720816` 和 velocity_observer e4 `0.4720605`，虽然未达到旧冻结线 `<=0.47200`，但按新协议值得读取 horizon/test 判断长时域 q/omega 是否改善。已停止训练 GPU watch `modeldev_gpu_watch_grutcn_attobserver_H20`；启动 eval tmux `eval_grutcn_attobserver_H20_e4_p1`，GPU watch `gpu_watch_eval_grutcn_attobserver_H20`，eval log `logs/eval_horizontest_20260512_grutcn_attobserver_H20_e4_b32.log`，命令使用 `scripts/eval.py --eval_batch_size 32 --eval_horizons 1,10,25,50 --experiment_path resources/experiments/modeldev_20260511_grutcn_attobserver_H20_from_anchor_e4_p1`。启动巡检：tmux/process/GPU healthy，GPU 约 `729/8188 MiB`、util `34%`，`horizon_summary.json` 尚未生成，尚未读取 horizon/test metric 内容。完成后必须记录 checkpoint、average rollout loss、h50/mean 关键指标、是否用于结构决策和结论。
- 2026-05-11 22:35 CST 启动 active training `modeldev_20260511_grutcn_attobserver_H20_from_anchor_e4_p1`。结构/协议：`scripts/dynamics_learning/models/grutcn.py` 在 GRUTCN H20 anchor e4 上新增 checkpoint-safe `attitude_observer_*`，输入 `base_feature + context + history_context + projected_x_last + decoder_feature + dx_last + base_delta[:, 6:12]`，final projection zero-init，`attitude_observer_scale=0.003`，只在最终输出后写 `y[:, 6:12]`；不写 `y[:, 0:6]`，不改 `encoder`、`base_decoder`、`state_initializer` 或 GRU memory。失败的 motion/velocity observer 分支保留 key 兼容，本候选用 `--trainable_parameter_patterns attitude_observer` 冻结 anchor，只训练姿态/角速度 observer。动机：horizon/test 显示 TCNLSTM velocity-only 和 GRUTCN velocity_observer 都没有带来有效 long-horizon 改善，转向 GRUTCN anchor 上更直接针对 h50 quaternion/omega 的 frozen observer。
- 检查/同步：local `python -m py_compile scripts/dynamics_learning/models/grutcn.py scripts/train.py scripts/config.py scripts/eval.py` 与 `git diff --check` 通过；remote code hash 与 local 一致：`grutcn.py=9328db7e9e1c6349155bd56d982c23e5576d522c0dda44ab579319d4e91902c9`、`eval.py=aa8971b415136a53725df7cd19ee16e7daa6c3485f79dcd3870abf8656c17007`；remote py_compile 通过。一次错误多文件 `rsync` 把 `grutcn.py/eval.py` 放到远程 repo 根目录，已立即删除这两个本次误放文件，并重新同步到正确路径；`Prompt.md` / `MODEL_DEV_HANDOFF.md` 后续按本记录同步。
- remote smoke `smoke_20260511_grutcn_attobserver_H20_from_anchor_e4_p1` 从 `modeldev_20260510_grutcn_anchor_e7_ultralow_p2/checkpoints/model-epoch=04-best_valid_loss=0.47.pth` 加载成功，missing keys 为新增/保留兼容分支（含 `attitude_observer_*`），无 unexpected keys；只训练 `attitude_observer*` 参数；一批 train/valid finite：`train_loss_step=0.138`、`valid_loss_epoch=0.0817`，checkpoint、CSVLogger、`train_summary.json` 正常；只确认 `plots/testset` 存在，未读取 horizon/test metric 内容。
- 正式训练配置：从 `resources/experiments/modeldev_20260510_grutcn_anchor_e7_ultralow_p2/checkpoints/model-epoch=04-best_valid_loss=0.47.pth` 初始化，GRUTCN H20/F50，epochs `5`，batch `64`、accumulate `8`、effective batch `512`，`limit_train_batches=0.25`、`limit_val_batches=0.5`，`warmup_lr=8e-7`、`cosine_lr=2e-7`，`warmup_steps=50`、`cosine_steps=1000`，early stopping patience `2`、min_delta `2e-5`，WANDB disabled，`--trainable_parameter_patterns attitude_observer`。tmux `modeldev_grutcn_attobserver_H20_from_anchor_e4_p1`；GPU watch `modeldev_gpu_watch_grutcn_attobserver_H20`；log `resources/experiments/modeldev_20260511_grutcn_attobserver_H20_from_anchor_e4_p1/logs/train_phase1.log`；GPU watch log `logs/gpu_watch.log`。启动巡检：tmux/process/GPU watch healthy，GPU 约 `2509/8188 MiB`、util `37%`，epoch 0 train steps finite；`plots/testset` 目录存在但未读取 horizon/test metric 内容。gate：参考 GRUTCN anchor e4/velocity_observer e4。e0 `>0.4728` 停止不做 horizon/test；e0 `0.47210-0.47280` 观察 e1/e2；e0 `<=0.47205` 健康。训练自然结束或 validation best 出现后按新协议读取 horizon/test metric，优先看是否改善 h50 `E_q`、`E_omega`，同时不得让 h50 `E_v` 明显退化；读取后必须记录 checkpoint、关键指标、决策影响和结论。
- 2026-05-11 22:24 CST 尝试启动 `modeldev_20260511_tcnlstm_lagobserver_H10_from_attitude_e3_p1` e2 horizon/test evaluation，但只完成启动检查即中止，未读取 horizon/test metric 内容。checkpoint：`checkpoints/model-epoch=02-best_valid_loss=0.46.pth`；log：`logs/eval_horizontest_20260511_tcnlstm_lagobserver_H10_e2_b32.log`。失败原因不是模型指标差，而是当前 `scripts/dynamics_learning/models/tcnlstm.py` active forward 已切到 velocity residual，且包含更新后的兼容模块，导致旧 lagobserver checkpoint 在 strict load 下缺少 `velocity_residual_*` 等 key；即使用 non-strict load，也会用 velocity-residual forward 评估旧 lagobserver 权重，结果不代表原候选。结论：旧候选 horizon/test 只能在恢复匹配代码叙事/forward 后评估；本次 eval tmux 和 GPU watch 已停止，artifacts 保留。该事件用于后续决策：不要用当前 forward 读取旧 lagobserver horizon/test 来调参。
- 2026-05-11 协议变更：用户明确要求训练/开发阶段可以读取 horizon/test metric 来做候选挑选，并据此改进模型。后续不再把 horizon/test metric 限制为 locked audit 后的一次性 pass/fail；可以用 horizon/test 指标分析 `best_valid_loss` 未过线但 long-horizon 效果可能更好的候选。任何读取都必须记录实验 id、checkpoint、关键 horizon/test 指标、是否用于后续调参/结构决策，以及结论。历史记录中“未读取 horizon/test metric”的陈述仅描述当时协议下的事实。
- 2026-05-11 22:19 CST `modeldev_20260511_tcnlstm_velres_H10_from_attitude_e3_p1` e2 horizon/test evaluation 完成并读取 metric 内容。checkpoint：`checkpoints/model-epoch=02-best_valid_loss=0.46.pth`，eval log `logs/eval_horizontest_20260511_tcnlstm_velres_H10_e2_b32_retry2.log`，average rollout loss `0.5291858315`，h50 `E_q=0.0908154197`、`E_v=0.3454937450`、`E_omega=0.3339743749`，mean `E_q=0.0459003559`、`E_v=0.1858658490`、`E_omega=0.2568162155`。结论：相对 TCNLSTM attitude H10 e3 locked audit（h50 `E_q=0.0907420`、`E_v=0.3453549`、`E_omega=0.3335756`，mean `E_q=0.0458700`）没有改善，h50 velocity/q/omega 均略差；该结果用于后续决策，关闭 velocity-only residual 分支，不继续细调。eval GPU watch 已停止，artifacts 保留。
- 2026-05-11 22:04 CST 启动 active horizon/test evaluation `modeldev_20260511_tcnlstm_velres_H10_from_attitude_e3_p1`，checkpoint `checkpoints/model-epoch=02-best_valid_loss=0.46.pth`（实际 validation best，`best_valid_loss=0.4617194`），tmux `eval_tcnlstm_velres_H10_e2_p1`，GPU watch `gpu_watch_eval_tcnlstm_velres_H10`，log `logs/eval_horizontest_20260511_tcnlstm_velres_H10_e2_b32_retry2.log`。目的：按新协议验证 velocity-only TCNLSTM 是否存在 validation 未过线但 h50 velocity / long-horizon 指标更好的情况。启动巡检确认加载 e2 checkpoint，test windows `50904`，H10/F50，GPU 正在计算；截至记录时尚未生成 `horizon_summary.json`。同时修复 `scripts/eval.py` checkpoint 选择 wiring：原正则会把 `best_valid_loss=0.46.pth` 解析成 `0.46.` 导致 `ValueError`；现改为只捕获数字小数，并在 rounded loss 相同、无 `train_summary.json` 时用 checkpoint mtime 选择较新的 non-last checkpoint，避免手动停止实验误选 e0。local/remote `eval.py` hash `aa8971b415136a53725df7cd19ee16e7daa6c3485f79dcd3870abf8656c17007`，local/remote py_compile 通过。
- 2026-05-11 21:50 CST `modeldev_20260511_grutcn_velobserver_H20_from_anchor_e4_p1` horizon/test evaluation 完成并读取 metric 内容。checkpoint：`checkpoints/model-epoch=04-best_valid_loss=0.47.pth`，eval log `logs/eval_horizontest_20260511_grutcn_velobserver_H20_e4_b32.log`，average rollout loss `0.5632427931`，h50 `E_q=0.0853668251`、`E_v=0.3810359016`、`E_omega=0.3012500490`，mean `E_q=0.0437536468`、`E_v=0.2161035278`、`E_omega=0.2426603199`。结论：相对 GRUTCN anchor e4 几乎只带来 h50 velocity 微小改善（约 `0.381059 -> 0.381036`），未接近 strongest targets（h50_v `0.353015`、h50_q `0.0800042`、h50_omega `0.260392`、mean_q `0.0420377`）；该结果用于后续决策，判定 velocity_observer 冻结大 anchor 的表达力不足，不继续沿该小 observer 单独细调。已停止该 eval 的 GPU watch，artifacts 保留。
- 2026-05-11 21:22 CST 按新协议启动 horizon/test-guided evaluation：实验 `modeldev_20260511_grutcn_velobserver_H20_from_anchor_e4_p1`，checkpoint `checkpoints/model-epoch=04-best_valid_loss=0.47.pth`（`best_valid_loss=0.4720605`），tmux `eval_grutcn_velobserver_H20_e4_p1`，GPU watch `gpu_watch_eval_grutcn_velobserver_H20`，log `logs/eval_horizontest_20260511_grutcn_velobserver_H20_e4_b32.log`，命令为 `scripts/eval.py --dataset neurobemfullstate --predictor_type full_state --accelerator cuda --gpu_id 0 --eval_batch_size 32 --eval_horizons 1,10,25,50 --wandb_mode disabled --experiment_path resources/experiments/modeldev_20260511_grutcn_velobserver_H20_from_anchor_e4_p1`。目的：验证用户提出的“validation loss 未过冻结线但 horizon/test 效果可能更好”假设。启动巡检确认加载 e4 best checkpoint，test windows `50784`，H20/F50，GPU 正在计算；截至本次记录尚未生成 `horizon_summary.json` / `horizon_metrics.csv`，因此尚未读取 metric 内容。完成后必须记录关键 horizon/test 指标及是否影响下一步结构/调参决策。
- 2026-05-11 18:33 CST `modeldev_20260511_tcnlstm_velres_H10_from_attitude_e3_p1` 按 validation-only gate 停止。e0：`valid_loss_epoch=0.4618285`、`best_valid_loss=0.4618286`、`valid_q=0.0412260`、`valid_p=0.0322214`、`valid_v=0.1505020`、`valid_omega=0.2378793`。e1：`valid_loss_epoch=0.4618840`、best 未刷新、`valid_q=0.0412363`、`valid_p=0.0322240`、`valid_v=0.1504817`、`valid_omega=0.2379420`。e2：`valid_loss_epoch=0.4617192`、`best_valid_loss=0.4617194`、`valid_q=0.0412369`、`valid_p=0.0322030`、`valid_v=0.1503207`、`valid_omega=0.2379586`。判定：e2 total/v 改善但 `valid_q/valid_omega` 从 e0 到 e2 连续回退，触发预声明 gate；best 未达冻结线，不做 locked audit。执行：仅停止该实验训练 tmux `modeldev_tcnlstm_velres_H10_from_attitude_e3_p1`、GPU watch `modeldev_gpu_watch_tcnlstm_velres_H10` 和匹配实验路径的训练进程；artifacts 保留（e0/e1/e2 checkpoints、`last_model.pth`、CSV/logs），`train_summary.json` 因手动 gate 停止不存在。训练阶段未读取 horizon/test metric 内容，仅确认 `plots/testset` 目录存在。
- 2026-05-11 18:44 CST 启动 active training `modeldev_20260511_grutcn_velobserver_H20_from_anchor_e4_p1`。结构/协议：`scripts/dynamics_learning/models/grutcn.py` 在 GRUTCN H20 anchor e4 上新增 checkpoint-safe `velocity_observer_*`，输入 `base_feature + context + history_context + projected_x_last + dx_last + base_delta[:, 3:6]`，final projection zero-init、`velocity_observer_scale=0.003`，只在最终输出后写 `y[:, 3:6]`；不写 `y[:, 0:3]` 或 `y[:, 6:12]`。失败的 `motion_encoder/motion_fusion`、temporal refiner、dual/raw 分支保留 key 兼容；本候选训练协议使用 `--trainable_parameter_patterns velocity_observer` 冻结 anchor，只训练 observer，验证“共享参数漂移导致 q/omega 回退”的假设。`scripts/config.py`/`scripts/train.py` 新增该训练参数过滤 wiring，并把 trainable names 写入 `train_summary.json`；未使用外部 trick/web research，未读取 horizon/test metric 内容。
- 检查/同步：local `python -m py_compile scripts/dynamics_learning/models/grutcn.py scripts/train.py scripts/config.py` 和 `git diff --check -- scripts/dynamics_learning/models/grutcn.py scripts/train.py scripts/config.py` 通过；最终 local/remote hash 一致：`grutcn.py=fe649b2d138dceea90fb9107e983e784c9acd72a60dee24b7439e855132d824e`、`train.py=f73e558e6295910412d65284c02c2f53137224ac58ab3cfa94af342ae3ea716b`、`config.py=e4be9bdc3574b3ade4c896c1a58082b3041d28efc3e4ddd9ff22b86f5e48ead8`；一次错误 `rsync --relative` 生成的远程 `scripts/scripts` 已删除，正式训练文件 hash 已校正。remote smoke `smoke_20260511_grutcn_velobserver_H20_from_anchor_e4_p1` 从 `modeldev_20260510_grutcn_anchor_e7_ultralow_p2/checkpoints/model-epoch=04-best_valid_loss=0.47.pth` 加载成功，missing keys 为新增 `velocity_observer_*` 及保留兼容分支，无 unexpected keys；只训练 `velocity_observer*` 参数；一批 train/valid finite：`train_loss_step=0.144`、`valid_loss_epoch=0.0821`（1-batch smoke），checkpoint、CSVLogger、`train_summary.json` 正常；只确认 `plots/testset` 存在，未读取 horizon/test metric 内容。
- 正式训练配置：从 `resources/experiments/modeldev_20260510_grutcn_anchor_e7_ultralow_p2/checkpoints/model-epoch=04-best_valid_loss=0.47.pth` 初始化，GRUTCN H20/F50，epochs `5`，batch `64`、accumulate `8`、effective batch `512`，`limit_train_batches=0.25`、`limit_val_batches=0.5`，`warmup_lr=8e-7`、`cosine_lr=2e-7`，`warmup_steps=50`、`cosine_steps=1000`，early stopping patience `2`、min_delta `2e-5`，WANDB disabled，`--trainable_parameter_patterns velocity_observer`。tmux `modeldev_grutcn_velobserver_H20_from_anchor_e4_p1`；GPU watch `modeldev_gpu_watch_grutcn_velobserver_H20`；log `resources/experiments/modeldev_20260511_grutcn_velobserver_H20_from_anchor_e4_p1/logs/train_phase1.log`；GPU watch log `logs/gpu_watch.log`。启动巡检：tmux/process/GPU watch healthy，GPU 约 `2459/8188 MiB`、util `33%`，e0 train steps finite；`train_summary.json` 尚未生成；只确认 `plots/testset` 目录存在，未读取 horizon/test metric 内容。gate：参考 GRUTCN anchor e4 best `0.4720816`、motiondiff e2 `0.4723428`；e0 `>0.4728` 停止不 audit；e0 `0.47210-0.47280` 最多观察 e1/e2；e0 `<=0.47205` 健康。冻结候选优先 `best_valid_loss <=0.47180`，或 `<=0.47200` 且 `valid_q <=0.0390`、`valid_omega <=0.22935`、`valid_v <=0.16030`、`valid_p <=0.04330` 同步成立；`valid_q/valid_omega` 连续两轮回退或 `valid_v >0.1610` 即 validation-only failure，不读取 horizon/test metric 内容、不做 locked audit。
- 2026-05-11 20:42 CST `modeldev_20260511_grutcn_velobserver_H20_from_anchor_e4_p1` 自然训练结束（`max_epochs=5`，`early_stopped=false`），e4 刷新本实验 best：`best_valid_loss=0.4720605`（`valid_q=0.0389170`、`valid_p=0.0434236`、`valid_v=0.1604807`、`valid_omega=0.2292394`），best checkpoint 为 `checkpoints/model-epoch=04-best_valid_loss=0.47.pth`。判定：未达到冻结候选线（`best_valid_loss <=0.47200` / `<=0.47180`），不做 locked audit；训练阶段未读取 horizon/test metric 内容（仅确认 `plots/testset` 目录存在）。执行：已停止该实验剩余运行态（GPU watch tmux），artifacts 保留。
- 2026-05-11 17:11 CST 启动 active training `modeldev_20260511_tcnlstm_velres_H10_from_attitude_e3_p1`。结构/协议：`scripts/dynamics_learning/models/tcnlstm.py` 在 H10 attitude e3 anchor 上新增 checkpoint-safe `velocity_residual_*` 分支，final projection zero-init、`velocity_residual_scale=0.005`，只写 `y[:, 3:6]` velocity slice；不写 `y[:, 6:12]`，不改 `encoder`、TCN-compatible `decoder`、`base_feature = anchor_seq[:, -1, :]`、`state_initializer` 或已训练 attitude path。动机：近期失败显示 stateinit/cellinit/H20/output-space attitude/coupled/consolidation 都容易让 q/omega 回退；velocity-only 分支利用 attitude H10 e3 locked audit 的 h50 velocity partial win，尝试吸收 actuator lag / aero drag / hidden disturbance 的慢变量速度误差，同时保护 q/omega。
- 检查/同步：local `python -m py_compile scripts/dynamics_learning/models/tcnlstm.py`、`git diff --check -- scripts/dynamics_learning/models/tcnlstm.py` 通过；local/remote `tcnlstm.py` hash 均为 `94ad150e9a80eee0ab16fe1b5be3514b93dd9970d18d7273de5f978e79027c10`；remote `python3 -m py_compile` 通过。remote smoke `smoke_20260511_tcnlstm_velres_H10_from_attitudee3_p1` 从 H10 attitude e3 checkpoint 加载成功，missing keys 为新增/保留兼容分支（含 `velocity_residual_*`），无 unexpected keys；一批 train/valid finite：`train_loss_step=0.181`、`valid_loss_epoch=0.563`（1-batch smoke），checkpoint、CSVLogger、`train_summary.json` 正常；只确认 `plots/testset` 目录存在，未读取 horizon/test metric 内容。
- 正式训练配置：从 `resources/experiments/modeldev_20260510_tcnlstm_attitude_H10_from_trueanchor_e0_p1/checkpoints/model-epoch=03-best_valid_loss=0.46.pth` 初始化，TCNLSTM H10/F50，epochs `5`，batch `64`、accumulate `8`、effective batch `512`，`limit_train_batches=0.25`、`limit_val_batches=0.5`，`warmup_lr=8e-7`、`cosine_lr=2e-7`、`warmup_steps=50`、`cosine_steps=1000`，early stopping patience `2`、min_delta `2e-5`，WANDB disabled。tmux `modeldev_tcnlstm_velres_H10_from_attitude_e3_p1`；GPU watch `modeldev_gpu_watch_tcnlstm_velres_H10`；log `resources/experiments/modeldev_20260511_tcnlstm_velres_H10_from_attitude_e3_p1/logs/train_phase1.log`；GPU watch log `logs/gpu_watch.log`。启动巡检：tmux/process/GPU watch healthy，GPU 约 `2053/8188 MiB`、util `37-40%`，e0 train steps finite；只确认 `plots/testset` 目录存在，未读取 horizon/test metric 内容。gate：参考 H10 attitude e3 best `0.4615005`；e0 `>0.46185` 或 e0 同时 `valid_q >0.04123` 且 `valid_omega >0.23785` 则停止不 audit；e0 `0.46155-0.46185` 且未触发 q/omega 保护线则最多观察 e1/e2；e0 `<=0.46150` 算健康；冻结候选优先 `best_valid_loss <=0.46130`，或 `<=0.46145` 且 `valid_q <0.04118`、`valid_omega <0.23746`、`valid_v <=0.15030` 同步成立；q/omega 连续两轮回退即 validation-only failure，不读取 horizon/test metric 内容、不做 locked audit；locked audit 只能在 natural finish/`train_summary.json` 指向 validation-selected best checkpoint 后启动。
- 2026-05-11 14:30 CST 启动 active training `modeldev_20260511_tcnlstm_consolidation_H10_from_attitude_e3_p1`。结构/协议：`scripts/dynamics_learning/models/tcnlstm.py` 将刚失败的 `coupled_residual_*` 从 forward 旁路，只保留模块/key 兼容；`attitude_output_residual_v2`、`attitude_fine_*`、`decoder_state_residual*`、`gru_context_bridge*`、`lag_context*`、`long_delta*` 也不参与 forward；当前 forward 回到 H10 attitude e3 的 TCN anchor + context_delta + attitude_delta 路线做 ultralow consolidation。不改 `encoder`、TCN-compatible `decoder`、`base_feature = anchor_seq[:, -1, :]`、`state_initializer`。local py_compile、`git diff --check` 通过；local/remote `tcnlstm.py` hash 均为 `300bfb2af70240924a8c9d66d3f199f2660c31a17a15c7e5ddfa1e0ade93674d`；remote py_compile 通过。remote smoke `smoke_20260511_tcnlstm_consolidation_H10_from_attitudee3_p1` 从 H10 attitude e3 checkpoint 加载成功，missing keys 为不参与 forward 的兼容分支（含 `coupled_residual_*`），无 unexpected keys；一批 train/valid finite：`train_loss_step=0.176`、`valid_loss_epoch=0.1178`，checkpoint、CSVLogger、`train_summary.json` 正常；只确认 `plots/testset` 目录存在，未读取 horizon/test metric 内容。
- 正式训练配置：从 `resources/experiments/modeldev_20260510_tcnlstm_attitude_H10_from_trueanchor_e0_p1/checkpoints/model-epoch=03-best_valid_loss=0.46.pth` 初始化，TCNLSTM H10/F50，epochs `5`，batch `64`、accumulate `8`、effective batch `512`，`limit_train_batches=0.25`、`limit_val_batches=0.5`，`warmup_lr=8e-7`、`cosine_lr=2e-7`、`warmup_steps=50`、`cosine_steps=1000`，early stopping patience `2`、min_delta `2e-5`，WANDB disabled。tmux `modeldev_tcnlstm_consolidation_H10_from_attitude_e3_p1`；GPU watch `modeldev_gpu_watch_tcnlstm_consolidation_H10`；log `resources/experiments/modeldev_20260511_tcnlstm_consolidation_H10_from_attitude_e3_p1/logs/train_phase1.log`。启动巡检显示 tmux/process/GPU watch healthy，日志实时写入，GPU 约 `1979/8188 MiB`、util `30%`，e0 train steps finite；只确认 `plots/testset` 目录存在，未读取 horizon/test metric 内容。gate：参考 H10 attitude e3 best `0.4615005`；e0 `>0.46185` 停止不 audit；e0 `0.46155-0.46185` 最多观察 e1/e2；e0 `<=0.46150` 健康；冻结候选优先 `best_valid_loss <=0.46130`，或 `<=0.46145` 且 `valid_q <0.04118`、`valid_omega <0.23746`、`valid_v <=0.15030` 同步成立；q/omega 连续两轮回退即 validation-only failure，不读取 horizon/test metric 内容、不做 locked audit；locked audit 只能在 natural finish/`train_summary.json` 指向 validation-selected best checkpoint 后启动。
- 2026-05-11 16:02 CST consolidation H10 e2 validation 完成并触发 “q/omega 连续两轮回退” gate，训练已 early stop 结束。e0：`train_loss_epoch=0.3324556`、`valid_loss_epoch=0.4616246`、`best_valid_loss=0.4616245`、`valid_q=0.0412200`、`valid_p=0.0322120`、`valid_v=0.1503584`、`valid_omega=0.2378343`。e1：`train_loss_epoch=0.3321951`、`valid_loss_epoch=0.4617252`，best 未刷新；`valid_q=0.0412524`、`valid_p=0.0321666`、`valid_v=0.1502967`、`valid_omega=0.2380092`。e2：`valid_loss_epoch=0.4617007`，best 仍为 e0；`valid_q=0.0412584`、`valid_p=0.0321514`、`valid_v=0.1502234`、`valid_omega=0.2380675`。判定：q/omega 从 e0->e1->e2 单调回退，触发 validation-only failure；best 未达冻结线，不冻结、不 locked audit。`train_summary.json`：`early_stopped=True`、`stopped_epoch=3`、`best_model_path` 指向 e0 checkpoint。执行：已停止该实验运行态（GPU watch tmux 已关闭；训练进程已退出），artifacts 保留。训练阶段未读取 horizon/test metric 内容，仅确认 `plots/testset` 目录存在。
- 2026-05-11 14:18 CST `modeldev_20260511_tcnlstm_coupledlowrank_H10_from_attitude_e3_p1` 按 validation-only gate 失败停止；当前无 active training，核心 automation 必须继续推进下一候选。e0 `train_loss_epoch=0.3328158`、`valid_loss_epoch=0.4621493`、`best_valid_loss=0.4621493`，`valid_q=0.0412490`、`valid_p=0.0322318`、`valid_v=0.1506851`、`valid_omega=0.2379835`；超过预声明 e0 stop line `0.4620`，且不满足冻结候选同步健康条件。决策：停止该实验、不做 locked audit、不读取 horizon/test metric 内容（仅确认 `plots/testset` 目录存在）；已仅停止训练 tmux `modeldev_tcnlstm_coupledlowrank_H10_from_attitude_e3_p1`、对应 GPU watch `modeldev_gpu_watch_tcnlstm_coupledlowrank_H10` 和匹配该实验路径的训练进程；artifacts 保留（`model-epoch=00-best_valid_loss=0.46.pth`、`last_model.pth`、CSV/logs），`train_summary.json` 因手动 gate 停止不存在；GPU 回到约 `504/8188 MiB`、util `2%`。
- `coupledlowrank` 结构记录：`scripts/dynamics_learning/models/tcnlstm.py` 基于 H10 attitude e3 anchor 新增 checkpoint-safe `coupled_residual_*` low-rank latent residual，输入 `attitude_input + base_delta`，以 `coeff -> basis -> output_size` 方式在全输出空间学习耦合修正；`coupled_residual_basis` zero-init，`coupled_residual_scale=0.01`，初始为 no-op。刚失败的 `attitude_output_residual_v2` forward 路径已旁路但模块/key 保留；不改 `encoder`、TCN `decoder` anchor、`base_feature = anchor_seq[:, -1, :]` 或 `state_initializer`。本地 py_compile、`git diff --check` 通过；local/remote `tcnlstm.py` hash 均为 `160804053c79e04f33512005880caece5370651c2dd9d7aead4a56df2958d434`；remote py_compile 通过。remote smoke `smoke_20260511_tcnlstm_coupledlowrank_H10_from_attitudee3_p1` 从 H10 attitude e3 checkpoint 加载，missing keys 为新增/保留兼容分支（含 `coupled_residual_*`），无 unexpected keys；一批 train/valid finite：`train_loss_step=0.180`、`valid_loss_epoch=0.676`，checkpoint、CSVLogger、`train_summary.json` 正常；只确认 `plots/testset` 路径存在，未读取 horizon/test metric 内容。
- 2026-05-11 13:07 CST 自动化职责纠偏：`AGENTS.md` 第 12/13 条明确核心任务是持续模型冲刺，直到至少拿到冻结候选 locked audit 胜利，并采用“主会话总控 + 分支 agent 执行/分析”的模式；单个 validation-only failure 只结束该实验分支，不应结束总控 automation。此前把 `monitor-grutcn-h50-history-sweep` 当作单实验 watcher 删除是错误处理；已恢复 heartbeat automation `automation`（名称 `继续模型冲刺总控`，每 30 分钟），在无 active training 时继续推进下一候选，在有 active training 时巡检该实验，失败后只停止对应实验运行态并继续下一轮。horizon/test metric 内容未读取。
- 2026-05-11 12:55 CST `modeldev_20260511_tcnlstm_attoutv2_H10_from_attitude_e3_p1` 按 validation-only gate 失败停止；当前无 active training。e0 `valid_loss_epoch=0.4621532` / `best_valid_loss=0.4621533`，`valid_q=0.0412496`、`valid_p=0.0322328`、`valid_v=0.1506867`、`valid_omega=0.2379841`，超过预声明 e0 stop line `0.4620`；e1 小幅到 `valid_loss_epoch=0.4621446` / `best_valid_loss=0.4621447`，`valid_q=0.0412693`、`valid_p=0.0322439`、`valid_v=0.1505715`、`valid_omega=0.2380598`，仍高于停止线且 q/omega 较 e0 回退，不满足冻结候选线。决策：停止该实验、不做 locked audit、不读取 horizon/test metric 内容（仅确认 `plots/testset` 路径存在）；已仅停止训练 tmux `modeldev_tcnlstm_attoutv2_H10_from_attitude_e3_p1`、匹配该实验路径的训练进程和 GPU watch `modeldev_gpu_watch_tcnlstm_attoutv2_H10`；artifacts 保留（`model-epoch=00...pth`、`model-epoch=01...pth`、`last_model.pth`），`train_summary.json` 因手动 gate 停止不存在；GPU 回到约 `502/8188 MiB`、util `3%`。
- 2026-05-11 11:52 CST 启动 active training `modeldev_20260511_tcnlstm_attoutv2_H10_from_attitude_e3_p1`。关键判断：`gru_context_bridge` 不属于 `TCNLSTM attitude H10 e3` checkpoint 的已训练路径；attitude H10 e3 于 2026-05-10 23:12 启动，只包含 attitude attention/correction，`gru_context_bridge` 是 2026-05-11 08:34 的失败 grubridge 候选新增，因此新候选 forward 已旁路 `gru_context_bridge`，只保留模块/key 兼容。代码文件 `scripts/dynamics_learning/models/tcnlstm.py` 新增 checkpoint-safe `attitude_output_residual_v2`：在最终 `y` 形成后，用 `attitude_input + y[:, 6:12]` 生成 output-space residual，只加到 `y[:, 6:12]`；final projection zero-init，`attitude_output_residual_scale=0.005`；不改 `encoder`、TCN `decoder` anchor、`base_feature = anchor_seq[:, -1, :]` 或 `state_initializer`。未使用外部 trick/web research，未读取 horizon/test metric 内容。
- 检查/同步：local `python -m py_compile scripts/dynamics_learning/models/tcnlstm.py` 和 `git diff --check -- scripts/dynamics_learning/models/tcnlstm.py` 通过；local/remote `tcnlstm.py` hash 均为 `bc7e5b7e8753a67109c08272ce027cd630e10cb8f562706c26d8265e5d5dcdaf`；remote `python3 -m py_compile` 通过。remote one-batch smoke `smoke_20260511_tcnlstm_attoutv2_H10_from_attitudee3_p1` 在 tmux 中从 `modeldev_20260510_tcnlstm_attitude_H10_from_trueanchor_e0_p1/checkpoints/model-epoch=03-best_valid_loss=0.46.pth` 加载成功；missing keys 为新增/保留兼容分支（`attitude_output_residual*`、`gru_context*`/`gru_context_bridge*`、`decoder_state_residual*`、`attitude_fine*`、`long_*`、`lag_*`），无 unexpected keys；一批 train/valid finite：`train_loss_step=0.181`、`valid_loss_epoch=0.118`，checkpoint、CSVLogger、`train_summary.json` 正常生成；只确认 `plots/testset` 路径存在，未读取 horizon/test metric 内容。
- 正式训练配置：从 `resources/experiments/modeldev_20260510_tcnlstm_attitude_H10_from_trueanchor_e0_p1/checkpoints/model-epoch=03-best_valid_loss=0.46.pth` 初始化，TCNLSTM H10/F50，epochs `5`，batch `64`、accumulate `8`、effective batch `512`，`limit_train_batches=0.25`、`limit_val_batches=0.5`，`warmup_lr=1.5e-6`、`cosine_lr=4e-7`、`warmup_steps=50`、`cosine_steps=1000`，early stopping patience `2`、min_delta `2e-5`，WANDB disabled。第一次启动发现 `conda run` 缓冲 stdout 导致实时日志为空，已仅停止该新实验训练 tmux、匹配实验路径进程和对应 GPU watch，不清理 artifacts；随后用同一实验 id 加 `conda run --no-capture-output` 重启。tmux `modeldev_tcnlstm_attoutv2_H10_from_attitude_e3_p1`；GPU watch `modeldev_gpu_watch_tcnlstm_attoutv2_H10`；log `resources/experiments/modeldev_20260511_tcnlstm_attoutv2_H10_from_attitude_e3_p1/logs/train_phase1.log`；重启巡检显示 tmux/process/GPU watch healthy，日志实时写入，GPU 约 `2041/8188 MiB`、util `29%`，e0 train steps finite。automation `monitor-grutcn-h50-history-sweep` 已继承并更新为 `继续巡检 TCNLSTM attoutv2 H10`，每 30 分钟巡检。gate：参考 H10 attitude best `0.4615005`；e0 `>0.4620` 停止不 audit；e0 `0.4616-0.4620` 最多观察 e1/e2；e0 `<=0.46155` 算健康；冻结候选优先 `best_valid_loss <=0.4613`，或 `<=0.46145` 且 `valid_q <0.04118`、`valid_omega <0.23746`、`valid_v <=0.1505` 同步成立；q/omega 连续两轮回退即 validation-only failure，不读取 horizon/test metric 内容、不做 locked audit。
- 上一状态：此前无 active training；最近实验 `modeldev_20260511_grutcn_motiondiff_H20_from_anchor_e4_p1` 已于 2026-05-11 10:55 CST 因 validation-only gate 失败停止；未做 locked audit，horizon/test metric 内容未读取，artifacts 保留；训练 tmux `modeldev_grutcn_motiondiff_H20_from_anchor_e4_p1`、匹配该实验路径的训练进程和 GPU watch `modeldev_gpu_watch_grutcn_motiondiff_H20` 已停止，GPU 回到约 `495/8188 MiB`、util `0%`。关键结果：e0 `valid_loss_epoch=0.4725016`、e1 `0.4726623`、e2 刷新本实验 best 到 `valid_loss_epoch=0.4723428` / `best_valid_loss=0.4723428`，e2 submetrics 为 `valid_q=0.0389410`、`valid_p=0.0435035`、`valid_v=0.1606968`、`valid_omega=0.2292014`；e2 虽刷新 best 且 q/omega 达标，但未达到冻结候选线 `best_valid_loss <=0.4718`，也不满足 `<=0.4720` 且 q/omega/v/p 同步健康，因此按预声明 `e2 未达到冻结线` 处理为 validation-only failure。
- 2026-05-11 10:15 CST 用户要求将自动巡检节奏从 5 分钟调整为 20 分钟；普通巡检不写流水账，状态变化、失败、冻结候选、audit、异常或协议/代码变更才写入长期记忆。
- 2026-05-11 09:10 CST 新候选 `GRUTCN motion-diff encoder fusion`：`scripts/dynamics_learning/models/grutcn.py` 新增 checkpoint-safe `motion_encoder`，输入 `[x, dx]`（`dx_t=x_t-x_{t-1}`）作为 actuator lag / aero lag / filter-state 的差分观测流；通过 `motion_fusion` 在 encoder-side zero-init 融合到 `enc_seq`，再进入 temporal refiner 与 `context_gru`。该分支不直接写 `y`、不改 decoder state、不读取 horizon/test；保留 GRUTCN 现有 TCN base decoder、temporal refiner、dual context、raw history latent fusion 叙事。local/remote `grutcn.py` hash 均为 `7897bb1d60f6a8d8f696418ebfbcd5900077440921cc89e6b37bf674d58e5b9d`；local py_compile、`git diff --check`、remote py_compile 通过。
- remote one-batch smoke `smoke_20260511_grutcn_motiondiff_H20_from_anchor_e4_p1` 从 `modeldev_20260510_grutcn_anchor_e7_ultralow_p2/checkpoints/model-epoch=04-best_valid_loss=0.47.pth` 初始化成功；missing keys 仅新增 motion 分支及既有 refiner/dual/raw 分支，无 unexpected keys；`train_loss_step=0.119`、`valid_loss_epoch=0.0772`，checkpoint 与 `train_summary.json` 正常生成；horizon/test metric 内容未读取，只确认 `plots/testset` 路径存在。
- motiondiff H20 配置：从 `resources/experiments/modeldev_20260510_grutcn_anchor_e7_ultralow_p2/checkpoints/model-epoch=04-best_valid_loss=0.47.pth` 初始化，GRUTCN H20/F50，epochs `5`，batch `64`、accumulate `8`、effective batch `512`，`limit_train_batches=0.25`、`limit_val_batches=0.5`，`warmup_lr=1.5e-6`、`cosine_lr=4e-7`、`warmup_steps=50`、`cosine_steps=1000`，early stopping patience `2`、min_delta `2e-5`，WANDB disabled。
- motiondiff H20 gate：参考 GRUTCN anchor e4 best `0.4720816`、anchor-noise e5 `0.4722003`、dual/raw e2 `0.4721887`。e0 `>0.4732` 判定 motion-diff fusion 破坏 GRUTCN 校准，停止不 audit；e0 `0.4722-0.4732` 最多给 e1/e2，看 `valid_q`、`valid_omega`、`valid_v`、`valid_p` 是否同步健康；e0 `<=0.4721` 算健康。冻结候选优先要求 `best_valid_loss <=0.4718`；或 `<=0.4720` 且 `valid_q <=0.0390`、`valid_omega <=0.22935`、`valid_v <=0.1606`、`valid_p <=0.04330` 同步成立。若 q/omega 连续两轮回退或 `valid_v >0.1612`，不读取 horizon/test metric 内容、不做 locked audit；locked audit 只能在 natural finish/`train_summary.json` 指向 validation-selected best checkpoint 后启动。
- 最近失败实验 `modeldev_20260511_tcnlstm_grubridge_H10_from_attitude_e3_p1` 已于 2026-05-11 09:03 CST 因 validation-only gate 失败停止；无 locked audit，horizon/test metric 内容未读取，artifacts 保留；训练 tmux `modeldev_tcnlstm_grubridge_H10_from_attitude_e3_p1`、匹配该实验路径的训练进程和 GPU watch `modeldev_gpu_watch_tcnlstm_grubridge_H10` 已停止，GPU 回到约 `495/8188 MiB`、util `0%`。
- 2026-05-11 08:25 CST 只读确认远程 `gpu4060` 无相关 `modeldev_`/`audit_` tmux、无 `scripts/train.py`/`scripts/eval.py` 进程，GPU 约 `495/8188 MiB`、util `2%`。结构复盘结论：H10 attitude e3 仍是唯一可靠 anchor；H20/长历史旁路、decoder state/cell、直接 attitude/output tiny residual、lag-observer head 注入都没有形成冻结候选，常见模式是 total loss 接近但 q/omega 校准脆弱。下一候选应避免继续堆叠 tiny adapter，优先考虑更结构性的 checkpoint-safe 改法，且仍只用 train/validation/架构信号。
- 2026-05-11 08:34 CST 启动 `TCNLSTM GRU context bridge` 候选：`scripts/dynamics_learning/models/tcnlstm.py` 新增 checkpoint-safe `gru_context` + `gru_context_bridge`，借 GRUTCN recurrent summary 在内部 attention context 上做 zero-init residual 重组；不直接写 `y`、不扰动 LSTM `h0/c0`、不改变 TCN anchor `encoder`/`decoder`/`base_feature = anchor_seq[:, -1, :]`。刚失败的 lagobserver head 注入已从 forward 停用，相关模块仅保留以兼容 checkpoint key；failed long/attfine/state residual 仍不参与 forward。未使用外部 web research 或 horizon/test 信息。
- 检查/同步：local py_compile、`git diff --check` 通过；local/remote `tcnlstm.py` hash 均为 `c6c8d06dcf5ee9cb26a4349d6157205c616a0f6f798e29ce42d66032bb30073f`；remote py_compile 通过。remote one-batch smoke `smoke_20260511_tcnlstm_grubridge_H10_from_attitudee3_p1` 从 H10 attitude e3 checkpoint 加载，missing keys 为新增/保留的 `gru_context*`、`gru_context_bridge*`、`lag_*`、`decoder_state_residual*`、`attitude_fine*`、`long_history*`、`long_delta*`，无 unexpected keys；一批 train/valid finite：`train_loss_step=0.177`、`valid_loss_epoch=0.118`，`train_summary.json` 正常生成；horizon/test metric 内容未读取，只确认 `plots/testset` 目录存在。
- grubridge H10 配置：从 `resources/experiments/modeldev_20260510_tcnlstm_attitude_H10_from_trueanchor_e0_p1/checkpoints/model-epoch=03-best_valid_loss=0.46.pth` 初始化，H10/F50，epochs `5`，batch `64`、accumulate `8`、effective batch `512`，`limit_train_batches=0.25`、`limit_val_batches=0.5`，`warmup_lr=1.5e-6`、`cosine_lr=4e-7`、`warmup_steps=50`、`cosine_steps=1000`，early stopping patience `2`、min_delta `2e-5`，WANDB disabled。
- 2026-05-11 09:03 CST grubridge e0 validation-only 失败：`train_loss_epoch=0.3323885`，`valid_loss_epoch=0.4620850`，`best_valid_loss=0.4620851`，`valid_q=0.0412587`，`valid_p=0.0322910`，`valid_v=0.1505874`，`valid_omega=0.2379481`。e0 超过预声明 stop line `0.4620`，判定 GRU context bridge 破坏 H10 anchor 校准，停止不 audit；horizon/test metric 内容未读取，只确认 `plots/testset` 目录存在；checkpoint `model-epoch=00-best_valid_loss=0.46.pth` 和 `last_model.pth` 保留。下一步只基于 train/validation/架构信号复盘，避免继续堆叠 latent/output tiny adapter，优先考虑更结构性的 checkpoint-safe TCNLSTM/GRUTCN 改法。
- 2026-05-10 20:58 CST 启动 TCNLSTM true-anchor screening：从 TCN H10 best checkpoint 初始化，H10/F50，batch `64`、accumulate `8`、effective batch `512`，epochs `6`，`warmup_lr=1e-5`、`cosine_lr=3e-6`。tmux：`modeldev_tcnlstm_trueanchor_refine_H10_from_tcnH10_p1`；GPU watch：`modeldev_gpu_watch_tcnlstm_trueanchor_H10`；train log：`resources/experiments/modeldev_20260510_tcnlstm_trueanchor_refine_H10_from_tcnH10_p1/logs/train_phase1.log`。
- 2026-05-10 21:18 CST TCNLSTM true-anchor e0 validation 完成且健康：`train_loss_epoch=0.3388692`，`valid_loss_epoch=0.4623863`，`best_valid_loss=0.4623865`，`valid_q=0.0414418`，`valid_p=0.0322573`，`valid_v=0.1502742`，`valid_omega=0.2384134`。该结果低于 e0 停止线 `0.58`、健康线 `0.55`，也低于 TCN H10 validation baseline `0.533292`；checkpoint `model-epoch=00-best_valid_loss=0.46.pth` 已产生。
- 2026-05-10 21:41 CST TCNLSTM true-anchor e1 validation 完成：`train_loss_epoch=0.3363273`，`valid_loss_epoch=0.4637655`，`best_valid_loss=0.4623865`，`valid_q=0.0413985`，`valid_p=0.0323606`，`valid_v=0.1515890`，`valid_omega=0.2384175`。e1 未刷新 e0 best，但仍显著低于 TCN H10 validation baseline；训练已进入 e2，继续等 e2 验证作为是否冻结/继续的关键窗口。当前不 audit，horizon/test metric 内容未读取。
- 2026-05-10 22:02 CST TCNLSTM true-anchor e2 validation 完成：`train_loss_epoch=0.3350681`，`valid_loss_epoch=0.4640823`，`best_valid_loss=0.4623865`，`valid_q=0.0413460`，`valid_p=0.0324449`，`valid_v=0.1518466`，`valid_omega=0.2384447`。e2 仍远低于 TCN H10 validation baseline `0.533292`，但未刷新 e0 best 且没有形成持续下降；训练已进入 e3，继续观察自然收敛/early stopping，不冻结、不 audit，horizon/test metric 内容未读取。
- 2026-05-10 22:50 CST TCNLSTM true-anchor 训练自然 early stop：e3 `valid_loss_epoch=0.4634408`，`best_valid_loss=0.4623865`，`train_summary.json` 显示 `early_stopped=True`、`stopped_epoch=4`。冻结 validation-selected checkpoint `checkpoints/model-epoch=00-best_valid_loss=0.46.pth` 作为 locked audit 候选；训练阶段 horizon/test metric 内容仍未读取，实验目录未发现 horizon/test 文件。
- 2026-05-10 22:54 CST 已启动冻结候选 locked audit：audit id `lockaudit_20260510_tcnlstm_trueanchor_H10_e0_p1`，tmux `audit_tcnlstm_trueanchor_H10_e0_p1`，日志 `resources/experiments/modeldev_20260510_tcnlstm_trueanchor_refine_H10_from_tcnH10_p1/logs/eval_lockaudit_20260510_tcnlstm_trueanchor_H10_e0_p1_b32.log`。命令使用 `scripts/eval.py --dataset neurobemfullstate --predictor_type full_state --accelerator cuda --gpu_id 0 --eval_batch_size 32 --eval_horizons 1,10,25,50 --wandb_mode disabled --experiment_path resources/experiments/modeldev_20260510_tcnlstm_trueanchor_refine_H10_from_tcnH10_p1`；启动检查显示 tmux/进程 alive、日志已到 `Seed set to 10`，horizon/test metric 内容仍未读取。
- 2026-05-10 23:04 CST locked audit 完成：audit id `lockaudit_20260510_tcnlstm_trueanchor_H10_e0_p1`，batch `32`，无 OOM retry，average rollout loss `0.5286130309`。h50：`E_q=0.0909455538`、`E_v=0.3444223371`、`E_omega=0.3350359351`；mean：`E_q=0.0458820136`、`E_v=0.1851966663`、`E_omega=0.2572232571`。结论：partial locked win，`h50_E_v` 同时优于 `gru_H20=0.353015` 和 `tcn_H10=0.347036`，但 h50 quaternion、h50 omega、mean quaternion 未赢；不是全面 winner。horizon/test metric 内容只在 locked audit 完成后读取；artifacts 保留，未清理；audit GPU watch `modeldev_gpu_watch_tcnlstm_audit_H10` 已停止。
- 2026-05-10 20:48 CST 已完成保守 resume64 确认：从原实验 `last_model.pth` resume 到 epoch 4，micro batch `64`、accumulate `8`、effective batch `512`；训练自然到 `max_epochs=6` 结束，未再出现整机重启或普通 PyTorch OOM。

实验：

- `modeldev_20260510_grutcn_dualraw_H20_resume64_from_last_p1`

远程信息：

- path：`resources/experiments/modeldev_20260510_grutcn_dualraw_H20_resume64_from_last_p1`
- tmux：`modeldev_grutcn_dualraw_H20_resume64_from_last_p1`
- train log：`logs/train_resume64.log`
- resume checkpoint：`resources/experiments/modeldev_20260510_grutcn_dualraw_H20_from_ultralow_e4_p1/checkpoints/last_model.pth`
- GPU watch tmux：`modeldev_gpu_watch_dualraw_resume64_H20`
- GPU watch log：`logs/gpu_watch.log`

配置：

- GRUTCN H20/F50
- epochs `6`
- batch `64`
- accumulate `8`
- effective batch `512`
- `limit_train_batches=0.25`
- `limit_val_batches=0.5`
- seed `10`
- `warmup_lr=4e-6`
- `cosine_lr=1e-6`
- `warmup_steps=50`
- `cosine_steps=1000`
- early stopping patience `3`
- min_delta `5e-5`
- no SWA
- WANDB disabled

架构变化：

- `scripts/dynamics_learning/models/grutcn.py` 增加 checkpoint-safe temporal refiner。
- 形式：`LayerNorm -> MultiheadAttention -> LayerNorm -> FFN`，带 zero-init output projection 和 residual scale。
- `_encode()` 在 `encoder_norm(enc_seq)` 之后、`context_gru(enc_seq)` 之前使用该 refiner。
- 新增 dual latent context：把 attention `context`、`history_context`、`enc_seq` last/mean 融合成第二 context residual，加在 `decoder_input` 前，最后 projection zero-init。
- 新增 raw history residual fusion：从完整 raw `[x,u]` history 汇总 latent residual，加到 `head_input`，最后 projection zero-init。
- 所有新增输出初始为 0，因此旧 checkpoint 初始保持校准行为，同时允许新分支学习更强 temporal context/raw-history context。

检查和同步：

- local `python -m py_compile scripts/dynamics_learning/models/grutcn.py` 通过。
- local `git diff --check -- scripts/dynamics_learning/models/grutcn.py Prompt.md MODEL_DEV_HANDOFF.md` 通过。
- 只同步了 `scripts/dynamics_learning/models/grutcn.py` 到 `gpu4060`，未使用 `--delete`；local/remote `grutcn.py` hash：`8c5ae1489fa1961df585f15789e64932cc9b9f2e10e6a2a25d163a76cf7a7f92`。
- remote py_compile 通过。
- remote one-batch CUDA smoke 通过：从 ultralow e4 checkpoint 加载，missing keys 只包含新增 temporal/dual/raw 模块，无 unexpected keys；`train_loss_step=0.150`，`valid_loss_epoch=0.0807`。
- 正式训练已于 2026-05-10 16:26 CST 在 tmux 启动；启动巡检显示 RTX 4060 约 `5525/8188 MiB`、util `39%`，epoch 0 finite train loss steps 约 `0.096-0.160`。
- e0 validation：`train_loss=0.233`，`valid_loss=0.4729816`，`best_valid_loss=0.4729815`，`valid_q=0.0389585`，`valid_p=0.0433674`，`valid_v=0.1613430`，`valid_omega=0.2293125`。
- e1 validation：`train_loss=0.23257`，`valid_loss=0.4730309`，`best_valid_loss=0.4729815`，`valid_q=0.0390640`，`valid_p=0.0434037`，`valid_v=0.1609298`，`valid_omega=0.2296333`。
- e2 validation：`train_loss=0.232`，`valid_loss=0.4721888`，`best_valid_loss=0.4721887`，`valid_q=0.0390779`，`valid_p=0.0433664`，`valid_v=0.1599812`，`valid_omega=0.2297631`。
- e3 validation：`valid_loss=0.4732611`，`best_valid_loss=0.4721887`，`valid_q=0.0391039`，`valid_p=0.0434005`，`valid_v=0.1609785`，`valid_omega=0.2297781`。
- 决策：e3 未刷新 e2 best，且仍未达到 preferred audit band `<=0.4720`；继续等 e4 作为后续确认，不做 locked audit。若 e4 不刷新到 `<=0.4720` 或 submetrics 明显改善不足，则优先停止该候选并 pivot。
- artifacts：当前可见 checkpoints 包括 `checkpoints/model-epoch=01-best_valid_loss=0.47.pth`、`checkpoints/model-epoch=02-best_valid_loss=0.47.pth`、`checkpoints/model-epoch=03-best_valid_loss=0.47.pth` 和 `last_model.pth`。
- horizon/test metric 内容未读取。
- 2026-05-10 17:28 CST 巡检异常：SSH 连接 `gpu4060` 超时，未能确认 e4 最新状态；未读取 horizon/test metric 内容，下次心跳重试。
- 2026-05-10 17:50 CST 远程连通性升级异常：`ping -c 2 192.168.1.108` 为 `100% packet loss`，`nc -vz -G 4 192.168.1.108 22` 连接 22 端口超时；当前从本机看 `gpu4060` 主机不可达，不只是 SSH 命令慢。未读取 horizon/test metric 内容；下一步先重试连通性，恢复后立刻检查 e4/e5 validation。
- 2026-05-10 18:38 CST 远程恢复后确认：`gpu4060` 已重启，`uptime` 约 0 分钟，GPU 空闲约 `381/8188 MiB`，tmux 缺失且训练进程不存在；训练日志停在 epoch 4 约 `554/771`（约 72%）并出现 NUL 尾部，未完成 e4 validation。CSV 仍只有 e0-e3 validation rows，checkpoint 只有 e1/e2/e3/last；日志未出现 `CUDA out of memory`、`Traceback`、`Killed`。判断为整机/驱动/WSL/电源级硬中断或重启，不是普通 PyTorch OOM；未读取 horizon/test metric 内容。当前 best 仍是 e2 `best_valid_loss=0.4721887`，未达到 preferred audit band，暂不 audit。
- 2026-05-10 18:40 CST 已启动远程 GPU watch tmux：`modeldev_gpu_watch_dualraw_H20`，日志 `resources/experiments/modeldev_20260510_grutcn_dualraw_H20_from_ultralow_e4_p1/logs/gpu_watch.log`，每 10 秒记录 `nvidia-smi` GPU memory/util/temp/power 和 compute apps，用于后续判断是否显存、驱动或整机问题。
- 2026-05-10 19:24 CST 按用户要求保守恢复：新实验 `modeldev_20260510_grutcn_dualraw_H20_resume64_from_last_p1` 保留原断点 artifacts，不覆盖旧目录；显式激活 conda `dynamics_learning`，remote py_compile 通过，`grutcn.py` hash 仍为 `8c5ae1489fa1961df585f15789e64932cc9b9f2e10e6a2a25d163a76cf7a7f92`；训练命令使用 `--resume_from_checkpoint` 指向原实验 `last_model.pth`，`--batch_size 64 --accumulate_grad_batches 8`，W&B disabled。未读取 horizon/test metric 内容。
- 2026-05-10 19:50 CST resume64 e4 validation 完成：`valid_loss=0.4732777`，`best_valid_loss=0.4732777`，`valid_q=0.0389887`，`valid_p=0.0435931`，`valid_v=0.1611713`，`valid_omega=0.2295246`；未刷新原 dual/raw e2 best `0.4721887`，也未到 preferred audit band `<=0.4720`。训练已进入 e5，继续等 e5 最终确认；当前不 audit，未读取 horizon/test metric 内容。
- 2026-05-10 20:48 CST resume64 e5 validation 完成：`valid_loss=0.4728829`，`best_valid_loss=0.4728828`，`valid_q=0.0389932`，`valid_p=0.0435362`，`valid_v=0.1608482`，`valid_omega=0.2295049`；仍未刷新原 dual/raw e2 best `0.4721887`，也未达到 preferred audit band `<=0.4720`。结论：dual/raw resume64 不是冻结候选，不做 locked audit；horizon/test metric 内容未读取；artifacts 保留；专属 GPU watch tmux `modeldev_gpu_watch_dualraw_resume64_H20` 已停止。
- 下一步：停止继续围绕 dual/raw 细调，不 audit；pivot 到 TCNLSTM 温启动或下一种更强 latent-context 结构，仍遵守只看 train/validation/log/checkpoint/架构信号的规则。
- 2026-05-10 20:58 CST TCNLSTM 结构 pivot：按 Harvey 子 agent 建议修正 `scripts/dynamics_learning/models/tcnlstm.py`，让 TCN-compatible anchor 分支命名为 `decoder`，从而旧 TCN checkpoint 的 `model.encoder.*` / `model.decoder.*` 直接加载到 base predictor；将 latent correction head 改名为 `context_decoder` 并 final zero-init；新增 checkpoint-safe temporal refiner，但 anchor decoder 使用未 `LayerNorm` 的 raw TCN feature，以保持 TCN checkpoint 行为。local/remote hash `ab3f30a6dd0eaa02369a7752739bf26e1210d5708cb9b3668efd2ccc50d8a6ab`；local/remote py_compile 通过；H10 true-anchor smoke 从 TCN H10 checkpoint 加载无 unexpected keys，missing 仅新增 context/refiner/LSTM 分支，一批次 `train_loss_step=0.189`、`valid_loss_epoch=0.123`，有限且量级正常。未读取 horizon/test metric 内容。
- 2026-05-10 23:12 CST TCNLSTM attitude-aware 结构改动：按架构/结果 agent 建议，在 `scripts/dynamics_learning/models/tcnlstm.py` 新增 checkpoint-safe attitude attention + attitude correction branch，只写入模型输出 `dtheta/delta_omega` 的 `[:, 6:12]`，用于增强姿态/角速度 latent context；不改 `encoder`、不改 TCN-compatible `decoder` anchor、不改变 `base_feature = anchor_seq[:, -1, :]` 路径。新增 `attitude_decoder` final zero-init、`attitude_delta_scale=0.05`，因此从旧 TCNLSTM e0 checkpoint 初始化时初始为 no-op。local/remote hash `527599fd329a5f4c9f010da276bdc34905e2297a73aa1bdfb01ea1471c80cc7d`；local/remote py_compile 和 `git diff --check` 通过；remote smoke `smoke_20260510_tcnlstm_attitude_H10_from_trueanchor_e0_p1` 从 e0 checkpoint 加载，missing 仅新增 attitude keys、无 unexpected keys，一批 train/valid finite：`train_loss_step=0.175`，`valid_loss_epoch=0.590`。未读取 horizon/test metric 内容。
- 2026-05-10 23:12 CST 启动 active 训练 `modeldev_20260510_tcnlstm_attitude_H10_from_trueanchor_e0_p1`：从 TCNLSTM true-anchor e0 best checkpoint 初始化，H10/F50，epochs `5`，batch `64`、accumulate `8`、effective batch `512`，`limit_train_batches=0.25`、`limit_val_batches=0.5`，`warmup_lr=3e-6`、`cosine_lr=8e-7`、`warmup_steps=50`、`cosine_steps=1000`，early stopping patience `2`、min_delta `2e-5`，WANDB disabled。tmux `modeldev_tcnlstm_attitude_H10_from_trueanchor_e0_p1`；GPU watch `modeldev_gpu_watch_tcnlstm_attitude_H10`；log `resources/experiments/modeldev_20260510_tcnlstm_attitude_H10_from_trueanchor_e0_p1/logs/train_phase1.log`。启动巡检：tmux/process/GPU healthy，RTX 4060 约 `1957/8188 MiB`、util `41%`，train steps finite；horizon/test metric 内容未读取。
- 2026-05-10 23:36 CST TCNLSTM attitude e0 validation 完成并刷新 true-anchor e0：`train_loss_epoch=0.3352483`，`valid_loss_epoch=0.4619423`，`best_valid_loss=0.4619424`，`valid_q=0.0411794`，`valid_p=0.0324030`，`valid_v=0.1508975`，`valid_omega=0.2374622`。相对 true-anchor e0 `best_valid_loss=0.4623865`、`valid_q=0.0414418`、`valid_omega=0.2384134` 有小幅同步改善，`valid_v` 从 `0.1502742` 轻微回退但仍接近；训练已进入 e1，继续等 e1/e2 确认趋势，当前不冻结、不 locked audit；horizon/test metric 内容未读取，artifacts 保留。
- 2026-05-11 00:04 CST TCNLSTM attitude e1 validation 完成并小幅刷新 best：`train_loss_epoch=0.3338704`，`valid_loss_epoch=0.4618453`，`best_valid_loss=0.4618454`，`valid_q=0.0412394`，`valid_p=0.0321995`，`valid_v=0.1505699`，`valid_omega=0.2378366`。相对 e0 总 loss 和 `valid_v` 改善，`valid_q/valid_omega` 较 e0 略回退但仍优于 true-anchor e0，且仍在 attitude gate 内；训练已进入 e2，继续等 e2 确认趋势，当前不冻结、不 locked audit；horizon/test metric 内容未读取，artifacts 保留。
- 2026-05-11 00:25 CST TCNLSTM attitude e2 validation 完成但未刷新 e1 best：`train_loss_epoch=0.3332587`，`valid_loss_epoch=0.4620062`，`best_valid_loss=0.4618454`，`valid_q=0.0412714`，`valid_p=0.0321716`，`valid_v=0.1506382`，`valid_omega=0.2379250`。e2 总 loss、`valid_q`、`valid_omega` 较 e1 回退，但 q/omega 仍优于 true-anchor e0 且在 attitude gate 内；训练已进入 e3，继续观察自然收敛/early stopping。当前不冻结、不 locked audit；horizon/test metric 内容未读取，只确认 `plots/testset` 目录存在；artifacts 保留。
- 2026-05-11 00:32 CST 已接手并更新本窗口 heartbeat automation 到当前 active experiment。只读交叉巡检确认训练仍在 e3、`train_summary.json` 尚未生成，当前 validation-selected best 应为 e1 checkpoint `checkpoints/model-epoch=01-best_valid_loss=0.46.pth`。关键决策：不要手动停止训练后直接 audit；等待 natural early stop/max epoch 生成 `train_summary.json`，确保 eval 稳定选择 validation best 后再冻结 locked audit。horizon/test metric 内容未读取。
- 2026-05-11 00:47 CST TCNLSTM attitude e3 validation 刷新 best：`train_loss_epoch=0.3327240`，`valid_loss_epoch=0.4615006`，`best_valid_loss=0.4615005`；checkpoint `checkpoints/model-epoch=03-best_valid_loss=0.46.pth` 已产生，训练已进入 e4。当前不手动停止、不 locked audit；等待 natural finish/summary 指向 validation-selected e3 best 后再冻结 audit。horizon/test metric 内容未读取，只确认 `plots/testset` 目录存在。
- 2026-05-11 01:12 CST TCNLSTM attitude 训练自然到 `max_epochs=5` 结束，`early_stopped=false`；e4 `valid_loss_epoch=0.4616226`、`best_valid_loss=0.4615005`、`valid_q=0.0412516`、`valid_p=0.0321305`、`valid_v=0.1503205`、`valid_omega=0.2379201`，未刷新 e3。`train_summary.json` 稳定指向 validation-selected checkpoint `checkpoints/model-epoch=03-best_valid_loss=0.46.pth`，冻结为 locked audit 候选；训练阶段 horizon/test metric 内容未读取，artifacts 保留。
- 2026-05-11 01:12 CST 已启动 locked audit：audit id `lockaudit_20260511_tcnlstm_attitude_H10_e3_p1`，tmux `audit_tcnlstm_attitude_H10_e3_p1`，日志 `resources/experiments/modeldev_20260510_tcnlstm_attitude_H10_from_trueanchor_e0_p1/logs/eval_lockaudit_20260511_tcnlstm_attitude_H10_e3_p1_b32.log`。命令使用 `scripts/eval.py --dataset neurobemfullstate --predictor_type full_state --accelerator cuda --gpu_id 0 --eval_batch_size 32 --eval_horizons 1,10,25,50 --wandb_mode disabled --experiment_path resources/experiments/modeldev_20260510_tcnlstm_attitude_H10_from_trueanchor_e0_p1`；启动检查显示 tmux/process alive，horizon/test metric 内容尚未读取。若 batch `32` OOM 或崩溃，用 batch `16` 重试同一冻结 checkpoint 并记录。
- 2026-05-11 01:25 CST locked audit 完成：audit id `lockaudit_20260511_tcnlstm_attitude_H10_e3_p1`，batch `32`，无 OOM retry，average rollout loss `0.5288807750`。h50：`E_q=0.0907420070`、`E_v=0.3453549326`、`E_omega=0.3335756319`；mean：`E_q=0.0458699548`、`E_v=0.1857792108`、`E_omega=0.2566082108`。结论：partial locked win，`h50_E_v` 仍优于 `gru_H20=0.353015` 和 `tcn_H10=0.347036`，但 h50 quaternion、h50 omega、mean quaternion 未赢 strongest targets；不是全面 winner。horizon/test metric 内容只在 locked audit 完成后读取；artifacts 保留，未清理；不从 audit 细节反向调参。
- 2026-05-11 01:27 CST 根据结果/结构 agent 交叉建议，选择无代码改动的 `TCNLSTM attitude H20/F50` history extension 作为下一步低风险 screening；理由是当前 `tcnlstm.py` 参数形状不依赖 `history_length`，可以从 H10 e3 checkpoint 温启动并给 latent context 更多历史。remote smoke `smoke_20260511_tcnlstm_attitude_H20_from_H10e3_p1` 通过：H20 数据窗口正常，checkpoint 加载成功，一批 train/valid finite：`train_loss_step=0.169`、`valid_loss_epoch=0.115`。训练阶段未读取 horizon/test metric 内容。
- 2026-05-11 01:29 CST 启动 active training `modeldev_20260511_tcnlstm_attitude_H20_from_H10e3_p1`：从 `modeldev_20260510_tcnlstm_attitude_H10_from_trueanchor_e0_p1/checkpoints/model-epoch=03-best_valid_loss=0.46.pth` 初始化，H20/F50，epochs `5`，batch `64`、accumulate `8`、effective batch `512`，`limit_train_batches=0.25`、`limit_val_batches=0.5`，`warmup_lr=2e-6`、`cosine_lr=5e-7`、`warmup_steps=50`、`cosine_steps=1000`，early stopping patience `2`、min_delta `2e-5`，WANDB disabled。tmux `modeldev_tcnlstm_attitude_H20_from_H10e3_p1`；GPU watch `modeldev_gpu_watch_tcnlstm_attitude_H20`；log `resources/experiments/modeldev_20260511_tcnlstm_attitude_H20_from_H10e3_p1/logs/train_phase1.log`。启动巡检：tmux/process/GPU healthy，RTX 4060 约 `2950/8188 MiB`、util `31%`，epoch 0 train steps finite；horizon/test metric 内容未读取，只确认 `plots/testset` 目录存在。H20 gate：e0 `>0.465` 直接判定跨 history mismatch 停止；`0.4625-0.465` 最多给 e1/e2；`<=0.4625` 健康；冻结候选优先要求 `best_valid_loss <= 0.4613`，或 `<=0.4615` 且 `valid_q < 0.04118`、`valid_omega < 0.23746`、`valid_v <= 0.1506` 同步成立。
- 2026-05-11 01:54 CST H20 e0 validation 完成：`train_loss_epoch=0.335`，`valid_loss_epoch=0.4630005`，`best_valid_loss=0.4630004`，`valid_q=0.0413083`，`valid_p=0.0323164`，`valid_v=0.1509458`，`valid_omega=0.2384300`；checkpoint `checkpoints/model-epoch=00-best_valid_loss=0.46.pth` 已产生，训练已进入 e1。结论：e0 落在 `0.4625-0.465` 灰区，未触发 `>0.465` 跨 history mismatch 停止线，但未满足健康/冻结线，且 q/omega/v 未同步过门；按预声明最多观察到 e1/e2。当前不冻结、不 locked audit；horizon/test metric 内容未读取，只确认 `plots/testset` 目录存在。
- 2026-05-11 02:17 CST H20 e1 validation 完成并小幅刷新 best：`train_loss_epoch=0.3348033`，`valid_loss_epoch=0.4628834`，`best_valid_loss=0.4628835`，`valid_q=0.0413141`，`valid_p=0.0322572`，`valid_v=0.1507668`，`valid_omega=0.2385453`；checkpoint `checkpoints/model-epoch=01-best_valid_loss=0.46.pth` 已产生，训练已进入 e2。结论：总 loss 较 e0 小幅改善但仍在 `0.4625-0.465` 灰区，未到冻结线；`valid_v` 改善且未触发 `>0.1510` 停止线，但 `valid_q/valid_omega` 较 e0 回退，尚非同步健康。继续按预声明观察 e2；当前不冻结、不 locked audit；horizon/test metric 内容未读取，只确认 `plots/testset` 目录存在。
- 2026-05-11 02:44 CST H20 e2 validation 完成并小幅刷新 best：`train_loss_epoch=0.3340761`，`valid_loss_epoch=0.4627466`，`best_valid_loss=0.4627467`，`valid_q=0.0413706`，`valid_p=0.0321600`，`valid_v=0.1504421`，`valid_omega=0.2387740`；checkpoint `checkpoints/model-epoch=02-best_valid_loss=0.46.pth` 已产生，训练已进入 e3。结论：虽然 total loss 和 `valid_v` 继续改善，但仍未达到冻结候选线 `best_valid_loss <= 0.4613` 或 `<=0.4615` 且 q/omega/v 同步健康；`valid_q`、`valid_omega` 从 e0 到 e2 连续回退，触发 H20 gate 停止条件。决策：不做 locked audit，不读取 horizon/test metric 内容；已仅停止该实验训练 tmux `modeldev_tcnlstm_attitude_H20_from_H10e3_p1` 和 GPU watch `modeldev_gpu_watch_tcnlstm_attitude_H20`，artifacts 保留（checkpoints e0/e1/e2 和 `last_model.pth`），`train_summary.json` 未生成。下一步基于 train/validation/架构信号考虑 `scripts/dynamics_learning/models/tcnlstm.py` 的 checkpoint-safe decoder-state residual initializer。
- 2026-05-11 02:52 CST TCNLSTM decoder-state residual initializer 代码改动：`scripts/dynamics_learning/models/tcnlstm.py` 在原 `state_initializer` 之后新增 `decoder_state_residual_norm`、`decoder_state_residual`、`decoder_state_residual_scale=0.05`。输入为 `enc_seq` last/mean、`history_context` 和用 `history_context+x_last` 查询得到的 attitude context；输出维度为 LSTM `h0/c0`，最后一层 zero-init，因此旧 checkpoint 初始为 no-op。保持 TCN-compatible anchor 不变：`encoder`、`decoder`、`base_feature = anchor_seq[:, -1, :]` 路径未改；未使用外部 trick 或 horizon/test 信息。
- 检查/同步：local `python -m py_compile scripts/dynamics_learning/models/tcnlstm.py` 通过；本地 forward smoke 因 Mac Python 缺少 `torch` 未跑，转由远程环境验证；local/remote `tcnlstm.py` hash 均为 `55ac4a3f86de7256d70bbbc2ee55351a5f1fd9bb1efdb19b149ccc5f317b3bb1`；remote py_compile 通过。remote one-batch smoke `smoke_20260511_tcnlstm_stateinit_H10_from_attitudee3_p1` 从 H10 attitude e3 checkpoint 加载，missing keys 仅新增 `decoder_state_residual*`，无 unexpected keys；`train_loss_step=0.176`、`valid_loss_epoch=0.122`，checkpoint 与 `train_summary.json` 正常生成；未读取 horizon/test metric 内容，只确认 `plots/testset` 目录存在。
- 2026-05-11 02:53 CST 启动 active training `modeldev_20260511_tcnlstm_stateinit_H10_from_attitude_e3_p1`：从 `modeldev_20260510_tcnlstm_attitude_H10_from_trueanchor_e0_p1/checkpoints/model-epoch=03-best_valid_loss=0.46.pth` 初始化，H10/F50，epochs `5`，batch `64`、accumulate `8`、effective batch `512`，`limit_train_batches=0.25`、`limit_val_batches=0.5`，`warmup_lr=2e-6`、`cosine_lr=5e-7`、`warmup_steps=50`、`cosine_steps=1000`，early stopping patience `2`、min_delta `2e-5`，WANDB disabled。tmux `modeldev_tcnlstm_stateinit_H10_from_attitude_e3_p1`；GPU watch `modeldev_gpu_watch_tcnlstm_stateinit_H10`；log `resources/experiments/modeldev_20260511_tcnlstm_stateinit_H10_from_attitude_e3_p1/logs/train_phase1.log`。启动巡检：tmux/process/GPU healthy，RTX 4060 约 `1974/8188 MiB`、util `31%`，train steps finite；horizon/test metric 内容未读取，只确认 `plots/testset` 目录存在。
- stateinit H10 gate：参考 H10 attitude best `0.4615005`。e0 `>0.4625` 判定破坏校准，停止不 audit；e0 `0.4617-0.4625` 最多给 e1/e2，看 `valid_q`、`valid_omega`、`valid_v` 是否同步健康；e0 `<=0.4615` 算健康。冻结候选优先要求 `best_valid_loss <= 0.4613`；或 `<=0.46145` 且 `valid_q < 0.04118`、`valid_omega < 0.23746`、`valid_v <= 0.1505` 同步成立。locked audit 只能在 natural finish/summary 指向 validation-selected checkpoint 后启动。
- 2026-05-11 03:21 CST stateinit H10 e0 validation 完成：`valid_loss_epoch=0.4617348`，`best_valid_loss=0.4617349`，`valid_q=0.0412232`，`valid_p=0.0321486`，`valid_v=0.1504455`，`valid_omega=0.2379177`；checkpoint `checkpoints/model-epoch=00-best_valid_loss=0.46.pth` 和 `last_model.pth` 已产生，训练已进入 e1，`train_summary.json` 尚未生成。判定：e0 位于预声明灰区 `0.4617-0.4625`，不触发 `>0.4625` 停止线，但未达到健康线/冻结线；继续最多观察 e1/e2 是否在 total loss、`valid_q`、`valid_omega`、`valid_v` 上同步健康。当前不冻结、不 locked audit；horizon/test metric 内容未读取，只确认 `plots/testset` 目录存在。
- 2026-05-11 03:40 CST stateinit H10 e1 validation 完成并刷新本实验 best：`valid_loss_epoch=0.4615996`，`best_valid_loss=0.4615997`，`train_loss_epoch=0.332`，`valid_q=0.0412552`，`valid_p=0.0321055`，`valid_v=0.1502778`，`valid_omega=0.2379611`；checkpoint `checkpoints/model-epoch=01-best_valid_loss=0.46.pth` 和 `last_model.pth` 已更新，训练已进入 e2，`train_summary.json` 尚未生成。判定：total loss 和 `valid_v` 较 e0 改善，但仍未达到冻结候选线 `best_valid_loss <= 0.4613`，也不满足 `<=0.46145` 且 q/omega/v 同步健康；`valid_q/valid_omega` 较 e0 回退。当前继续观察 e2，不冻结、不 locked audit；horizon/test metric 内容未读取，只确认 `plots/testset` 目录存在。
- 2026-05-11 04:06 CST stateinit H10 e2 validation 完成但未刷新 e1 best：`valid_loss_epoch=0.4617738`，`best_valid_loss=0.4615997`，`valid_q=0.0413049`，`valid_p=0.0320988`，`valid_v=0.1501462`，`valid_omega=0.2382236`；checkpoint `checkpoints/model-epoch=02-best_valid_loss=0.46.pth` 和 `last_model.pth` 已产生，`train_summary.json` 未生成。判定：e2 未达到冻结候选线 `best_valid_loss <= 0.4613`，也不满足 `<=0.46145` 且 q/omega/v 同步健康；`valid_q/valid_omega` 从 e0 到 e2 连续回退，触发 stateinit H10 gate 失败。决策：不做 locked audit，不读取 horizon/test metric 内容；已仅停止该实验训练 tmux `modeldev_tcnlstm_stateinit_H10_from_attitude_e3_p1` 和 GPU watch `modeldev_gpu_watch_tcnlstm_stateinit_H10`，artifacts 保留（checkpoints e0/e1/e2 和 `last_model.pth`）。下一步只能基于 train/validation/架构信号讨论 TCNLSTM/GRUTCN 结构。
- 2026-05-11 04:12 CST TCNLSTM cell-memory residual 改动：`scripts/dynamics_learning/models/tcnlstm.py` 将 `decoder_state_residual` 从同时扰动 LSTM `h0/c0` 改为只调 `c0`，`h0` 保持 base initializer，用 `residual_gate` 控制 cell residual；动机是 stateinit H10 的 q/omega 连续回退说明 hidden-state query 可能污染 attention，相比 full h0/c0 residual 更局部、更稳。TCN anchor 仍不变：`encoder`、`decoder`、`base_feature = anchor_seq[:, -1, :]` 路径未改；未使用外部 trick 或 horizon/test 信息。local/remote hash `239d693579fa11d594d3bc9e163e69ed73bc6c25f1862bcace907b16656ddd84`；local py_compile、`git diff --check`、remote py_compile 均通过。remote one-batch smoke `smoke_20260511_tcnlstm_cellinit_H10_from_attitudee3_p1` 从 H10 attitude e3 checkpoint 加载，missing keys 仅新增 `decoder_state_residual*`，一批 train/valid finite：`train_loss_step=0.169`、`valid_loss_epoch=0.118`；未读取 horizon/test metric 内容，只确认 `plots/testset` 目录存在。
- 2026-05-11 04:14 CST 启动 active training `modeldev_20260511_tcnlstm_cellinit_H10_from_attitude_e3_p1`：从 `modeldev_20260510_tcnlstm_attitude_H10_from_trueanchor_e0_p1/checkpoints/model-epoch=03-best_valid_loss=0.46.pth` 初始化，H10/F50，epochs `5`，batch `64`、accumulate `8`、effective batch `512`，`limit_train_batches=0.25`、`limit_val_batches=0.5`，`warmup_lr=2e-6`、`cosine_lr=5e-7`、`warmup_steps=50`、`cosine_steps=1000`，early stopping patience `2`、min_delta `2e-5`，WANDB disabled。tmux `modeldev_tcnlstm_cellinit_H10_from_attitude_e3_p1`；GPU watch `modeldev_gpu_watch_tcnlstm_cellinit_H10`；log `resources/experiments/modeldev_20260511_tcnlstm_cellinit_H10_from_attitude_e3_p1/logs/train_phase1.log`。启动巡检：tmux/process/GPU healthy，RTX 4060 约 `1928/8188 MiB`、util `25%`，epoch 0 train steps finite；horizon/test metric 内容未读取，只确认 `plots/testset` 目录存在。cellinit H10 gate：参考 H10 attitude best `0.4615005`；e0 `>0.4622` 直接停止不 audit；e0 `0.4616-0.4622` 最多给 e1/e2；冻结候选仍要求 `best_valid_loss <= 0.4613`，或 `<=0.46145` 且 `valid_q < 0.04118`、`valid_omega < 0.23746`、`valid_v <= 0.1505` 同步成立；q/omega 连续两轮回退即 validation-only 失败，不 audit。
- 2026-05-11 04:41 CST cellinit H10 e0 validation 完成：`train_loss_epoch=0.333`，`valid_loss_epoch=0.4617287`，`best_valid_loss=0.4617288`，`valid_q=0.0412230`，`valid_p=0.0321477`，`valid_v=0.1504408`，`valid_omega=0.2379174`；checkpoint `checkpoints/model-epoch=00-best_valid_loss=0.46.pth` 和 `last_model.pth` 已产生，训练进入 e1，`train_summary.json` 尚未生成。判定：e0 落在预声明灰区 `0.4616-0.4622`，不触发停止，也未达到健康/冻结候选线；`valid_v` 达到同步线但 `valid_q/valid_omega` 未过门。继续观察 e1/e2，不冻结、不 locked audit；horizon/test metric 内容未读取，只确认 `plots/testset` 目录存在。
- 2026-05-11 05:01 CST cellinit H10 e1 validation 完成并刷新本实验 best：`valid_loss_epoch=0.4615999`，`best_valid_loss=0.4616000`，`valid_q=0.0412553`，`valid_p=0.0321055`，`valid_v=0.1502781`，`valid_omega=0.2379610`；checkpoint `checkpoints/model-epoch=01-best_valid_loss=0.46.pth` 与 `last_model.pth` 已更新，训练进入 e2，`train_summary.json` 尚未生成。判定：total loss 与 `valid_v` 较 e0 改善，但未达到冻结候选线 `best_valid_loss <= 0.4613`，也不满足 `<=0.46145` 且 q/omega/v 同步健康；`valid_q/valid_omega` 较 e0 回退。继续观察 e2，若 q/omega 再次回退则按预声明 validation-only 失败停止；当前不冻结、不 locked audit；horizon/test metric 内容未读取，只确认 `plots/testset` 目录存在。
- 2026-05-11 05:25 CST cellinit H10 e2 validation 完成但未刷新 e1 best：`valid_loss_epoch=0.4617757`，`best_valid_loss=0.4616000`，`valid_q=0.0413049`，`valid_p=0.0320990`，`valid_v=0.1501480`，`valid_omega=0.2382238`；checkpoint `checkpoints/model-epoch=02-best_valid_loss=0.46.pth` 和 `last_model.pth` 已产生。判定：e2 未达到冻结候选线 `best_valid_loss <= 0.4613`，也不满足 `<=0.46145` 且 q/omega/v 同步健康；`valid_q/valid_omega` 从 e0 到 e2 连续回退，触发 cellinit H10 validation-only failure。执行：不做 locked audit，不读取 horizon/test metric 内容；已仅停止训练 tmux `modeldev_tcnlstm_cellinit_H10_from_attitude_e3_p1`、匹配该实验路径的训练进程和 GPU watch `modeldev_gpu_watch_tcnlstm_cellinit_H10`；artifacts 保留（checkpoints e0/e1/e2 和 `last_model.pth`），`train_summary.json` 未生成。下一步只能基于 train/validation/架构信号讨论 TCNLSTM/GRUTCN 结构，优先复盘 stateinit/cellinit 的共同 q/omega 退化。
- 2026-05-11 05:34 CST TCNLSTM local attitude fine residual 改动：`scripts/dynamics_learning/models/tcnlstm.py` 保留但不再使用 `decoder_state_residual` 对 LSTM `h0/c0` 的扰动，避免 stateinit/cellinit 已暴露的 q/omega 退化；新增 checkpoint-safe `attitude_fine_*` 分支，只在输出端用 `attitude_input + base_delta[:, 6:12]` 生成 `dtheta/delta_omega` 的小尺度 residual，`attitude_fine_delta_scale=0.02`，final projection zero-init。TCN anchor 保持不变：`encoder`、`decoder`、`base_feature = anchor_seq[:, -1, :]`；未使用外部 trick 或 horizon/test 信息。local/remote `tcnlstm.py` hash `be9793501f16aab8aad4e45c0e72cf78462c42eb124e4fce23c59b8b822f2df5`；local py_compile、`git diff --check`、remote py_compile 均通过。remote one-batch smoke `smoke_20260511_tcnlstm_attfine_H10_from_attitudee3_p1` 从 H10 attitude e3 checkpoint 加载，missing keys 仅新增/保留 `decoder_state_residual*` 与 `attitude_fine*`，无 unexpected keys；`train_loss_step=0.181`、`valid_loss_epoch=0.118`，checkpoint 和 `train_summary.json` 正常生成；horizon/test metric 内容未读取，只确认 `plots/testset` 目录存在。
- 2026-05-11 05:37 CST 启动 active training `modeldev_20260511_tcnlstm_attfine_H10_from_attitude_e3_p1`：从 `resources/experiments/modeldev_20260510_tcnlstm_attitude_H10_from_trueanchor_e0_p1/checkpoints/model-epoch=03-best_valid_loss=0.46.pth` 初始化，H10/F50，epochs `5`，batch `64`、accumulate `8`、effective batch `512`，`limit_train_batches=0.25`、`limit_val_batches=0.5`，`warmup_lr=2e-6`、`cosine_lr=5e-7`、`warmup_steps=50`、`cosine_steps=1000`，early stopping patience `2`、min_delta `2e-5`，WANDB disabled。gate：参考 H10 attitude best `0.4615005`；e0 `>0.4622` 判定 fine residual 破坏校准，停止不 audit；e0 `0.4616-0.4622` 最多观察 e1/e2；e0 `<=0.46155` 算健康；冻结候选优先要求 `best_valid_loss <= 0.4613`，或 `<=0.46145` 且 `valid_q < 0.04118`、`valid_omega < 0.23746`、`valid_v <= 0.1505` 同步成立。locked audit 只能在 natural finish/`train_summary.json` 指向 validation-selected best checkpoint 后启动；训练阶段不读取 horizon/test metric 内容。
- 2026-05-11 06:03 CST attfine H10 e0 validation 完成并触发预声明停止线：`train_loss_epoch=0.3329748`，`valid_loss_epoch=0.4623119`，`best_valid_loss=0.4623121`，`valid_q=0.0412572`，`valid_p=0.0322411`，`valid_v=0.1507487`，`valid_omega=0.2380650`；checkpoint `checkpoints/model-epoch=00-best_valid_loss=0.46.pth` 和 `last_model.pth` 已产生，`train_summary.json` 未生成。判定：e0 `0.4623119 > 0.4622`，local attitude fine residual 破坏校准，validation-only failure。执行：不做 locked audit，不读取 horizon/test metric 内容；已仅停止该实验训练 tmux、匹配实验路径的训练进程和 GPU watch；artifacts 保留（e0 checkpoint、`last_model.pth`、CSV/logs）。
- 2026-05-11 06:12 CST 根据结果/架构 agent 交叉建议，选择下一候选 `TCNLSTM dual-view long-history translational residual`：保留 H10 attitude anchor，只让 H20 长历史进入非姿态 `[:, :6]` 的慢变量 residual，避免 stateinit/cellinit/attfine 已反复出现的 `valid_q/valid_omega` 退化。代码文件 `scripts/dynamics_learning/models/tcnlstm.py`：新增 `anchor_history_len=min(history_len,10)`，`encoder`/`decoder`/`base_feature=anchor_seq[:, -1, :]` 只看最后 10 帧以保持 H10 checkpoint 校准；新增 `long_history_lstm`、`long_delta_*`，full H20 只写 `long_delta[:, :6]`，`long_delta_decoder` final zero-init、`long_delta_scale=0.03`；保留但不再应用失败的 `attitude_fine_*` 输出 residual。未使用外部 trick、web research 或 horizon/test 信息。
- 检查/同步：local `python -m py_compile scripts/dynamics_learning/models/tcnlstm.py` 和 `git diff --check` 通过；local/remote `tcnlstm.py` hash 均为 `55717404b8786d64fcc5e96c8b742a30ee8b869a60e25d47a2a11df8e37581b6`；remote py_compile 通过。remote one-batch smoke `smoke_20260511_tcnlstm_dualview_longtrans_H20_from_attitude_e3_p1` 从 H10 attitude e3 checkpoint 加载，missing keys 仅新增/保留 `decoder_state_residual*`、`attitude_fine*`、`long_history*`、`long_delta*`，无 unexpected keys；`train_loss_step=0.178`、`valid_loss_epoch=0.115`，checkpoint 与 `train_summary.json` 正常生成；horizon/test metric 内容未读取，只确认 `plots/testset` 目录存在。
- 2026-05-11 06:14 CST 启动 active training `modeldev_20260511_tcnlstm_dualview_longtrans_H20_from_attitude_e3_p1`。gate：参考 H10 attitude best `0.4615005`；e0 `>0.4622` 判定长历史旁路破坏校准，停止不 audit；e0 `0.4616-0.4622` 最多观察 e1/e2，看 `valid_q`、`valid_omega`、`valid_v` 是否同步健康；e0 `<=0.46155` 算健康。冻结候选优先要求 `best_valid_loss <= 0.4613`，或 `<=0.46145` 且 `valid_q < 0.04118`、`valid_omega < 0.23746`、`valid_v <= 0.1505` 同步成立。若 q/omega 连续两轮回退，不读取 horizon/test metric 内容、不做 locked audit；locked audit 只能在 natural finish/`train_summary.json` 指向 validation-selected best checkpoint 后启动。
- 2026-05-11 06:43 CST dualview longtrans H20 e0 validation 完成并触发预声明 stop line：`train_loss_epoch=0.3349663`，`valid_loss_epoch=0.4628875`，`best_valid_loss=0.4628872`，`valid_q=0.0413091`，`valid_p=0.0322539`，`valid_v=0.1509052`，`valid_omega=0.2384190`；checkpoint `checkpoints/model-epoch=00-best_valid_loss=0.46.pth` 和 `last_model.pth` 已产生，`train_summary.json` 未生成。判定：e0 `0.4628875 > 0.4622`，长历史 translational bypass 破坏 H10 attitude anchor 校准，validation-only failure。执行：不做 locked audit，不读取 horizon/test metric 内容；已仅停止训练 tmux `modeldev_tcnlstm_dualview_longtrans_H20_p1`、匹配该实验路径的训练进程和 GPU watch `modeldev_gpu_watch_tcnlstm_dualview_H20`；artifacts 保留。下一步只能基于 train/validation/架构信号讨论 TCNLSTM/GRUTCN 结构，优先复盘 H20/长历史分支为何系统性抬高 total loss 与 q/omega。
- 2026-05-11 06:52 CST 根据结果/架构 agent 交叉建议，选择下一候选 `TCNLSTM H10 Lag-Observer Context`：保留 H10 attitude e3 anchor，不再把 failed `long_delta` 加到输出，也不触碰 LSTM `h0/c0`；新增 H10 内 `lag_observer`，用 `[x_anchor, dx_anchor]` 提取 actuator lag / aero lag / filter-state latent context，只注入 `head_input`，不直接写 `y`。文件：`scripts/dynamics_learning/models/tcnlstm.py`。新增 `lag_context_proj` final projection zero-init、`lag_context_scale=0.05`，从 H10 attitude e3 checkpoint 初始化时初始为 no-op。未使用 horizon/test 信息，也未使用外部 web research。
- 检查/同步：local py_compile 和 `git diff --check` 通过；local/remote `tcnlstm.py` hash 均为 `d5857bca84d7535e1022f14f999cd9630ea09b9a30dbe37e7580a94ce9ea6790`；remote py_compile 通过。remote one-batch smoke `smoke_20260511_tcnlstm_lagobserver_H10_from_attitudee3_p1` 从 H10 attitude e3 checkpoint 加载，missing keys 为新增/保留的 `lag_*`、`decoder_state_residual*`、`attitude_fine*`、`long_history*`、`long_delta*`，一批 train/valid finite：`train_loss_step=0.168`、`valid_loss_epoch=0.124`；horizon/test metric 内容未读取，只确认 `plots/testset` 目录存在。
- 2026-05-11 06:55 CST 启动 active training `modeldev_20260511_tcnlstm_lagobserver_H10_from_attitude_e3_p1`：从 `resources/experiments/modeldev_20260510_tcnlstm_attitude_H10_from_trueanchor_e0_p1/checkpoints/model-epoch=03-best_valid_loss=0.46.pth` 初始化，H10/F50，epochs `5`，batch `64`、accumulate `8`、effective batch `512`，`limit_train_batches=0.25`、`limit_val_batches=0.5`，`warmup_lr=1.5e-6`、`cosine_lr=4e-7`、`warmup_steps=50`、`cosine_steps=1000`，early stopping patience `2`、min_delta `2e-5`，WANDB disabled。启动巡检：tmux/process/GPU watch healthy，GPU 约 `2134/8188 MiB`、util `40%`，e0 train steps finite；horizon/test metric 内容未读取，只确认 `plots/testset` 目录存在。
- lagobserver H10 gate：参考 H10 attitude best `0.4615005`。e0 `>0.4620` 判定 lag observer 破坏校准，停止不 audit；e0 `0.4616-0.4620` 最多观察 e1/e2；e0 `<=0.46155` 算健康。冻结候选优先要求 `best_valid_loss <= 0.4613`；或 `<=0.46145` 且 `valid_q < 0.04118`、`valid_omega < 0.23746`、`valid_v <= 0.1503` 同步成立。q/omega 连续两轮回退即 validation-only failure，不读取 horizon/test metric 内容、不做 locked audit；locked audit 只能在 natural finish/`train_summary.json` 指向 validation-selected best checkpoint 后启动。
- 2026-05-11 07:23 CST lagobserver H10 e0 validation 完成并进入 e1：`train_loss_epoch=0.3326606`，`valid_loss_epoch=0.4618268`，`best_valid_loss=0.4618267`，`valid_q=0.0412433`，`valid_p=0.0321268`，`valid_v=0.1503970`，`valid_omega=0.2380596`；checkpoint `checkpoints/model-epoch=00-best_valid_loss=0.46.pth` 和 `last_model.pth` 已产生，`train_summary.json` 尚未生成。判定：e0 位于灰区 `0.4616-0.4620`，不触发 `>0.4620` 停止线，但未达到健康/冻结候选线；继续观察 e1/e2，当前不冻结、不 locked audit，horizon/test metric 内容未读取，只确认 `plots/testset` 目录存在。
- 2026-05-11 07:49 CST lagobserver H10 e1 validation 完成并刷新本实验 best：`train_loss_epoch=0.3322823`，`valid_loss_epoch=0.4615656`，`best_valid_loss=0.4615656`，`valid_q=0.0412526`，`valid_p=0.0321126`，`valid_v=0.1501766`，`valid_omega=0.2380238`；checkpoint `checkpoints/model-epoch=01-best_valid_loss=0.46.pth` 和 `last_model.pth` 已更新，`train_summary.json` 尚未生成，训练进入 e2。判定：total loss、`valid_v`、`valid_omega` 较 e0 改善，但仍未达到冻结候选线；`valid_q` 较 e0 回退。继续观察 e2，若 e2 未达到冻结线或 q/omega 形成连续回退，则按 validation-only failure 停止；当前不冻结、不 locked audit，horizon/test metric 内容未读取，只确认 `plots/testset` 目录存在。
- 2026-05-11 08:16 CST lagobserver H10 e2 validation 完成并刷新本实验 best：`train_loss_epoch=0.3323109`，`valid_loss_epoch=0.4614547`，`best_valid_loss=0.4614547`，`valid_q=0.0412437`，`valid_p=0.0320399`，`valid_v=0.1501071`，`valid_omega=0.2380640`；checkpoint `checkpoints/model-epoch=02-best_valid_loss=0.46.pth` 和 `last_model.pth` 已更新，`train_summary.json` 未生成。判定：e2 total loss 和 `valid_v` 较 e1 改善，但仍未达到冻结候选线 `best_valid_loss <= 0.4613`，且不满足 `<=0.46145` 与 q/omega/v 同步健康；尤其 `valid_q`、`valid_omega` 未过门。按预声明 e2 未改善到冻结线处理为 validation-only failure。执行：不做 locked audit，不读取 horizon/test metric 内容；已仅停止训练 tmux `modeldev_tcnlstm_lagobserver_H10_from_attitude_e3_p1`、匹配该实验路径的训练进程和 GPU watch `modeldev_gpu_watch_tcnlstm_lagobserver_H10`；artifacts 保留（checkpoints e0/e1/e2 和 `last_model.pth`）。下一步只能基于 train/validation/架构信号讨论 TCNLSTM/GRUTCN 结构。
- TCNLSTM true-anchor gates：e0 若 `>0.58` 直接停；e0 `<=0.55` 视为健康；e2 需低于 TCN H10 validation baseline `0.533292`，理想 `<=0.525` 才继续；只有 validation 大幅改善并呈现 submetrics 同步改善时才考虑冻结 audit。

监控门槛：

- 训练/开发阶段允许读取 horizon/test metric 内容，并可用于候选挑选、结构判断和模型改进；每次读取必须记录实验 id、checkpoint、关键指标和决策影响。
- e0 `>0.485`：停止。
- e0 `0.4732-0.485`：最多给 e1/e2。
- e0 `<=0.4732`：健康。
- e2 `>0.4725`：停止，除非它是 `0.47250-0.47255` 内的新低，可预声明给 e3 确认。
- locked audit 优先只给 `best_valid_loss <= 0.4720`，理想 `<=0.4718`，且 validation submetrics 不回退：约 `valid_v <= 0.1605`、`valid_omega <= 0.2294`、`valid_q <= 0.0390`。

## 近期关键实验摘要

### `modeldev_20260510_grutcn_anchor_e7_ultralow_p2`

- 模型：GRUTCN H20 pure stable anchor。
- 初始化：来自 earlier low-LR/SWA anchor candidate。
- 最佳 validation：e4 `valid_loss=0.4720815`，`best_valid_loss=0.4720816`。
- checkpoint：`resources/experiments/modeldev_20260510_grutcn_anchor_e7_ultralow_p2/checkpoints/model-epoch=04-best_valid_loss=0.47.pth`。
- locked audit：`lockaudit_20260510_grutcn_anchor_e7_ultralow_p2`。
- audit 结果：average rollout loss `0.5632579327`；h50 `E_q=0.0853667619`、`E_v=0.3810590338`、`E_omega=0.3012506581`；mean `E_q=0.0437535938`。
- 结论：目前最强 validation checkpoint，但不是 locked winner；未击败 `gru_H10`/`gru_H20` 关键目标。保留，不从 audit 细节反向调参。

### `modeldev_20260510_grutcn_anchor_H50_fromH20_ultralow_p1`

- 目的：测试 H20 checkpoint 跨 history_length 到 H50。
- e0/e1 validation：`0.9261976` -> `0.8817936`。
- 结论：明显 validation failure，停止 H50，无 audit；保留 artifacts。

### `modeldev_20260510_grutcn_anchor_H10_fromH20_ultralow_p1`

- 目的：测试 H20 checkpoint 跨 history_length 到 H10。
- e0/e1 validation：`0.6045368` -> `0.5921974`。
- 结论：仍远高于 `0.50` sprint gate，停止 H10，无 audit；保留 artifacts。

### `modeldev_20260510_grutcn_no_output_gain_H20_from_ultralow_p1`

- 代码变化：从 GRUTCN 输出中删除整体 `output_gain`，尝试避免 TCN anchor 被全局重缩放。
- e0 validation：`1.3612511`。
- 结论：删除 `output_gain` 破坏 checkpoint 输出尺度，立即停止，无 audit；随后恢复 `output_gain`。

### `modeldev_20260510_grutcn_refreshboost_H20_from_ultralow_p1`

- 代码变化：恢复 `output_gain`，增加 `memory_refresh_boost=1.15`，让 memory refresh 更偏向当前窗口隐状态。
- e0/e1/e2 validation：`0.4836859` -> `0.4813507` -> `0.4818282`。
- 结论：未恢复到 `<=0.4725` audit screening band，停止，无 audit；未读取 horizon/test metric 内容。

### `modeldev_20260510_grutcn_anchor_noise_H20_from_ultralow_p1`

- trick 来源：`trick.md` 的 physical/noise robustness 思路。
- 代码/协议变化：
  - `scripts/config.py` 增加默认关闭的 `input_noise_std`、`input_noise_loss_weight`。
  - `scripts/dynamics_learning/lighting.py` 增加 train-only input noise rollout objective，并记录 `train_noisy_loss`、`train_objective`。
  - 验证/测试/评估保持 clean。
- best validation：e5 `valid_loss=0.4722001`，`best_valid_loss=0.4722003`。
- checkpoint：`resources/experiments/modeldev_20260510_grutcn_anchor_noise_H20_from_ultralow_p1/checkpoints/model-epoch=05-best_valid_loss=0.47.pth`。
- locked audit：`lockaudit_20260510_grutcn_anchor_noise_H20_e5_p1`。
- audit 结果：average rollout loss `0.5636401176`；h50 `E_q=0.0854911`、`E_v=0.3807892`、`E_omega=0.3017333`；mean `E_q=0.0438164`。
- 结论：诚实 locked audit 失败；保留 artifacts；不从 audit 细节调参。

### `modeldev_20260510_grutcn_feedbacktail_H20_from_noisee5_p1`

- 代码/协议变化：
  - `scripts/config.py` 增加默认关闭 `feedback_noise_std`、`rollout_loss_tail_weight`。
  - `scripts/dynamics_learning/lighting.py` 增加 quaternion-safe state noise、train-only feedback noise、可选 normalized linear tail weighting。
  - existing input-noise 现在扰动后会重新归一化 quaternion history。
- 初始化：physical-noise e5 checkpoint。
- e0/e1/e2 validation：`0.4727978` -> `0.4730892` -> `0.4725238`。
- 结论：e2 是新低但仍高于预声明 `<=0.4725` audit screening band 约 `2.4e-5`，按门槛停止，无 audit；tmux/进程已停止，artifacts 保留。
- 下一步理由：避免为了 `1e-4` 级 validation gain 过度 audit；转向增强 encoder-side temporal context 表达力。

### `modeldev_20260510_grutcn_temporalrefine_H20_from_ultralow_e4_p1`

- 代码变化：`scripts/dynamics_learning/models/grutcn.py` 新增 checkpoint-safe encoder-side temporal refiner：`LayerNorm -> MultiheadAttention -> LayerNorm -> FFN`，zero-init output residual 接在 `encoder_norm(enc_seq)` 后、`context_gru(enc_seq)` 前。
- 初始化：`modeldev_20260510_grutcn_anchor_e7_ultralow_p2/checkpoints/model-epoch=04-best_valid_loss=0.47.pth`。
- validation：e0 `0.4727792`，e1 `0.4732321`，e2 `0.4731706`，e3 `0.4729020`；best `0.4727792442` 未进入 `<=0.4725` screening band。
- 结论：natural early stop，无 locked audit；tmux/进程已退出，artifacts 保留；horizon/test metric 内容未读取。
- 下一步理由：temporal refiner 没带来 validation 改善，转向 decoder-side dual latent context + raw history residual，仍保持 zero-init/checkpoint calibration。

## 较早实验摘要

### TCNLSTM 系列

- `modeldev_20260508_tcnlstm_gate_H20_p1`：validation gate 失败并清理；不恢复。
- `modeldev_20260509_tcnlstm_context_residual_H20_p1`：best `0.6649035` at e12；e16 `valid_loss=0.6828123`、`valid_v=0.2939221`；validation-only 失败并清理；不恢复。
- 结论：当前 sprint 重点转向 GRUTCN，但 TCNLSTM 仍可在新结构故事明确时继续开发。

### GRUTCN anchored context residual

- `modeldev_20260509_grutcn_anchored_context_residual_H20_p1`：natural early stop，best e22 `best_valid_loss=0.6142662`。
- locked audit：average rollout loss `0.5747820139`；h50 `E_q=0.08536557`、`E_v=0.40000114`、`E_omega=0.29806767`。
- 结论：比 TCN H10 部分 quaternion 指标好，但未击败最强 GRU targets；保留 checkpoint，不标记 winner。

### GRUTCN low-LR/SWA anchor

- `modeldev_20260509_grutcn_anchor_H20_e10_lowlr_swaft_p1`：best validation e7 `0.4776078`。
- locked audit：average rollout loss `0.5678076148`；h50 `E_q=0.0856633007`、`E_v=0.3885666346`、`E_omega=0.3016224673`。
- 结论：validation 强，但 locked audit 失败；后续 tiny/ultralow LR 继续降低 validation，但仍没有 locked winner。

## 已知代码/协议变更

- `scripts/dynamics_learning/lighting.py`：支持 non-finite validation 传播到 `best_valid_loss`，避免 NaN run 逃过 early stopping。
- `scripts/run_neurobem_sweep.sh`：避免从 non-finite log 对应的 `last_model.pth` 恢复；rerun 日志不覆盖旧日志。
- `scripts/sync_to_gpu.sh`：默认排除 `resources/experiments/`，避免 Mac-to-remote sync 覆盖远程实验结果。
- `scripts/config.py` / `scripts/train.py`：支持 `--init_from_checkpoint`，以 `strict=False` 加载模型权重，不恢复 optimizer/scheduler/callback state，并打印 missing/unexpected keys。
- SWA trick 已用过：来自 `trick.md` sections 3.3/3.4，目的是平滑 late-training 参数；相关行为必须记录。
- input-noise、feedback-noise、tail-weighted rollout loss 已加入为默认关闭选项；训练可开启，validation/test/eval 保持 clean。

## 当前开放问题

- TCNLSTM true-anchor 已拿到 partial locked win：`h50_E_v=0.3444223`，优于 `gru_H20=0.353015` 和 `tcn_H10=0.347036`；但还不是全面 winner。
- 下一阶段重点是保持 TCNLSTM 的 velocity 优势，同时改善 h50 quaternion、h50 omega 和 mean quaternion。
- GRUTCN validation 曾被压到 `0.4720816` 附近，但 locked h50 quaternion/velocity/omega 仍不够。
- temporal refiner 已验证：best 只到 `0.4727792442`，未进入 `<=0.4725` screening band，不 audit。
- GRUTCN dual/raw residual 已确认不是冻结候选；优先沿 TCNLSTM true-anchor 方向做下一轮结构改进或训练协议筛选。
- 下一步应让结果分析 + 架构 agent 辅助判断 TCNLSTM 后续结构，不要继续做 tiny adapter 堆叠。

## 记录模板

### <experiment_id> - <YYYY-MM-DD HH:mm CST>

- 类型：training / locked audit / code change / cleanup / decision。
- 模型与配置：
- 初始化 checkpoint：
- 最佳 validation：
- locked audit 结果：仅冻结候选完成后填写。
- 决策：
- 是否读取 horizon/test metric 内容：
- 文件改动/同步/检查：
- artifacts 处理：
